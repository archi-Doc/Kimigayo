func classify(text: string) -> string
    return match text
        "ok" => "accepted"
        let other => other

public func main()
    writeLine(classify("ok"))
    writeLine(classify("other"))
    let number = match 2
        0 => 10
        1 => 20
        _ => 30
    if number == 30 => writeLine("matched")
