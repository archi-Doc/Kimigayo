func echo(text: string) -> string => text

public func main()
    let text = "apple"
    if text == "apple" => Console.writeLine("equal")
    if text < echo("banana") => Console.writeLine("ordered")
    Console.writeLine(text)
