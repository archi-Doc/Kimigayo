public func main()
    var text = "first"
    var i = 0
    while i < 3
        if i == 1 => writeLine(text)
        text = "next"
        i += 1
    text = text
    defer => writeLine(text)
    writeLine("done")
