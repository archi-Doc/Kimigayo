var pair = ("first", "second")
pair = pair@move
pair = ("replacement", "last")

let numbers: [3 of i32] = [1, 2, 3]
let copied = numbers
let nested = ("outer", (42, "inner"))

var i = 0
while i < 3
    let words: [2 of string] = ["iteration", "cleanup"]
    i += 1

Console.writeLine("aggregates complete")
