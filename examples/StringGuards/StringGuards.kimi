func same(a: ref/string, b: ref/string) -> bool => a == b

func choose(text: string, wanted: ref/string) -> string
    return match text
        let value if same(value, wanted) => value
        _ => "other"

public func main() -> ()
    Console.writeLine(choose("hello", "hello"))
    Console.writeLine(choose("different", "hello"))
