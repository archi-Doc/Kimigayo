# Kimigayo Compiler Completion Plan

## 1. Goal and Scope

### Baseline

Complete the Kimigayo compiler for **all settled language rules and implementation contracts in the finalized specification**, preserving existing supported programs and required rejection behavior. Minimize allocations, retained memory, compilation cost, generated work, and execution time where measurements and semantic proofs justify changes. This is the authoritative implementation design and execution record for this effort, not a declaration of compiler completion.

**Execution boundary:** stopped at 2026-09-16T06:32:39Z; elapsed **67m03s** from `2026-09-16T05:25:36Z`. Soft deadline was `06:25:36Z`. No new unit began after the deadline; only the active I3-G27 unit's required verification and final checkpoint continued. Seven units DONE; M2/I3 remains IN_PROGRESS. No draft edits or NativeAOT.

### Specification authority

- `SPEC.md` identifies Chapters 1–22 and Appendix A as normative. Under §1.3, unqualified declarative rules are normative; examples do not introduce APIs or override rules. Each concept's owning section controls its cross-references.
- Appendix B provides optional algorithms; Appendix F is a non-normative syntax summary. Appendix D identifies boundaries rather than granting syntax or permissions. `STATUS.md`, tests, comments, and this plan describe implementation or proposals, not language authority.
- The dependency/artifact rules are integrated into §§18, 20.8, 21.3.7 and A.16. They are settled work, not deferred simply because implementation is absent.
- `SPEC.md` explicitly supersedes the testing draft's old `$require` return behavior with §17.5: false verification records failure; condition/message temporary cleanup precedes Abort; ordinary enclosing cleanup does not run after Abort.
- The withdrawn Composition Root design grants no Entry/Provider declarations or selection. Built-in `$abort`, `$expect`, and `$require` have independent settled rules.
- No draft proposal is used as an independent acceptance criterion. New discoveries of contradictory normative text must enter §5 before dependent implementation.

### Included and excluded work

Include the requirement families R1–R24 in §4, including specified features currently rejected by the executable subset. Windows x64 is the initial native profile; Library inspection output and common generation from source dependencies are in scope, without a stable external Kimigayo ABI.

Exclude undefined public APIs and deferred extensions in Appendix D. In particular: Composition Entry/Provider; source concurrency; runtime Contract Views and general checked casts; virtual/override dispatch; user-defined arithmetic; string `+`/`+=` acquisition; additional Pattern forms; dynamic Non-Copy element Move; mutable Slice; raw-storage acquisition/safe-reference conversion APIs; public Exchange/Swap spelling; private binary distribution; network registries/version ranges; persistent object-code/generation-plan caches; extra targets; debug information; and NativeAOT. Preserve their specified rejection boundaries. Do not exclude settled object/Weak APIs, collection mutation, interpolation, source packages, or test membership merely because nearby extensions are deferred.

Mods and testing contain settled semantic contracts with undefined external interfaces. Implement independently testable internal semantics, but do not invent the interfaces listed in G1–G2. Their separate specification work is outside this effort; integrated acceptance remains explicitly blocked where it depends on that work. An internal collection representation is an implementation choice, not a requirement to standardize a public collection ABI.

## 2. Execution State

### Current snapshot

A later independent verification pass (I3-V1, §8) re-read the clean working tree at HEAD `680ed552c8246463f6fbf7de5c1b8e7e27396e5c`, reproduced the recorded gates, verified the I3-G24…G27 / I3-A8…A10 scope directly against the source and SPEC, and corrected one Contract constraint-subject defect; current full suites are 6,430 each.

This execution freshly read HEAD `3f85460088f2cf7b6d3e6543d9ae03cf63c4e5f7`, the clean working tree, instructions and latest PLAN at `2026-09-16T05:25:36Z`. All 61 preceding checkpoint source hashes match; prior work is committed and preserved. Current evidence is under `TestResults/timed-execution-20260916-052536/`. Seven bounded units are DONE: I3-G24/G25/G26/G27 and I3-A8/A9/A10. The unit active at the deadline finished its required verification; no later unit began. M2/I3 remains IN_PROGRESS; per-unit states and verification records appear in §8.

| Field | Value |
| --- | --- |
| Last updated | 2026-09-16 (Asia/Tokyo); timed execution checkpoints below use UTC |
| Code baseline | Current execution: clean `3f85460088f2cf7b6d3e6543d9ae03cf63c4e5f7`. Previous execution began at `b0096b50801843b45244eda30a18b5c5d3f92db4`; earlier checkpoints retain their own baselines below. |
| Working directory | `C:\Users\bwff1\repos\archi-Doc\Kimigayo` |
| Initial user changes | None; clean checkout. All 61 preceding checkpoint source hashes match. Initial hashes, comparison and diff identify the baseline. |
| Changes in this effort | Numeric/Identity conversions, Constant-readable lengths, API exposure/default environments, fixed-array inference/arguments, fragment header/base merging, Project dependency resolution and locks, module-local whole-graph checking, CLI/example coverage, product inline-Test filtering, bounded Windows verification and documentation. Intervening user changes are preserved. |
| Planning readiness | `PARTIALLY_READY` |
| Current milestone | M2/I3 remains IN_PROGRESS. Current units validate normalized projection input and conformance dependencies in expression/declaration certificates, with module integration. General role/formation proofs under M4/M7 and other milestone states remain open. |
| Ready work | M1 evidence/coverage setup; M2 semantic completion; M3 project graph/configuration work. Design can proceed for other unblocked items in dependency order |
| Blocking conditions | G1–G2 public interfaces remain unresolved. G3 resolved with current restore assets; G4 resolved with authorized tool execution. G5 remains open for subsequent families. |
| Current verification | Through I3-V1: Debug/Release builds PASS with zero warnings/errors; full suites 6,430 each PASS, zero failed/skipped. The preceding timed execution added 134 managed cases and was independently re-verified at 6,418 each before I3-V1 added 12. Prior evidence remains historical; native NOT_RUN and NativeAOT excluded. |

Evidence labels throughout: **Verified fact** = directly inspected source/configuration or current command result; **Evidence-based inference** = conclusion from those facts, without current execution; **Proposed change** = future work; **Unverified assumption** = premise still requiring investigation. A current source path is evidence of code, not proof of runtime conformance.

Item states: `TODO`, `IN_PROGRESS`, `IMPLEMENTED_UNVERIFIED`, `DONE`, `BLOCKED`, `NOT_APPLICABLE`. `DONE` requires its acceptance criteria and required verification; neither historical success nor code presence alone qualifies. Verification results use `PASS`, `FAIL`, `NOT_RUN`, `BLOCKED`.

### Progress

| ID | State | Completion evidence or remaining work | Next action |
| --- | --- | --- | --- |
| M1 | IN_PROGRESS | I2 DONE: Debug/Release baselines, exact MTP filters, nonzero counts, hash records and tested Windows job containment. I1 numeric clause inventory complete; remaining-family audit open | Continue I1/G5 before subsequent families; reuse current evidence only while relevant source/artifact identities match |
| M2 | IN_PROGRESS | I4 numeric and currently executable owned Identity Acquisition subranges DONE; I3 Constant-readable lengths, ordinary declaration APIs, static defaults, local fixed-array inference, selected Layout/fragment validation, distinct Type arities, current-grammar Origin fragment audit, grouped pair reconstruction and annotation representation/certificate safety and grouped nested-array Type-argument disambiguation plus Property header/storage API-access and accessor receiver formation/API-access plus named function projection API-exposure and I3-P1/P2 explicit Property/accessor and I3-C1/C2 declaration-constraint projection and I3-R1/R2 requirement-parsing subranges DONE | Earlier timed units remain DONE; current I3-G18 through I3-G23 and I3-A6/A7 cover retained projection certificate validity; I3-G24/G25/G26/G27 and I3-A8/A9/A10 are DONE for projected-call normalization, conditional premise validity/access, associated-projection subjects, module integration and conditional Property certificates; annotation and general role/formation proofs remain open under M4/M7 dependencies. Other Identity representations and Borrow/Reborrow depend on later milestones |
| M3 | IN_PROGRESS | I5 Project-only DAG resolution, module-local whole-graph checking and I6 deterministic locks/restore verified; persistent semantic records and native generation remain unfinished | Finish the I5 record/conformance audit; continue I3 selected Attributes and M4 prerequisites |
| M4 | TODO | General Origins, Loans, effects and usage legality | Implement I7–I8 after M2 and module identity foundation |
| M5 | TODO | Struct/Property/construction/deinit/static execution | Implement I9–I10 after M4 |
| M6 | TODO | Enum/Pattern execution and aggregate control flow | Implement I11–I12 after M4–M5 layout foundation |
| M7 | TODO | Universal generic verification and full specialization | Implement I13–I14 after semantic prerequisites |
| M8 | TODO | Generic generation, entries, metadata and frames | Implement I15–I16 after M7 |
| M9 | TODO | Function values, Closure capture and indirect calls | Implement I17–I18 after M7–M8 |
| M10 | TODO | Object/Weak and runtime struct tests | Implement I19–I20 after M5, M8, M9 |
| M11 | TODO | Index/Range/Slice/collections/iteration | Implement I21–I22 after M4, M6–M8 |
| M12 | TODO | Core comparison mappings, Stringify and interpolation | Implement I23–I24 after M5, M7–M8 |
| M13 | TODO | FFI, Library output and generalized native connection | Implement I25–I26 after M3–M5; native checks need G4 |
| M14 | TODO | Packages, content stores and verified semantic reuse | Implement I27–I28 after M3; complete semantic records after M4–M13 |
| M15 | TODO | Internal Mod sequencing/provisional Binding is executable work | Implement I29; external adapter I30 is blocked by G2 |
| M16 | IN_PROGRESS | I31 product exclusion of inline Test functions is a prerequisite for correct M3 product inputs; discovery/test generation remain unfinished | Product filtering verified; implement discovery/test compilation under their prerequisites and keep I32/I33 and their dependencies separate, with public runner blocked by G1 |
| M17 | TODO | Full conformance, performance and documentation closure | Implement I34–I35 after preceding applicable milestones |

Update this table and §8 after every meaningful implementation/verification change. For a partially completed milestone, identify completed I/T items; never mark the whole milestone complete while interface or verification dependencies remain.

## 3. Current State and Architecture

### Actual processing path

| Stage / entry points | Actual input and output | Support and needed work |
| --- | --- | --- |
| CLI: `Kimi/Unit/Command/{BuildCommand,EmitCommand,RunCommand,DefaultCommand}.cs`; `Solution` in `Kimi/SolutionAndProject/Solution.cs` | Input path/options → selected Project(s); implicit single-source Application supported | **Verified fact:** build/run/emit/check/restore commands exist; Project-only restore/check includes bare `--locked`. Package and test commands remain unfinished |
| `Project.BuildCore` / `BuildTarget`, `Compilation.Prepare` | Project configuration and source bytes → one target-specific Compilation, environment variables and build metadata | **Verified fact:** Project resolution fixes configuration/source bytes and direct module mappings before parsing. `Dependencies`, `TestDependencies`, settings and default aliases use defining-module environments; nonempty legacy `KotonohaArray` is rejected. Persistent content/processing records remain M14 work |
| `SourceDocument.FromUtf8`; `Kotonoha.AddSource` / `ParseSource`; `Tokenizer`, `TokenReader`, `RootKoto.Parse` | Immutable source snapshots and source-local `CodeContext` → merged Koto declarations and `GeneratedFunction` for root executable syntax | **Verified fact:** source contexts and diagnostics are retained; tokenizer is disposed. Reuse this representation. Audit remaining syntax/membership/fragment rules; do not add a separate Bound Tree |
| `Compilation.Bind` → `Binding.Bind(BindingMode.Final)` | Existing Koto → `BoundType`, `BoundSymbol`, selected call/Pattern/Property/conversion information, issues and obligations | **Verified fact:** pass resets semantic fields and ownership, binds schemas/constraints/headers/storage/bodies, verifies capabilities/conformances and acquisitions. It does not execute Mods. General effects, generic body proofs and usage deadlines need completion within this architecture |
| `Binding.CheckStartup`, `ControlFlowAnalysis`, `BindingControlFlowTypes` | Selected source and bound facts → startup selection, structural result/transfer facts, flow issues and pending Binding | **Verified fact:** control-flow checking is separate from execution reachability. Preserve this distinction for unreachable syntax, `require`, covered arms and cleanup |
| `OwnershipAnalysis.Analyze` → pooled `OwnershipBody` | Committed Koto operations and flow → Places, value operations, CFG edges, initialization/Move/Loan facts, cleanup/result plans | **Verified fact:** `BuildBody` and solver use reused storage. Current captures/defaults explicitly call `Unsupported`; one unchecked body invalidates all body certificates. General borrowing/effects are prerequisite work, not a permission to bypass this gate |
| `LlvmEmitter.TryPrepare` / `CheckInputs`; `BodyLowering.Lower` | Current verified ownership bodies and `FunctionAbiPool` → checked physical `EmissionModule` | **Verified fact:** rejects external modules, Library, nested declaration containers, unsupported signatures/captures/specializations. Registers signatures before bodies for recursion; failure clears the module. Lowering validates actual plans, not just a stale boolean |
| `EmissionModule`, `EmissionFunction`, `LlvmModuleWriter`, `WindowsLowering` | Compact physical opcodes/operand arrays, constants, layouts and signatures → LLVM text | **Verified fact:** physical module does not hold Koto/Binding/ownership objects. Preserve syntax-free physical pooling and shared definition/call ABI contracts; add opcodes only for needed behavior |
| `EmissionArtifacts.Publish`, `ArtifactFiles`, `ArtifactPaths` | Checked module/configuration → `.ll` and `.link.json` with identities/hashes | **Verified fact:** publication is separate from inspection `WriteIr`. Extend records/Library/dependency contracts; successful text generation is not LLVM or native success |
| `NativeToolchain.Build`, `ToolchainResolver`, `WindowsProfile`, `Kernel32Imports` | Matched IR/manifest, actual tools and libraries → checked objects, linked executable and build record | **Verified fact:** checks versions/hashes and actual undefined symbols; fixed runtime/backend allowlist is insufficient for general source FFI/dependency supply. M13 extends it using §20.8.2 |
| `Project.Run` / native launch | Existing executable and latest record → program output/exit | **Verified fact:** run does not rebuild. Preserve direct-executable behavior, input summary, byte forwarding and cancellation |

There is no evidence for a separate Bound Tree. Terms such as “bound operation” describe facts attached to Koto or retained tables. `OwnershipBody` is the existing semantic CFG/operation representation; `EmissionModule` is the later physical plan. Generic semantic plans and metadata proposed below extend these responsibilities rather than replacing the whole pipeline.

### Current support by requirement, stage and input range

This support inventory originated in planning; §8 records current execution evidence and updated rows identify delivered slices. Whole-family classification remains partial or unknown unless the range is explicit. Existing test classes are under `xUnitTest/Tests/`.

| Requirement range | Stage / classification | Evidence and limitation |
| --- | --- | --- |
| R1 UTF-8, Unicode/NFC, layout, literals, selected directives | Parsing: Implemented for inspected paths; complete conformance: Unknown | `Lexing/`, `Parsing/`, `SourceDocument`; `UnicodeIdentifierTest`, `SourceEncodingTest`, `DirectiveConditionValidationTest`, `KotonohaSerializationTest`. No wholesale parser rewrite scheduled |
| R2–R5 names, complete Types, declaration Origins, ordinary generic inference, static Contracts, Property signatures | Binding: Partially Implemented | `Binding.cs`, `Binding.Types.cs`, `Binding.Properties.cs`, `Binding.Contracts.cs`, candidate/constraint partials; `ReceiverShorthandTest`, `TypeBindingTest`, `ContractBindingTest`, `PropertyBindingTest`. Access-domain, symbolic proof and execution completion remain |
| R3 numeric conversions | Binding/IR: numeric pairs implemented for the Windows <=64-bit float conversion profile; general Identity Acquisition still partial | `Binding.BindConversion` now accepts integer pairs, float widening/narrowing/identity, <=64-bit integer↔float and direct integer literal fitting. `NumericConversionEmissionTest` adds range/rounding/Abort/reload/allocation coverage; typed 128-bit↔float remains rejected under §21.5.3. See current §8 verification. |
| R6–R7 ordinary owned values and static Tuple/fixed-array paths | Ownership/IR: Partially Implemented | `OwnershipAnalysis.Elements.cs`, `OwnershipBody.MovePaths.cs`, `BodyLowering.MovePaths.cs`; `ElementParameterMoveEmissionTest.ParametersTransferOnlyTheSelectedResponsibility`. Local/parameter static Move and local repair exist; dynamic Non-Copy Move remains forbidden, general field/borrow/effect work remains |
| R6 shared string comparisons/arguments | Ownership/IR: Implemented for the narrow inspected range; general borrowing: Partially Implemented | `ReferenceTypes`, comparison/element paths; `ReferenceEmissionTest`, `ElementBorrowOwnerEmissionTest`. Owned locals/parameters/temporaries with static string elements supported; ref storage/results, uniq, nested borrowing and general receiver paths not certified |
| R8 struct construction, custom/computed Property execution, deinit | Parsing/signature Binding: Partial; executable path: Not Implemented | `Binding.Expressions.cs` explicitly rejects constructors/destructors; `LlvmEmitter.CheckInputs` rejects containers. `AggregateLayoutPool.Get` accepts only owned Tuples/fixed arrays. Existing Property declarations must be reused |
| R9 control flow/results/defer | Analysis/IR: Partially Implemented | `ControlFlowAnalysis`, `StructuralCompletion`, ownership result/cleanup plans and BodyLowering result partials; `ControlFlowConformanceTest`, `DeferredEmissionTest`, `AggregateResultEmissionTest`. General for/iterator/borrowed result paths depend on later milestones |
| R10 enum construction/Pattern coverage | Binding and selected ownership: Partially Implemented; enum layout/native: Not Implemented | `Binding.Enums.cs`, `Binding.Patterns.cs`, `OwnershipAnalysis.Enums.cs`, `OwnershipAnalysis.Match.cs`; `EnumBindingTest`, `EnumOwnershipTest`, `PatternBindingTest`. Scalar/string match code is reusable; no enum native claim |
| R11 Closures/function values; R5 full specialization | Parsing/contract representation: Partial; executable path: Not Implemented | `Binding.Expressions.cs` rejects anonymous/specialization functions; captures/defaults rejected by ownership and emitter. `Binding.CallableContracts.cs` is not a Closure runtime |
| R12 runtime struct `is` | Binding: Partially Implemented; execution: Not Implemented | `BindRuntimeTypeTest` retains `BoundRuntimeTypeTest` for supported concrete struct/object Types, rejects dependent targets. `RuntimeTypeTest`; no completed metadata/ownership emission path established |
| R12 object/Weak catalog | Incorrect or Inconsistent with current specification | `CoreIntrinsics.cs` still says object public spellings are deferred and has a missing unnamed ObjectOwnership entry; §§13.5.8–9 and 22.1 now specify them. Fix catalog and full implementation; do not treat the stale comment as authority |
| R13–R15 sequence/collection/Core runtime | Partially Implemented overall; missing catalog entries verified | `CoreIntrinsics` supplies Copy/Owned/Callable/writeLine/Option/Result and marks Array/Index/Range/Slice/Dictionary/Stringify/comparison/iteration entries Missing. Scalar/string literals and fixed aggregates already execute in code; remaining operations need full paths |
| R16 startup/Abort/string runtime | Partially Implemented | `Binding.Startup.cs`, `WindowsRuntime.ll.in`, `WindowsLowering.Abort.cs`; startup selection, UTF-8 output, allocation/free and Abort paths exist. Demand-driven static execution and object runtime remain |
| R17 raw pointer/C imports | Syntax/type representation: Partial; end-to-end: Unknown or unavailable through current container gate | `Unsafe` syntax, Type representations, fixed kernel32 import runtime do not prove general source imports. M13 must audit source imports and reach intended unsafe/ABI checks |
| R18–R19 dependencies/packages/semantic persistence | Not Implemented on inspected product entry path | `Compilation.Prepare` placeholder, old ProjectFile settings, command registry/default rejection; no resolution integrated into `BuildTarget`. Internal Kotonoha serialization is not package support |
| R20 Mods; R22 language test execution | Not Implemented on inspected product entry path | `Compilation.Bind` explicitly documents no Mod execution; compiler commands have no test command; `#Test` syntax availability is not runner support. Interface portions also have G1–G2 |
| R21 emit/build/run and Windows native support | Partially Implemented | `EmissionArtifactsTest`, `NativeToolchainTest`, `SolutionInputTest`, backend scripts. Application-only generation gate; generalized native input closure and immutable historical records remain |
| R23–R24 current regression/performance claims | Unknown for this checkout's execution | Existing allocation tests/benchmarks and STATUS results are leads only; current V3–V12 remain unexecuted or blocked |

### Reload, invalidation and reuse

**Verified fact:** `Kotonoha.OnDeserialized` reconstructs the tree by reparsing saved source documents into fresh contexts; syntax-tree edits are not persisted. `Binding.Bind` resets semantic fields and calls `InvalidateOwnership`. `LlvmEmitter` clears its declaration-to-ABI map and function context in `finally`, including failure. `AggregateLayoutPool` separates physical shapes from temporary BoundType lookup; it currently limits depth to 64 and offsets/counts/sizes to signed 32-bit implementation storage.

**Proposed change:** keep same-snapshot warm reuse; audit source replacement for retained syntax and rebuild source-local environments. Introduce versioned semantic records only for R19, with verified dependencies, current source mappings and corruption fallback. Physical equality never establishes Type identity, Origin equality, Copy capability or cached proof validity. Document justified resource/representation limits under §21.1.1 instead of silently truncating or claiming support for arbitrarily large layouts.

## 4. Specification Requirements

The following are stable requirement families. Each includes all applicable normative clauses of its cited sections, not just the examples here. M1 must expand families into clause-level T variants in this file before declaring a family complete. Appendix A coverage is mandatory, whereas optional reference algorithms are not acceptance criteria.

| ID | Required observable behavior / owning specification | Implementation / verification coverage |
| --- | --- | --- |
| R1 | §§2, 19; A.1–2, A.6: immutable UTF-8 source identity, Unicode 15.0 identifiers/NFC, exact literals and source spans; layout/Body syntax; immediate case-sensitive directives and precise excluded-syntax boundary | M2/M15; T1–T3, T27; V3–V4, V8 |
| R2 | §§6.1–6.2, 6.4–6.5, 9, 18.1; A.3–4: namespace/role separation, source-local aliases, fragments/access/inheritance, modifier/Attribute validation and API reachability; no alias leakage or inherited fallback after committed lookup | M2/M3/M5/M16; T3–T5, T23, T28; V4, V8–V10 |
| R3 | §§3, 4.1–4.4, 10, 13.2–13.5.4; A.6, A.12, A.14: complete Types/Semantics, fixed lengths, exact fitting, required constant evaluation, checked numeric operations/conversions and specified FP behavior | M2/M7/M8; T2, T6–T8, T15; V4–V7, V11 |
| R4 | §§7.1–7.5, 10, 12.2/12.4, 13.7; A.3: select a unique operation before usage checks; no fallback after Loan/access failure. Receiver first for bound calls; explicit arguments once in source order; defaults in declaration environment; immutable parameters and normal-result acquisition | M2/M4/M5/M7; T5, T9, T12; V4–V8 |
| R5 | §8, §§10.5/10.7/10.8, 21.3–21.4.6; A.11–12, A.14: universal generic definition proofs; restricted constraint calculus, verified witnesses/associated Types; closed mandatory full specialization; legitimate deferred representation obligations; bounded deterministic shared generation/entries/frames | M7/M8; T14–T16, T25, T29; V4–V8, V11 |
| R6 | §§3.3–3.8, 15.2–15.6/15.8–15.9; A.3/A.8/A.12–14: Origins, return contracts, actual Loan anchors, reborrowing, variance, retained dependencies and static/call effects. Unknown proof never certifies usage; Owned is not thread safety | M4/M7/M9–M11; T10–T11, T16–T20; V4–V8, V11 |
| R7 | §§3.5–3.6, 15.1, 16; A.7/A.14: distinct construction/history/completeness, sparse static Move Paths; no borrowed Non-Copy extraction or Partial Move across deinit; secure result → reverse logical cleanup → normal delivery; Abort/divergence stops subsequent work | M4–M11; T9–T13, T17–T20, T30; V4–V8 |
| R8 | §§6.2, 11, 16.3, 21.1; A.4/A.7/A.14: standard versus accessor operations, first placement/custom setters, receiver projection, constructors/base completion/deinit and static storage; Kimigayo alignment sorting versus C packing-16 layout | M5; T11–T13, T22; V4–V8 |
| R9 | §§12, 14, 17.4; A.9–10/A.15: expression contexts, result inference, nearest transfer targets, static versus runtime paths, refinement validity, coverage, guard/body identities and mandatory warnings; for uses the defined non-lending protocol | M2/M4/M6/M11; T9–T10, T13, T20, T26; V4–V8 |
| R10 | §§3.5.2, 6.3, 14.8, 17.2, 22.1; A.10–11: enum identity/Case order/payload acquisition, conditional Copy, Option/Result contracts, positional owned/shared Pattern acquisition and complete cleanup | M6/M7; T13–T14, T19–T20; V4–V8 |
| R11 | §§3.2.1, 7.6, 8.6, 15.8.2–3, 21.2.4–5; A.8/A.14: Function Items, capture order/mutability, callable receivers, Owned common-type erasure, per-call Origins and independent results; empty/inline/heap environments and typed indirect ABI | M9; T16–T17; V4–V8, V11 |
| R12 | §§3.2.2/3.3.3–5, 12.4.4, 13.5.7–9/13.6.1, 21.2.2–3; A.8/A.14: specified objects/Weak, exact dynamic cleanup/view adjustment, completed ObjectCompatible guarantees, runtime struct tests/refinement, rc/arc counts and cyclic builder publication | M10; T10, T18; V4–V8, V11 |
| R13 | §§4.1–4.6, 14.6, 22.1; A.12–14: fixed arrays, Index/Range/ResolvedRange, saved metadata, O(1) shared Slice operations, SharedReadResult families, permanent iterator exhaustion, literal-only Move/disjointness and correct runtime bounds locations | M4/M7/M11; T8, T11, T19–T20; V4–V8, V11 |
| R14 | §4.7, §12.3.4, §22.1; A.13–14: Array/Dictionary mutation/lookup, duplicate checking before value evaluation, capacity/no-allocation and amortized bounds, insertion order, result responsibility, conservative dependency union and user effects | M11; T20–T21; V4–V8, V11 |
| R15 | §§12.3.3–4, 13.4, 22.1/22.4/22.5.6; A.11/A.14: exact Core identities, built-in/user comparison mappings, UTF-8/NUL strings, scalar Stringify and single-evaluation interpolation; document permitted float spelling choices | M7/M12; T2, T14, T21–T22; V4–V8, V11 |
| R16 | §§14.11, 17.1–17.4, 22.2/22.5; A.9/A.14–15: unique startup, demand-driven static initialization, cycle/shutdown guards, Option/Result failure policy, Abort diagnostic provenance and required warnings; no exception unwinding or silent omitted checks | M4–M6/M13; T9–T10, T12, T22, T26; V4–V7, V9 |
| R17 | §5, §§7.5, 21.5.3–4, 22.3; A.5/A.14: safe pointer holding versus unsafe arithmetic/access/conversions; positive stride and supported round trips; exact direct scalar/raw-pointer C imports, no implicit marshalling; unsupported signatures rejected before generation | M13; T22; V4–V7, V9 |
| R18 | §§18.1–18.5, 18.8, 20.1–20.6/20.8.6; A.1–3/A.16: exact-version DAG/direct names, definition environments, conflict paths, immutable snapshots, product/test lock partitions, restore-only atomic lock changes and old-setting migration | M3/M16; T23–T24, T28; V4, V8–V10 |
| R19 | §§18.6–18.7, 21.3.4; A.16: fixed source package closure and canonical identity; local transactional publication; integrity versus semantic validity; bounded stores/pins/reuse, absence/effect dependencies and source remapping; no-cache semantic equivalence | M14; T24–T25; V4, V8–V9, V11 |
| R20 | §20.7, §6.5 Mod markers; A.1–3: single execution in dependency order, selected-syntax snapshots, provisional Binding/query validity, append-only permitted targets, independent generated contexts/provenance and deterministic order; finalization after generation | M15; T27; V4, V8. External interfaces: G2 |
| R21 | §§20.8, 21.4–21.5, 22.2–22.5; A.14/A.16: check/emit/build/run distinctions, Library inspection, pinned windows-x64-v1/LLVM 22.1.8/backend ABI 2, native member closure, checked publication and current-record execution, cancellation and output | M3/M8/M13/M14; T22–T25, T31; V3–V9 |
| R22 | §§6.5.1, 17.5, 18.8, 20.9, 21.3.7, 22.6; A.17: test-only membership, all-body verification before filters, product plan isolation, once/lazy verification, false require Abort, static identities, one process per case, reliable bounded reporting/recovery | M16; T28–T30; V4, V8, V10–V11. Public profile interfaces: G1 |
| R23 | §§1.3, 17.3–17.4; A.1–17: preserve diagnostic category, stage and original location; unsupported/unresolved operations rejected before finalization even unused; poisoned/stale plans cannot publish; optimization/caches cannot alter acceptance | All milestones; T1–T31; V1–V10/V12 |
| R24 | §4.6.8/4.7.7, §§21.3–21.4, 22.6.5, A.14/A.16; repository guidelines | Reused/interned storage, bounded algorithms, measured cold/warm allocation/retention and throughput; semantic checks before optimization. M1/M8/M11/M14/M16/M17; T15/T21/T25/T29/T32; V11–V12 |

### Acceptance rules for negative and unsupported cases

For every negative case, demonstrate that earlier parsing/Binding checks succeeded as needed to reach the intended check, then assert the category/reason, stage and source span. Expected unsupported rejection is temporary for settled features and cannot satisfy their final positive acceptance. For genuinely deferred language features, keep a targeted rejection case and identify the boundary. Unsafe contract violations have no invented deterministic runtime outcome; test valid operations and static prohibitions, not an assumed crash or recovery.

## 5. Gaps and Decisions

### Open issues

| ID / classification | Evidence / affected requirements and milestones | Resolution needed / current disposition |
| --- | --- | --- |
| G1 `SPEC_GAP` | Appendix D.4 explicitly leaves test temporary-directory API, deadline/grace option forms/defaults, budgets/queues/log retention, transport/schemas, empty-set option, exit allocation, ID encodings/versions/bounds unsettled. R22; I33/M16/M17 | Separate specification decision must finalize those interfaces before a conforming public runner can be completed. I31–I32 internal membership/verification work may proceed. Do not invent option spellings or call exit 0 sufficient success |
| G2 `SPEC_GAP` | §20.7.7/Appendix D: query/marker APIs, assembly compatibility, configuration, cache and host cancellation contracts undefined. R20; I30/M15/M17 | Separate Mod host/interface design. Internal sequencing/invalidation tests I29 may proceed with private test doubles; illustrative `IMod` is not an adopted public API |
| G3 `INVESTIGATION_GAP`, resolved for execution | Historical help discovery reported `Access to the path 'C:\Users\bwff1\AppData\Roaming\NuGet\NuGet.Config' is denied.` At `e47b667`, explicit no-restore Debug build and no-build/no-restore project help/tests succeed. R23–R24 | Current restore assets suffice; no user NuGet settings changed. Reopen if a later required restore cannot run. See current V2–V4 evidence. |
| G4 `INVESTIGATION_GAP`, resolved for execution | Sandbox probes returned `C:\Users\bwff1\repos\archi-Doc\Kimigayo\toolchain\opt.exe: permission denied` (likewise llc/clang/lld-link). Authorized escalation establishes pinned LLVM 22.1.8 versions and ordinary native execution. R21/R23 | Use authorized execution of the pinned tools; retain per-run tool/fixture hashes. No reinstall or unpinned fallback. Each new native target still requires its own verification. |
| G5 `INVESTIGATION_GAP` | Broad source inspection establishes major gates, not every normative clause or all current regressions. STATUS history is not current evidence. All R families/M1/M17 | Before each family starts, map its owning subsections and Appendix A rows to precise T variants, inspect the actual rejecting branch/caller and confirm with focused checks. Mark residual input ranges Unknown; full completion cannot skip this audit |
| G6 `IMPLEMENTATION_MISMATCH` | `CoreIntrinsics` missing object/Weak catalog and stale comment versus §§13.5.8–9/22.1. R12/R15; M10 | Adopt existing specified names/formation/acquisition, update catalog and negative tests. No language decision required |
| G7 `IMPLEMENTATION_MISMATCH` | `ProjectFile.KotonohaArray`, `Compilation.Prepare` placeholder and current command set versus §§18.4–18.6/20.8.6. R18–R19/R21; M3/M14 | Dependencies replacement plus explicit nonempty legacy-setting migration diagnostic. New diagnostics are proposed, not already available identifiers |
| G8 `INVESTIGATION_GAP` | Current `AggregateLayoutPool` depth/Int32 bounds versus §21.1.1 allowance for reasoned stricter backend limits. R3/R8/R24; M5/M8 | Measure representative large/nested layouts; document and diagnose legitimate limits, widen internal fields only where needed. Do not classify every finite limit as a language contradiction |

No unresolved `SPEC_CONFLICT` has been established by this investigation. If one appears, record both authoritative clauses and pause only dependent work. §22.1's broad mention of string concatenation does not override the explicit executable-finalization prohibition in its owning §13.3; it remains excluded. This is an authority resolution, not permission to select an acquisition strategy.

### Design decisions

| ID | Decision and rationale |
| --- | --- |
| D1 | Extend Koto facts, Binding tables, OwnershipBody and physical emission plans. Existing architecture already separates selection, usage legality and physical representation without duplicating syntax |
| D2 | Start cross-module implementation with source-first loading and conservative module-wide invalidation, explicitly permitted by §18.7.3. Add persistent/finer reuse only after no-cache correctness and measurement |
| D3 | Finish universal generic semantics before shared/specialized code generation. Unknown Copy needs correlated behavior, not hidden constraints or treating all values as Move |
| D4 | Introduce call/static/destructor effect fixed points before general object/Property/collection borrowing. Preserve winner selection; later legality failures cannot reopen overload search |
| D5 | Keep sparse integer-ID Place paths and reused arrays/worklists. Extend projections by semantic Field/Case identity; do not expand fixed arrays by length or allocate per use |
| D6 | Establish a valid generic shared baseline and budget-zero behavior, then bounded optional specialization. Entry ABI is budget-independent; explicit full specialization is mandatory. Budget defaults and physical positions are documented compiler choices |
| D7 | For collections, choose private storage meeting §4.7 cost/retention rules; do not block on a stable public storage ABI. Prototype geometric Array growth and Dictionary reusable indexed slots/order links without per-entry allocations; measure before fixing strategy |
| D8 | G1–G2 go to separate specification work under the user's exclusion. Do not ask the user to choose arbitrary interfaces in this implementation plan or silently interpret silence as agreement |
| D9 | Reuse normal string/runtime cleanup and Abort paths for interpolation/tests. Heap-string construction must gain real allocation/free coverage; Static-backed destruction audits alone are insufficient |
| D10 | Performance work is continuous, with M17 closure. Retain existing warm zero-allocation assertions where applicable, but set no universal zero-allocation promise or unsupported percentage speedup |

## 6. Milestones

The milestones below define required work; current implementation and verification states are recorded in §§2, 7 and 8. Existing file/symbol names are evidence anchors; `Binding.*`, `OwnershipAnalysis.*`, `BodyLowering.*`, and `LlvmModuleWriter.*` denote existing partial families. Every explicitly proposed file/symbol is marked **new**. Dependent slices may add private records within existing files where that is the smaller coherent change.

### M1 — Establish executable evidence and clause coverage

- Objective: R23–R24 and the verification foundation for R1–R22. No implementation prerequisite; G3–G4 block execution, not read-only clause mapping.
- I1: create the clause/test inventory in this plan from the owning chapters and A.1–17; inspect only paths implicated by each clause. Compare existing tests with language rules and identify obsolete subset rejections. No blanket source-file rewrite.
- I2: inspect/fix verification configuration only in the implementation phase: `global.json`, project files, `.github/workflows/test.yml`, backend scripts, existing fixture helpers. Establish explicit configuration, runner selection, nonzero test counts, artifact identities and bounded processes. Runner-specific filter syntax must be confirmed after G3.
- Invariants: preserve user changes; don't run tests concurrently against shared fixture/native directories; don't turn historical totals into a current baseline.
- Exit: V2–V3 current baseline recorded, every R family assigned to cases and a milestone, and G5 closed for the next executable slice. Environment failures stay visible. Risk: tests rejecting valid specified features can pass while conformance is incomplete.

### M2 — Complete source/static semantics and numeric operations

- Requirements: R1–R4/R9/R23; depends on M1 inventory, with independent investigation permitted while G3 is open.
- I3: extend `Binding.Access.cs`, `Binding.MemberLookup.cs`, `Binding.CandidateEvaluation.cs`, `Binding.Lengths.cs`, `Binding.Types.cs`, parsing/diagnostic/writer paths only for demonstrated gaps: access domains/fragments, complete nested Semantics/target applications, exact constant-readable lengths, expected-Type boundaries including collection arguments, receiver/default environments and selected Attribute checks. Retain already supported receiver shorthand, Unicode and directive behavior.
- I4: extend `Binding.BindConversion`, `ConversionBinding`, `OwnershipAnalysis.Values.cs`, `BodyLowering.Conversions.cs`, `LlvmModuleWriter.Conversions.cs`: exact integer-literal-to-float fitting, runtime f64→f32 and allowed integer↔float, general identity acquisition where semantics permit. Diagnose prohibited typed i128/u128↔float and bool/char conversions; inspect i128 arithmetic helper needs under M13 rather than silently emitting missing libcalls.
- Invariants: no extra evaluation or premature default literal Type; every adaptation in a chain retains rounding/checks; definition errors versus runtime numeric Abort remain distinct. Unsupported ownership adaptations await M4/M10.
- Exit: applicable nongeneric T1–T8 and affected T26 variants pass V4–V8; numeric admitted pairs reach V5–V6; no redundant replacement of working arithmetic. Generic T8 variants remain assigned to M7–M8, and later-stage T4–T5 variants to their milestones. Main risk: FP overflow boundaries/LLVM poison and expected-Type inference leakage.

### M3 — Source modules, configuration, locks and semantic checking

- Requirements: R2/R18/R21/R23; depends on M2 identity/access foundations. First complete the non-native portion; general dependency execution waits for M13.
- I5: change `ProjectFile`, `ProjectConfiguration`, `Solution`, `Project`, `Compilation.Prepare`, source-local Binding lookup. Add **new** `Kimi/SolutionAndProject/DependencyResolver.cs` and **new** `DependencyLock.cs` for exact-version DAG resolution, direct-name mapping, per-definition settings, conflict paths, Core identity, byte snapshots and product/test partitions. Replace nonempty `KotonohaArray` with a migration error; do not silently reinterpret it.
- I6: add **new** `RestoreCommand.cs` and **new** `CheckCommand.cs` in `Kimi/Unit/Command/`; register through existing command conventions. Restore only resolves local inputs and atomically updates both lock partitions; check performs required semantics without emission/native execution. Stage Package loading through M14 interfaces, rejecting unsupported Package inputs until implemented.
- Invariants: restore alone writes locks, stale empty locks remain errors, source edits don't require lock rewrites, tests can't supply product candidates, module traversal uses bounded explicit worklists. Configurations and source snapshots are fixed once per attempt.
- Exit: Project-only graphs pass T23–T24 via V4/V8/V9; no-cache check is independently verifiable. Package coverage remains assigned to M14. Risks: environment contamination, transitive name exposure, graph explosion and concurrent lock replacement.

### M4 — General ownership, Origins, effects and usage checking

- Requirements: R4/R6–R7/R9/R16/R23; depends on M2 and M3 module identities. Recursive effect tests may first use one module; cross-module closure requires M3.
- I7: extend `Binding.Origins.cs`, `Binding.TypeOrigins.cs`, `Binding.OriginRequirements.cs`, `Binding.ArgumentOperations.cs`, callable/Contract mappings and `BindingModel.cs`. Add **new** `Kimi/Compiler/Analysis/EffectAnalysis.cs` for bounded recursive effect components, static-root access, defaults, destructors, returned anchors and implementation-family ObjectCompatible proof. Separate pending work from Proven/NotProven/Error.
- I8: extend `OwnershipModel`, `OwnershipBody`, element/Move/comparison/checking partials, `BodyLowering.Calls.cs` and reference lowering: general ref/uniq/object-borrow storage and results, explicit reborrow/adaptation, universals/variance, retained dependencies, sparse field paths and suspension, destruction lifetime checks, result Loans. Connect default argument acquisition in declaration environments and partial-call cleanup to the selected call mapping; remove the existing default-parameter rejection only when those operations are checked. Use current snapshots/results/checking continuations instead of a second ownership graph.
- Invariants: Origins don't merge distinct Loan authority; result secured before cleanup; temporary life isn't shortened to Loan end; no two-phase receiver reservation; no default/callee/static reentry hidden by a missing summary. Existing parameter immutability and sparse array paths persist.
- Exit: T9–T11 with direct/recursive/module/cleanup interactions pass V4–V8. General reference ABI and cleanup must land together for native tests; semantic negatives can land first. Risks: unsound cycle proof, stale call summaries, false acceptance after rebind.

### M5 — Structs, Properties, construction, destruction and statics

- Requirements: R2/R7–R8/R16/R21; depends on M4, plus M2 fragment/Attribute validation.
- I9: extend `Binding.Storage.cs`, `Binding.Properties.cs`, `Binding.PropertyMatching.cs`, `Binding.Expressions.cs`, `AggregateLayoutPool`, `WindowsLowering`, `FunctionAbiPool`; implement stored/custom/computed operations, source init/base/deinit, complete Types and logical component layout. Add **new** `BodyLowering.Properties.cs` only if needed to keep this coherent.
- I10: extend ownership cleanup/result and physical module/runtime paths for partial construction and per-layer deinit, getter-result temporaries/custom-set self-assignment, demand-driven static slots with initialization/shutdown state and reverse successful-init cleanup. Relax container emission gates only once all selected members are validated.
- Invariants: standard getters aren't synthesized user functions; physical sorting doesn't change logical effects; no deinit on incomplete layers; base stored at zero; C layout packing 16 and fragment restrictions; first static write initializes before replacement.
- Exit: T11–T12 and layout/FFI layout portion of T22 pass V4–V8. Constructor selection, initialization tracking, cleanup and layout form one minimum native slice; no independently callable placeholder constructor. Risks: inaccessible setters, incomplete self escape, static reentry, zero-size responsibility.

### M6 — Full enum and Pattern execution

- Requirements: R7/R9–R10/R16; depends on M4 and M5 aggregate layout/cleanup foundation.
- I11: reuse `Binding.Enums.cs`, `Binding.Patterns.Model.cs`, `MatchCoverage`, `OwnershipAnalysis.Enums.cs` and Match partials; complete borrowed/nested positional acquisition, correlated read Types, guard candidate restrictions and separate body identities.
- I12: extend `AggregateLayoutPool`, `BodyLowering.Match.cs`, physical instructions/writer for tagged layout, payload construction/partial cleanup, owned/shared Subject decomposition, selected Case destruction and Option/Result execution. Preserve existing scalar/string match path.
- Invariants: acquire Subject once; false-guard effects flow onward; covered arms still checked without runtime arrivals; no general enum payload access or deferred Pattern forms; no borrowed referent destruction.
- Exit: T13–T14 pass V4–V8 for nested/borrowed/non-Copy/guard/Abort/divergent cases. Runtime enum layout and match cleanup must be validated together. Risk: layout bytes mistaken for initialized state or overly powerful coverage inference.

### M7 — Universal generic semantics and closed specialization

- Requirements: R3–R6/R10/R15/R23; depends on M2–M6 semantic contracts (can implement independent proof slices earlier).
- I13: extend existing constraints, schemas, associated Types, conditional conformance, call matching and obligations. Verify unused generic bodies under declared premises; retain correlated Copy/Move and SharedReadResult cases. Complete input-order-independent Type/length/Origin inference, occurs checks and permitted projection normalization.
- I14: add **new** `Kimi/Compiler/Binding/Binding.Specializations.cs` for ordinary header matching, closed keys and inherited contracts; finalize sets before calls/function references, validate family effects, and commit required selected implementation without overload reselection. Representation obligations retain definition site, premise/dependency and deadline.
- Invariants: no hidden body capability premise; specialization cannot rescue invalid ordinary body; Unknown is not successful proof; caller aliases never participate in definition lookup. Runtime Contract extensions stay rejected.
- Exit: T14–T16 semantic and reload cases pass V4/V8; no native claim until M8. Risk: conflating candidate isolation with reusable proof state.

### M8 — Generic generation, metadata, entries and fixed frames

- Requirements: R5/R7/R11–R13/R21/R24; depends on M7, existing layout/ABI and cleanup.
- I15: add **new** `Kimi/Compiler/Emission/GenericGenerationPlan.cs` and **new** `TypeMetadataPool.cs`; extend FunctionAbi/BodyLowering/EmissionModule using typed entry/context pairs, complete ArgKey versus payload CoreId, immutable metadata and operation-context schemas. Required plan worklist distinguishes finite recursion, growing instantiations and inline layout cycles.
- I16: implement exact per-substitution fixed scratch frames, alignment/zero-capacity adapters, budget-independent entries and shared baseline; then bounded optional specialization/deduplication. Adopt Appendix B.7's initial proposal: exclusive all-member choices per sharing class, per-original-generic-function and whole-generation-unit growth limits, one finite nonnegative multiplier, deterministic benefit/cost ordering and bounded exploration. Count body work moved into entries/helpers; do not refund cross-class deduplication savings. Choose numerical defaults by measurement. Product/test generation requests have separate budgets and stable product-based tie breaks.
- Invariants: same-size Types do not imply interchangeable destruction/alignment; no dynamic unknown-substitution storage; no maximum-across-instances frame buckets; no operation-only schema casts. Optional exploration exhaustion falls back to verified baseline, not a new source error; required exhaustion gets a resource diagnostic.
- Exit: T15–T16/T29 pass V4–V8/V11 at budget zero and nonzero, including explicit selection, recursion, no-cache/order changes, frame/probe/unwind and call-result storage. Entries/adapters/frame plans must be delivered as a consistent unit. Risks: generation explosion, accidental ABI dependence on budgets, retained syntax in pools.

### M9 — Callable values and Closures

- Requirements: R4/R6–R7/R11; depends on M7–M8, M4 Loans and M5 cleanup.
- I17: extend `FunctionKoto`, existing callable contracts/Binding expressions and capture records only where semantics require missing information; implement capture resolution/acquisition, Function Items, result expectations, receiver classification and common Function Type erasure.
- I18: extend FunctionAbi, metadata and physical plans for empty/inline/heap environments, typed direct/indirect entries, Shared/Exclusive/Consuming calls and environment destruction. Add **new** `BodyLowering.Callables.cs` and focused **new** `CallableEmissionTest.cs`.
- Invariants: direct-syntax capture order and general-expression allocation order follow §21.2.5.3; callable Copy is independent of receiver kind; no hidden receiver/call-local result escape; consuming partial cleanup exact.
- Exit: T16–T17 pass V4–V8/V11 across repeated shared calls, consuming capture Moves, escaping rejection and indirect/cross-module effects. Risks: accidental borrow extension and adapters acquiring twice.

### M10 — Object ownership, Weak, metadata and runtime tests

- Requirements: R6–R7/R12/R15; depends on M5, M8–M9 and completed effect-family proofs.
- I19: update `CoreDeclaration.cs`, `CoreIntrinsics.cs`, existing runtime Type-test Binding and explicit operations for specified makeObj/makeRc/makeArc/clone/downgrade/upgrade/cyclic factories, exact upcasts/Supports and Effective Type facts. Correct G6; do not introduce a general deep clone or runtime Contract Views.
- I20: add **new** `BodyLowering.Objects.cs` and **new** `ObjectRuntime.ll.in` (or a smaller extension of `WindowsRuntime.ll.in`); implement §21.2.3's object/side-table states, payload +16, count limits/migration, arc ordering, Weak guard lifetime and exact dynamic destruction. Keep current backend external contracts unless measured runtime requirements necessitate validated supply changes.
- Invariants: no blanket Owned on ordinary concrete creation; cyclic payload requires Owned; builder result and cleanup before publication; upgrade secures strong count before object-pointer read; no resurrection; source concurrency remains excluded.
- Exit: T18 passes V4–V8/V11; arc protocol ordering review and controlled native interleaving/fault adapters supplement IR tests. Race tests do not introduce source threads. Risks: use-after-free, count overflow, object identity conflation, unsupported alignment.

### M11 — Sequences, collections and iteration

- Requirements: R6–R7/R9–R10/R13–R15/R24; depends on M4, M6–M8. Dictionary work additionally requires M12/I23 Equatable mappings; run that independent Core slice before I22 Dictionary implementation. Add object-element cases after M10.
- I21: complete Core Index/Range/ResolvedRange/Slice, metadata, fixed-array length integration and shared access. Extend `Binding.Elements.cs`, `ElementAccess`, ownership element paths and lowering; add **new** `BodyLowering.Sequences.cs` for bounds/view operations and for protocol calls. Preserve existing local/parameter static element paths.
- I22: implement specified Array/Dictionary construction and mutation via **new** `Kimi/Compiler/Emission/CollectionRuntime.ll.in` and/or generated Core bodies, using shared layout/effect/cleanup metadata. Private contiguous Array and reusable Dictionary slots/order links are a starting proposal under D7, not mandated ABI. Implement consuming and shared iterators using committed Core witnesses.
- Invariants: Slice methods O(1) without per-element allocation; empty Slice retains whole-storage Loan; dynamic Non-Copy index reads borrow rather than Move; lookup versus insertion argument order differs; reserve means additional; within-capacity operations/churn allocate no internal storage; no dependency subtraction after clear/None/Err; reverse current/insertion cleanup.
- Exit: T19–T21 pass V4–V8/V11, including growth/reindex operation counts. Split delivery into Index/Range, fixed-array Slice, iteration, Array mutation, Dictionary mutation; each uses prior closed capabilities and keeps unsupported later APIs rejected. Risks: quadratic churn/order maintenance, hidden equality/destructor effects, conservative dependency loss.

### M12 — Core comparison, Stringify and interpolation

- Requirements: R4/R6–R7/R15; depends on M5/M7–M8; collection equality dependencies can use completed Contract mappings before all M11 runtime work.
- I23: complete Core Stringify/Equatable/Comparable identities/mappings and operator selection using existing Contract machinery. Preserve IEEE float operators while intrinsic Equatable treats same-Type NaNs and signed zeros as specified.
- I24: extend interpolation Binding/ownership and `BodyLowering.Strings.cs`, `LlvmModuleWriter.Strings.cs`, runtime allocation with builder scratch reuse: each embedded value shared once, mapping invoked once, independent owned result appended before next value. Document implementation-defined locale-independent float spellings in usage/profile documentation during implementation.
- Invariants: no Non-Copy input Move merely for printing; shared operation Loans end correctly; actual Heap strings release exactly once; string concatenation remains deferred under §13.3.
- Exit: T2/T14/T21/T22 string cases pass V4–V8/V11, including heap failure injection and exact UTF-8/NUL output. Risks: locale dependence, double rounding/formatting, treating Static-handle audit as heap coverage.

### M13 — FFI, Library output and native connection

- Requirements: R16–R17/R21/R23; depends on M3–M5; instantiate generic/library bodies after M8.
- I25: implement safe versus unsafe raw-pointer operations and `#LibraryImport` direct ABI using shared declarations/calls, checked pointer instruction premises and C-layout facts. Extend existing Binding/physical ABI paths; add **new** `ForeignFunctionAbi.cs` only for the actual distinct C contract. Test valid pointers obtained from a controlled foreign fixture, not invented public raw-storage APIs.
- I26: extend `LlvmEmitter.CheckInputs`, `EmissionArtifacts.Publish`, `NativeToolchain`, native input configuration and manifest/build records for Library inspection, common source-dependency generation, owner-qualified requirements/supplies, COFF/archive member closure and directives. Preserve fail-before-publication and actual symbol inspection; reject unsupported Library native executable requests.
- Invariants: selected unused bodies still checked; Library no OS entry/export ABI; no C aggregate passing; scalar/opaque pointer C calls use §22.3.2; tools run as argument vectors; five-minute tool deadline/cancellation, no fixed product run timeout.
- Exit: T22–T25/T31 pass V4–V9, actual O0/O2 C signature checks and backend audits included when supply changes. G4 blocks native evidence. Risks: false archive allowlists, implicit CRT/TLS/default-library dependencies, mismatched immutable outputs.

### M14 — Source packages, stores and semantic reuse

- Requirements: R18–R19/R21/R24; depends initially on M3, then completed semantic records/effects/generation contracts from relevant milestones.
- I27: add **new** `SourcePackage.cs`, **new** `ContentStore.cs` in `Kimi/SolutionAndProject/`, and **new** `PackCommand.cs`, **new** `PublishCommand.cs`, **new** `StoreCommand.cs`. Implement §18.6 manifest/archive validation, canonical streaming hashes, closure packing, local destination-scoped release transactions, bounded decompression and store verification/pins. Pack validates the final Package graph and never reserves versions or silently rewrites them.
- I28: add **new** `Kimi/Compiler/Core/SemanticRecord.cs` and **new** `SemanticCache.cs`; version verified compiler-owned plans separately from input integrity/native summaries. Begin whole-module reuse; add reverse dependencies, structural correspondence, effect/absence invalidation and current-source remapping only when complete. Regenerate ABI/frame/budget/IR/object plans.
- Invariants: one fixed byte snapshot through check/pack, SourceId is exact manifest-based domain hash, compression doesn't define identity, module versions retain distinct Types; publisher verification claims not trusted; cached corruption never hides package corruption; no load-order-sensitive acceptance.
- Exit: T24–T25 pass V4/V8/V9/V11, with no-cache/cold/warm equality and interrupted/concurrent publication. Risks: incomplete pins/collection, ZIP path collisions, false hash trust, stale private effects or locations.

### M15 — Mod semantics and separately blocked host integration

- Requirements: R1/R2/R20/R23; depends on M3/M7 and source context/invalidation foundations.
- I29: add **new** `Kimi/Compiler/Core/ModPipeline.cs` as an internal scheduler; implement dependency ordering, per-invocation snapshot queries, append-only selected declarations, provisional Binding availability/invalidity and final pass. Use private test doubles and new SourceDocuments for generated fragments; don't mutate original syntax or rerun completed Mods.
- I30: external host/configuration/query adapter is `BLOCKED` by G2 and excluded API design. Concrete production symbol/path is intentionally undecided until the separate contract exists.
- Invariants: no per-instantiation execution, function-body injection or general Koto rewriting; generated source saves are not automatically new ordinary inputs; raw observed bytes invalidate Mod output; compilation is not finalized before generated dependencies stabilize.
- Exit: internal T27/V4/V8 can complete independently. M15 as a whole stays blocked at integration until G2 resolution and its adapter tests; pack continues rejecting Mod-dependent inputs under §18.6.1. Risk: stale semantic query handles and undocumented host observability.

### M16 — Test language and isolated execution

- Requirements: R2/R7/R18/R22/R24; depends on M3 membership foundation, M4/M6 checking, M8 budget isolation, M9 for nested Closure coverage, M13 runtime/artifacts. Independent membership checks can start after M3.
- I31: implement Test membership/Attribute eligibility, source/test partitions and verified discovery; add **new** `Kimi/Compiler/Binding/Binding.Tests.cs` and internal case catalog. Product binding never acquires test candidates; every test body is checked before list/filter, including test-only helpers.
- I32: add missing test statement representation only after inspecting current `$` syntax handling; proposed **new** `TestVerificationKoto` if existing nodes cannot retain the required standalone-item/control boundary. Extend parsing/writing/reload, ownership and lowering for once-only bool/snapshots, basic failure before cleanup/message, lazy messages, outward-message transfer rejection and expect/require continuations. Verify internally through a private failure-event adapter; never treat it as the public protocol.
- I33: **new** `TestCommand.cs` and production runner/reporting/OS process-management adapter are `BLOCKED` by G1. After separate profile specification, implement one immutable all-case artifact, stable IDs, one managed child per case, finite monotonic deadlines/recovery, bounded channels/storage and reliable require-Abort identification. Do not publish interim arbitrary schemas/defaults.
- Invariants: false require does not return or unwind; failed cleanup/message retains basic failure plus actual termination; zero exit without completion is failure; product/test generation budgets independent; no process reuse, source parallelism or implicit retry.
- Exit: T28–T30/V4/V8 semantic portion first; V10/V11 public end-to-end requires G1. No claim of complete test execution from parsing, private adapter events or `--list`. Risks: output saturation, descendant pipe retention, stale ArtifactId and lost failure before Abort.

### M17 — Conformance, performance and documentation closure

- Requirements: all R; depends on all applicable milestones, with G1–G2-dependent completion explicitly unresolved.
- I34: close G5, execute impact-based regression then final V3–V12. Tune measured bottlenecks without weakening normative acceptance or extending deferred syntax. Document mandatory collection bounds and actual compiler/runtime measurements independently.
- I35: update `STATUS.md` with product-wide support levels/input limits and concise evidence; update `SPEC.md` only for index/navigation changes and owning spec sections only for required implementation-defined choices or separately resolved decisions. Update `README.md`, executable examples and `examples/SpecTour/README.md` to distinguish supported versus illustrative APIs. Keep detailed execution history here; do not edit `draft/` automatically. Update `.github/workflows/test.yml` for explicit managed configuration, nonzero counts, retained reports and ordinary Windows native coverage; publishing is separate.
- Exit: §11 satisfied, every in-scope R linked to current passing evidence, and no hidden unsupported range. If interfaces remain unsettled, report completion of the defined subset with M15/M16 blocked; do not rename that result “complete compiler.”

## 7. Implementation Checklist

All unchecked items are `TODO` unless explicitly `BLOCKED`. Completion is recorded here and in §2 with evidence, not by deleting items.

| ID | Milestone / requirements | Implementation action / state |
| --- | --- | --- |
| I1 | M1 / all R | [ ] IN_PROGRESS: numeric, selected Layout/fragment, Type arity, Origin fragment and grouped/annotated Semantics-pair plus nested Type-argument and Property header/accessor receiver API-access clauses mapped in §8; remaining-family clause audit retained under G5 |
| I2 | M1 / R23–R24 | [x] DONE: explicit MTP configuration/nonzero counts, Debug/Release baselines, per-run input hash verification, deadlines, bounded drains and tested launch-time Windows job containment. CI uses the built Release assembly and a 15-minute job bound. |
| I3 | M2 / R1–R4/R9 | [ ] IN_PROGRESS: length constants, ordinary APIs, static defaults, local fixed-array inference, matching fragment headers/bases, fixed-parameter array arguments, selected Layout/fragment validation, distinct Type arity/explicit selection, current-grammar Origin fragment audit, grouped original Semantics-pair reconstruction and annotation representation/certificate safety and grouped nested-array Type-argument disambiguation plus Property header/storage API-access and accessor receiver formation/API-access subranges DONE with §8 evidence; named function parameter/result projection exposure and I3-P1/P2 explicit Property/accessor and I3-C1/C2 parsed declaration-constraint projection exposure DONE with §8 evidence. I3-R1/R2 Unit/Tuple requirements and constructed-qualifier projection parsing DONE with §8 evidence. I3-A1 conformance-specific projection exposure, I3-A2 inferred Property access audit, I3-N1 numeric metadata relocation, I3-E1 enum payload projection exposure, I3-B1 direct-base projection exposure, I3-A3 module access audit, I3-L2 Layout certificate invalidation and I3-F2 function API certificate ordering DONE with timed-execution evidence. Current I3-U1/U2/U3/U4/U5 cover Attribute targets/chains and declaration-context certificates/proofs; I3-G1/G2 cover nested generic signature/enum-plan constraints and malformed getter recovery, all DONE. I3-U6 non-function LibraryImport target validation is DONE. I3-G3 through I3-G23 and I3-A4/A5/A6/A7 cover call/Property/Pattern completion, retained projection certificate validity and module integration. I3-G24/G25/G26/G27 and I3-A8/A9/A10 cover normalized projected-call constraints, conditional premise validity/API domains, associated-projection subject clauses and conditional Property certificates. I3-V1 independently re-verified that scope and corrected Contract constraint subjects. Their unit states and evidence appear below. Remaining static/access/generic/Attribute audit, annotation proof support and general Semantics formation open; bound proofs remain M4/I7 |
| I4 | M2 / R3/R23 | [ ] IN_PROGRESS: numeric and owned primitive/Tuple/fixed-array Identity Acquisition subranges DONE with Debug/Release/native/reload/warm allocation evidence. Other representations and borrowed adaptations retain later milestone prerequisites |
| I5 | M3 / R2/R18 | [ ] IN_PROGRESS: Project-only exact DAG, fixed source/configuration bytes, conflict/cycle paths, direct module references, per-definition settings/default aliases and whole-graph Binding/flow/ownership implemented. Source graphs check within existing semantic support; generalized execution stays M13. Final identity/record and conformance audit remains |
| I6 | M3 / R18/R21 | [x] DONE for Project-only check/restore and lock lifecycle: Debug/Release and actual CLI positives/negatives verified; bare --locked corrected. Package consumers remain M14 and test command integration remains M16 |
| I7 | M4 / R4/R6/R12 | [ ] Origins, recursive effects and completed public guarantees |
| I8 | M4 / R6–R7/R9 | [ ] General Loans, reference ABI and ownership/result checking |
| I9 | M5 / R2/R8 | [ ] Property/constructor/deinit binding and struct/C layout |
| I10 | M5 / R7–R8/R16 | [ ] Field execution, partial construction, deinit and statics |
| I11 | M6 / R6–R7/R9–R10 | [ ] Complete enum/Pattern acquisition and guard semantics |
| I12 | M6 / R7/R9–R10 | [ ] Tagged layout, enum dispatch and cleanup generation |
| I13 | M7 / R3–R6/R10 | [ ] Universal bodies, constraint and associated-Type proofs |
| I14 | M7 / R4–R6 | [ ] Full specialization and closed-family selection |
| I15 | M8 / R5/R21 | [ ] Metadata, shared entry/context generation and recursion limits |
| I16 | M8 / R5/R7/R24 | [ ] Exact frames, bounded optional specialization and budget isolation |
| I17 | M9 / R4/R6/R11 | [ ] Function Items/capture/callable semantic checking |
| I18 | M9 / R7/R11 | [ ] Callable storage, indirect ABI and cleanup |
| I19 | M10 / R6/R12/R15 | [ ] Specified object/Weak catalog, operations and runtime tests |
| I20 | M10 / R7/R12 | [ ] Object/Weak states, counts, ordering and final release |
| I21 | M11 / R6/R9/R13 | [ ] Sequence metadata/views and non-lending iteration |
| I22 | M11 / R7/R10/R14/R24 | [ ] Array/Dictionary operations, dependency/effect/cost guarantees |
| I23 | M12 / R5/R15 | [ ] Core comparison and Stringify mappings |
| I24 | M12 / R4/R7/R15 | [ ] Heap-string interpolation and formatting |
| I25 | M13 / R17/R21 | [ ] Raw-pointer instructions and exact C imports |
| I26 | M13 / R16/R21 | [ ] Library/common output, native closure and publication |
| I27 | M14 / R18–R19 | [ ] Canonical packages, local stores and transactional commands |
| I28 | M14 / R19/R23–R24 | [ ] Bounded verified semantic records/reuse/invalidation |
| I29 | M15 / R1–R2/R20 | [ ] Internal Mod schedule, snapshots and provisional Binding |
| I30 | M15 / R20 | [ ] `BLOCKED`: public host adapter requires G2 |
| I31 | M16 / R2/R18/R22 | [ ] IN_PROGRESS: product TestSources and inline Test-function exclusion, syntax-level target/signature validation, reload/rebind and dependency membership verified; discovery/test generation remain TODO |
| I32 | M16 / R7/R22 | [ ] Standalone test verifications and cleanup-aware continuations |
| I33 | M16 / R21–R22 | [ ] `BLOCKED`: public runner requires G1 |
| I34 | M17 / all R | [ ] Current conformance/regression/performance evidence |
| I35 | M17 / R23–R24 | [ ] Product status, documentation/examples and verification CI |

### Previously completed Property projection subitems

These two items share retained projection records and must reject invalid signatures before Property/conformance verification. They refine I3 without replacing its stable ID or closing M2.

| ID | State | Scope and acceptance |
| --- | --- | --- |
| I3-P1 | DONE | Explicit stored/computed/requirement Property header projections retain qualifier and defining-requirement domains after normalization, independently of accessor restrictions. Correct Access diagnostic and IsVerified false; valid effective domains, reload/rebind and warm allocation controls pass. |
| I3-P2 | DONE | Explicit accessor receiver/input/result projections use the accessor domain (or requirement Contract domain), excluding implementation bodies. Invalid signatures prevent Property/conformance verification. Narrow accessor controls, reload/replacement and Debug/Release regression checks pass. |

### Selected constraint projection subitems

These two I3 subitems share retained projection identities and declaration-domain validation. They cover constraint projection Binding; the two parser limitations recorded at that checkpoint are now resolved by I3-R1/R2 below. They do not close M2/I3 or broaden conformance-specific access domains.

| ID | State | Scope and acceptance |
| --- | --- | --- |
| I3-C1 | DONE | Named member-function and Contract function-requirement constraints retain projection qualifier/defining-requirement access on both sides. Source-local functions and implementation-only uses remain separate. Rejection, effective-domain controls, reload/rebind and warm allocation checks pass. |
| I3-C2 | DONE | Struct/enum generic and Contract declaration constraints, including associated requirements, retain projection domains. Self conformances and implementation associated specifications keep their separate intersection domains. Required Debug/Release regression checks pass; no invalid declaration is certified. |

### Selected requirement parsing subitems

These two items share requirement-primary parsing and preserve §§8.3/8.7 Boolean precedence, Type identity and §9.3.2 recursive projection accessibility.

| ID | State | Scope and acceptance |
| --- | --- | --- |
| I3-R1 | DONE | Parse Unit/Tuple Type requirements, including nested Types, while retaining Boolean grouping/negation precedence. Verify syntax trees, writing/reload, Binding identity/access and malformed-source recovery. |
| I3-R2 | DONE | Parse associated projections following constructed qualifiers in requirement expressions using existing Type/projection representations. Verify qualified/nested arguments, access rejection, reload/rebind and full Debug/Release regression. |

### Timed execution units

| ID | State | Scope and next check |
| --- | --- | --- |
| I3-V1 | DONE | Independent verification pass over the I3-G24…G27 / I3-A8…A10 scope (§8 checkpoint below). Re-ran the recorded gates on the current tree, traced the changed Binding paths against SPEC 8.2/8.4.8, probed malformed and permuted sources, and corrected one Contract constraint-subject defect. Debug/Release builds PASS with zero warnings/errors; full suites 6,430 each PASS, zero failed/skipped; 12 cases added. |
| I3-G27 | DONE | Computed Property certificates inside invalid conditional blocks are revoked by the shared declaration-context guard. Focused 23 and full Debug/Release 6,418 each PASS; recovery, independent outer Properties and zero warm Property allocations. |
| I3-A10 | DONE | Projected calls, conditional premise validity/API domains, published member conditions and associated-subject Type arguments verified across source modules, both alias routes and reparsing. Focused module suite 153 and full Debug/Release 6,413 each PASS; no production change required. |
| I3-G26 | DONE | Constructed structs/enums enforce associated-projection subject clauses after substitution, such as `T.Origin.Item is i32`. Focused 26 and Debug/Release 6,393 each PASS; nested formation, dependent evidence, enum construction and zero warm allocations. Self-conformance responsibilities remain separate. |
| I3-A9 | DONE | Published preconditions of conditional functions/computed members cover each member's own effective API domain; a narrower conformance does not narrow a public member. Focused 22 and Debug/Release 6,367 each PASS; member-only failures stay independent. |
| I3-A8 | DONE | Conditional conformance premises and their retained projection domains cover each Type/Contract intersection, including ancestor paths. Focused 23 and Debug/Release 6,345 each PASS; unrelated paths remain independent. |
| I3-G25 | DONE | Retained projection inputs in conditional premises satisfy SPEC 8.4.8 before certification, and final call completion rechecks the committed candidate's substitutions and member conditions. Focused 18 and Debug/Release 6,322 each PASS. |
| I3-G24 | DONE | Candidate proof Types use existing associated-Type normalization even with no receiver Self, resolving the recorded projected-constraint call limitation. Focused 20 and Debug/Release 6,304 each PASS; generic native generation stays separately gated. |
| I3-G17 | DONE | Retained enum payload projection inputs validate before conformance/construction certification. Focused 10; Debug/Release 6,131 each PASS; nested/unused/dependent payloads, reload and zero warm allocations. |
| I3-G18 | DONE | Final expression certificate validation checks retained projection qualifiers erased by normalization. Six baseline stale certificates reproduced; focused 15 and full Debug/Release 6,146 each PASS. |
| I3-G19 | DONE | Expression projection inputs require final conformance validity as well as Type constraints. Baseline 2 stale certificates reproduced; focused 11 and full Debug/Release 6,157 each PASS. |
| I3-A6 | DONE | Added 16 imported expression projection cases; focused module 105 and full Debug/Release 6,173 each PASS. |
| I3-G20 | DONE | Final signature projection proof failures propagate through dependent Property/conformance certificates until declaration states stop changing. Focused 16 and Debug/Release 6,189 each PASS. |
| I3-G21 | DONE | Late projection proof failures invalidate enum payload owners and direct bases, propagating to descendants and construction plans. Focused 20 and Debug/Release 6,209 each PASS. |
| I3-G22 | DONE | Associated specifications retain final projection proof; ordinary conformance validation rejects invalid matching clauses. Focused 18 and Debug/Release 6,227 each PASS. |
| I3-A7 | DONE | Added 28 cross-module declaration projection cases; focused module 133 and full Debug/Release 6,255 each PASS. |
| I3-G23 | DONE | Ordinary declaration constraint projection validity, dependent certificates, reload/recovery and provisional/final lifecycle verified. Full Debug/Release 6,284 each PASS; separate projected-constraint calls remain unresolved and explicitly open. |
| I3-G16 | DONE | Function/Property/accessor retained projection inputs validate before final certificates. Focused 20; Debug/Release 6,121 each PASS; call/witness rejection, valid dependent inputs, reload/recovery and zero warm allocations. |
| I3-G15 | DONE | Retained base projection qualifiers validate input constraints before descendant certification. Focused 12; Debug/Release 6,101 each PASS; nested/dependent/source-order/reload/recovery and zero warm allocations. |
| I3-G14 | DONE | Conformance checks retained associated definition inputs before normalization. Focused 13; Debug/Release 6,089 each PASS; projected/nested/fixed/inherited/conditional/dependent inputs and zero warm allocations. |
| I3-A5 | DONE | Added 24 module integration cases for associated/base/runtime inputs and inherited access. Module suite 89 and Debug/Release 6,076 each PASS; explicit/default aliases and reparse. |
| I3-B2 | DONE | Reject accessible inherited Value-role Name collisions, independent of signatures, derived access and conditions. Focused 19; Debug/Release 6,052 each PASS; private names, fragments, ancestor order, certificates and zero warm allocations. |
| I3-G13 | DONE | Runtime-test operand/target Types validate before publication and after late declaration validation. Focused 15; Debug/Release 6,032 each PASS; nested inputs, Never, reload/recovery and zero warm allocations. |
| I3-G12 | DONE | Late base formation checks propagate invalid generic bases through descendants before Property/conformance certification. Focused 12; Debug/Release 6,017 each PASS; nested/dependent/order/reload/recovery and zero warm allocations. |
| I3-G11 | DONE | Conformance verification proves nested constraints in associated-Type definitions. Focused 15; Debug/Release 6,005 each PASS; fixed/inherited/conditional/dependent definitions, reload/recovery, sibling isolation and zero warm allocations. |
| I3-G10 | DONE | Nonexhaustiveness diagnostics wait for final Pattern Type checks; unresolved matches remain unavailable. Focused 4; Debug/Release 5,990 each PASS, including valid missing-coverage, reload and zero warm allocations. |
| I3-G9 | DONE | Unreachable warnings are published after Pattern completion checks. Focused 7; Debug/Release 5,986 each PASS; valid/nonexhaustive/body-error controls, reload and zero warm allocations. |
| I3-A4 | DONE | Added 12 cross-module generic-call, late API, Property and Pattern cases. Explicit/default aliases and module reparsing pass; module suite 65 and Debug/Release 5,979 each PASS. |
| I3-G8 | DONE | Property certification follows Origin/API validation and precedes final conformance verification. Focused 11; Debug/Release 5,967 each PASS; invalid Types/owners/static borrows, dependent evidence, reload/recovery and zero warm allocations. |
| I3-G7 | DONE | Completed calls revalidate substituted result/argument/receiver/declaring Types; candidate selection validates declaring-Type constraints. Focused 13; Debug/Release 5,956 each PASS, including dependent receivers, reload/recovery and warm zero allocations. |
| I3-G6 | DONE | Pattern Type constraints checked before coverage and after late validation, including discards. Focused 15; Debug/Release 5,943 each PASS; no misleading coverage warnings, valid dependent Types, reload/recovery and warm zero allocations. |
| I3-G5 | DONE | Final Binding completion invalidates calls whose targets failed late declaration validation. Focused 8; Debug/Release 5,928 each PASS; reload, sibling isolation, same-plan recovery and zero warm allocations verified. |
| I3-G4 | DONE | Call candidates require valid enclosing declarations and definition-side signature-Type constraints. Focused 13; Debug/Release 5,920 each PASS; sibling isolation, rebind/reload and warm zero allocations verified. |
| I3-G3 | DONE | Explicit/inferred generic call substitutions validate nested constraints before candidate success. Focused 16; Debug/Release 5,907 each PASS, including dependent evidence, reload/recovery and warm zero allocations. |
| I3-U6 | DONE | LibraryImport on non-function declarations cannot certify. Focused 8; Debug/Release 5,891 each PASS; enum/nested/chain/reload/recovery cases verified. Foreign-function support remains M13 work. |
| I3-U5 | DONE | Wrong-target Test Attributes invalidate declarations; indexing and Binding retain preceding-marker checks. Focused 10; Debug/Release 5,883 each PASS. |
| I3-G2 | DONE | Enum result/payload Types require proved nested generic input constraints. Focused 11; Debug/Release 5,873 each PASS; reload/recovery/dependent evidence and warm zero allocations verified. |
| I3-G1 | DONE | Property and function-witness certificates check recursively constructed signature-Type constraints. Malformed missing-getter recovery fixed. Focused 13; Debug/Release 5,862 each PASS. |
| I3-U4 | DONE | Constraint proofs check enclosing Type/Contract validity before identity, capability or exact-assumption success. Focused 11; Debug/Release 5,849 each PASS; reload/recovery/sibling isolation and warm zero allocations verified. |
| I3-U3 | DONE | Enum construction plans require valid owner declaration contexts. Focused 10; Debug/Release 5,838 each PASS; early headers, contextual/generic cases, plan reuse, reload and warm zero allocations verified. |
| I3-U2 | DONE | Conformance/Property certificates require valid enclosing declarations. Focused 9; Debug/Release 5,828 each PASS; independent siblings/bodies, removal recovery, reload and warm zero allocations verified. |
| I3-U1 | DONE | Unknown selected Attribute targets invalidate before certification. Focused 13; Debug/Release 5,819 each PASS, including parameter/chained markers, inherited Types, reload, provisional syntax, removal recovery and warm zero allocations. |
| I3-F2 | DONE | Named-function API validation precedes final conformance certification. Focused 6; full Debug/Release 5,806 each PASS, including reload/replacement recovery and zero measured warm allocations. |
| I3-L2 | DONE | Invalid Layout targets/owners invalidate before inheritance/conformance certification. Focused 10; Debug/Release 5,800 each PASS, including witness/derived rejection and rebind recovery. |
| I3-A3 | DONE | Added 14 cross-module projection access cases; focused module suite 53 and full Debug/Release 5,790 each PASS. Unused dependency signatures and explicit/default aliases retain defining domains. No production change required. |
| I3-B1 | DONE | Direct bases retain projection access against the derived domain before inheritance validation. Focused 17; full Debug/Release 5,776 each PASS, including descendant/conformance invalidation, reload/replacement and warm zero allocations. |
| I3-E1 | DONE | Enum payload projections retain qualifier/requirement access before certification. Focused 19 and Debug/Release 5,759 each PASS, including invalid certificates, reload/replacement and warm zero allocations. |
| I3-N1 | DONE | Numeric replacement retains exact spelling independently of diagnostic relocation. Focused 15; Debug/Release 5,740 each; five LLVM fixtures and ten O0/O2 native executions PASS. |
| I3-A2 | DONE | Inferred Property exposure audit: check the resulting complete Type, not initializer-only qualifier/requirement spellings. Added 13 regression cases; full Debug/Release 5,725 each PASS. No production change required. |
| I3-A1 | DONE | Ordinary/conditional associated specifications retain projection access under each Type/Contract intersection, including ancestor paths. Focused 22 and full Debug/Release 5,712 each PASS; invalid paths cannot certify. |

## 8. Test Plan and Verification Records

### Test conventions

Reuse `ParseTestHelper` for syntax-only cases; `MinimalEmissionTest.Analyze` for prepared target → Binding → startup → ownership; `ScalarEmissionTest.EmitFixture` / `WriteFixture` for IR plus expected output; `StringEmissionTest.WriteAuditedFixture` and `StringLifetimeAudit` for logical destruction counts/order. Use `AllocationMeasurement.Measure` for warmed isolated-thread allocation checks. Its 30-second thread join throws on timeout; it does not forcibly terminate a hung measuring thread, so the outer test process also needs a deadline.

This matrix records verification obligations; current results are in §8. “Extend” means cases in an existing class, “new” means a proposed class/file. All classes named here map to `xUnitTest/Tests/<Class>.cs`. Positive/Negative/Boundary/Interaction/Regression variants belong to the same stable T family; append suffixes when splitting, retaining the parent ID. No exhaustive irrelevant cross-product is required; Appendix A's specified boundaries and distinct semantic paths must be covered.

| ID / requirements | Input and setup | Expected result; verification stage / observable evidence | Existing or proposed location |
| --- | --- | --- | --- |
| T1 / R1/R23 | Valid UTF-8; malformed byte sequence; decomposed identifier; Unicode line endings, indentation/continuation and Body forms | Parse exact valid forms; reject encoding/NFC/layout error at original document/span; parse/write/parse preserves Body form | Extend `SourceEncodingTest`, `UnicodeIdentifierTest`, `SourceDocumentAndDiagnosticTest`, `ControlFlowConformanceTest`; V4/V8 |
| T2 / R1/R3/R15 | Direct literal `5000000000@f64`, `-0.0@f32`, `3.5e38@f32`, decoded NUL string and reload | Exact one-round fitting; direct overflow is Binding error at literal/adaptation; byte/bits preserved through reload and emission | Extend `NumberLiteralParseTest`, `FloatConversionEmissionTest`, `StringLiteralParseTest`; V4–V8 |
| T3 / R1–R2 | Valid/invalid reached Conditions, unknown name inside unselected switch arm, same name within false if target; merged source fragments with different aliases | Respect §19.5 validation boundary; no alias leakage/extra scope; current source locations retained | Extend `DirectiveConditionValidationTest`, `CompilationSpecificationTest`, `BindingTest`; V4/V8 |
| T4 / R2/R8 | Multi-file fragments; inherited same-name member; private Type in public/internal signature; Layout Attribute conflict | Correct declaration/Binding access/fragment failures even unused; failures identify offending header/use rather than later unsupported emission | Extend `InheritedReceiverBindingTest`, `TypeBindingTest`, `PropertyBindingTest`; new `LayoutEmissionTest`; V4/V8 |
| T5 / R2/R4 | Named/default arguments and receiver in non-first parameter position; incomparable candidates; selected candidate fails borrow/access | Receiver and arguments execute once in specified order; default uses definition environment; fail intended selected use, no fallback candidate | Extend `BindingTest`, `FunctionEmissionTest`, `ReceiverShorthandTest`; V4–V8 |
| T6 / R3 | `let x: f64 = 1.25` then `let y = x@f32`; float-to-integer fractions, adjacent representable boundary floats, NaN/infinities/±0; allowed integer widths | Native truncation/rounding/checks match §13.5.4; failures Abort at conversion, literal-fit failures static. Explicitly test prohibited typed 128-bit↔float and bool/char conversions | Extend `FloatConversionEmissionTest`, `ConversionEmissionTest`; V4–V7 |
| T7 / R3/R16 | Signed minimum / -1 including remainder; shift width -1/width; overflow in executed ordinary expression versus required length constant; i128 helper-requiring operations | Runtime Abort versus constant-evaluation error correctly separated; actual object dependencies validated; no overflow/poison suppressed at O2 | Extend `IntegerEmissionTest`, `WideIntegerEmissionTest`, `DivisionEmissionTest`, `BitwiseEmissionTest`; V4–V7 |
| T8 / R3/R13 | Fixed array lengths 0, dependent N, invalid negative intermediate, zero-stride elements; named length and literal-only index comparison | Correct formation proofs/deadlines; zero-size values retain semantics; arithmetic/name index cannot become Move Path through folding | Extend `TypeBindingTest`, `ElementPathEmissionTest`; new `GenericGenerationTest`; V4–V8 |
| T9 / R4/R7/R9/R16 | `var x = 2` then `let y = x + x++`; RHS-first self-replacement, target-first compound assignment, named result transfer with defer/Abort/divergence | Snapshots/order/counts; secure before cleanup, no result delivery after non-normal cleanup; invalid premature physical consumption refused | Extend `ScalarEmissionTest`, `DeferredEmissionTest`, `AggregateResultEmissionTest`, `EmissionPlanTest`; V4–V8 |
| T10 / R6/R9/R12 | require failure body that can continue, including `require true`; object struct tests, Move/replace refinement invalidation; dead checking continuations | Flow/Binding errors at intended require/use; truth doesn't hide invalid failure body; live paths never regain Moved values or ended Loans; runtime test evaluates once | Extend `SpecRevisionAnalysisTest`, `RuntimeTypeTest`, `UnreachableOwnershipTest`; V4–V8 |
| T11 / R6–R8/R13 | P2 below; returned local borrow; simultaneous shared arguments; nested ref/uniq reborrow; borrowed Non-Copy extraction; mutation while Slice remains live | Legal shared results stay valid; escape/conflict/missing-part rejected in ownership/use legality at use; parameter immutability remains Binding error | Extend `OwnershipAnalysisTest`, `ElementParameterMoveEmissionTest`, `ElementBorrowOwnerEmissionTest`; new `BorrowEmissionTest`; V4–V8 |
| T12 / R7–R8/R16 | Constructor/base fields with logged deinit; partial transfer; custom getter/setter self-assignment; static first-write/cycle and shutdown reentry | Per-layer initialization and reverse logical cleanup; no getter backing bypass; cyclic access Aborts at operation; no unused static eager init | Extend `PropertyBindingTest`, `StartupBindingTest`; new `PropertyEmissionTest`, `StaticInitializationEmissionTest`; V4–V8 |
| T13 / R7/R9–R10 | P3 below; nested Tuple/enum, guarded arm then catch-all, shared/non-Copy payload, duplicate fitted literal, `.Some`/`.None()` invalid | Complete coverage under specified structural rule; exact Case construction/acquisition/cleanup, warning at later contained Pattern; invalid shape fails Binding before emission | Extend `EnumBindingTest`, `EnumOwnershipTest`, `PatternBindingTest`, `MatchOwnershipTest`; new `EnumEmissionTest`; V4–V8 |
| T14 / R5/R10/R15 | Canonical and reordered Core Option/Result condition atoms; wrong same-spelled Copy identity; inherited associated Types and conditional witnesses; user Stringify/comparison | Correct nominal intrinsic identity and required mappings; no unresolved/cyclic proof published; selected map survives reload | Extend `CoreCatalogTest`, `CoreModelTest`, `ConditionalConformanceBindingTest`, `ContractBindingTest`; V4/V8 and V6 after generation |
| T15 / R3/R5/R24 | P4 below; length-generic arrays, mandatory specialization at optional budget zero; finite mutual recursion versus growing Type keys; shared body with same-size/different-cleanup Types | Generic invalidTwice rejected at definition even unused; valid transfer works for Copy and Non-Copy. Stable selection/entry ABI, exact frames, bounded required-generation diagnostic | Extend `ConstraintBindingTest`, `TypeBindingTest`; new `GenericGenerationTest`; V4–V8/V11 |
| T16 / R5–R6/R11 | Same generic/callable call with permuted argument discovery order, multiple input-Origin meets, indirect/recursive default or destructor static effect | Principal inference stable; no hidden lifetime/capability requirement, no unsound static access while Loan active; invalidate after effect/specialization change | Extend `ConstraintBindingTest`, `ContractBindingTest`; new `BorrowEmissionTest`, `CallableEmissionTest`; V4–V8 |
| T17 / R6–R7/R11 | Empty/inline/heap Closure captures, explicit unused capture, repeated Shared calls, Consuming partial Move, borrowed capture escape | Capture/call/cleanup order; reject escape/illegal receiver in semantic phase; native no duplicate acquisition/destruction and exact result lifetime | New `CallableEmissionTest`, extend `FrontEndSyntaxTest`; V4–V8/V11 |
| T18 / R6–R7/R12 | §13.5.9 `expired()` and cyclic Node examples; clone/drop in different views, count maximum and allocation-failure adapters; upgrade before/after publication/final release | Weak absence versus expiry distinct; one payload cleanup; no resurrection/freed-pointer reads; exact dynamic Supports and arc ordering. No undefined source concurrency tests | New `ObjectEmissionTest`, extend `RuntimeTypeTest`; native adapter additions under `backend/windows-x64/tests/` (new cases); V4–V8/V11 |
| T19 / R6/R13 | P5 below; ^0/^1, negative/saved Index, inclusive/reversed ranges, empty split, shorter target reuse, nested reference Slice and tryGet | Correct None versus Abort, once-only bounds location, Copy read versus explicit slot borrow, persistent source Loan for empty view; O(1) view operations | Extend `RangeIndexParseTest`, `ElementEmissionTest`; new `SequenceEmissionTest`; V4–V8/V11 |
| T20 / R6–R7/R9–R10/R13–R14 | for over owned arrays/dictionary and shared Slice, immediate exit/continue/outer return, Tuple binding arity, iterator stays exhausted | Single iterable acquisition; short next Loan ends before body; binding then remaining iterator cleanup correct; Range (unresolved) iteration rejected at conformance check | Extend `ForParseTest`; new `SequenceEmissionTest`, `CollectionEmissionTest`; V4–V8 |
| T21 / R7/R14–R15/R24 | P6 below; §4.7 mutation matrix, duplicate literal separated by nonliteral; runtime duplicate with effectful value; NaN/±0 keys; reserve/shrink, full-capacity churn | Static duplicates at later key when eligible; runtime duplicate before later value; insertion identity/order; Result owns rejected inputs; zero internal allocations within capacity; operation-count growth bounds | Extend `CollectionLiteralParseTest`; new `CollectionEmissionTest`; V4–V8/V11 |
| T22 / R8/R15–R17/R21 | P1/ P7; interpolation of scalar and user Stringify, C NativeRecord (§21.1.3), small signed/unsigned/mixed FP/5+ C arguments, valid opaque-pointer round trip, runtime fault adapters | Exact bytes/format rules; C size/offset/ABI matches; forbidden bool/aggregate import fails source ABI check; runtime allocation/write/free failures retain provenance; no promised unsafe-violation output | Extend `MinimalEmissionTest`, `StringEmissionTest`, `CharEmissionTest`; new `InterpolationEmissionTest`, `ForeignFunctionEmissionTest`, `LayoutEmissionTest`; V4–V9 |
| T23 / R2/R18/R21 | P8 two-Project fixture; diamond aliases, two versions, cycle, mismatched content and inaccessible transitive name; nonempty old KotonohaArray | Binding uses direct reference and definition contexts; conflict reports both graph paths; old setting receives migration reason; check does not run native tools | New `DependencyResolutionTest`; extend `SolutionInputTest`; V4/V8/V9 |
| T24 / R18–R19/R21 | restore product success/test failure; source edit/relocation without edge change; corrupt stale empty lock; package trials then same-release conflicting publish; races/interruption | Restore-only lock updates with both partitions; no unnoticed stale lock; full closure integrity/semantic validation; publication all-or-nothing mappings with exact SourceIds | New `DependencyLockTest`, `SourcePackageTest`; V4/V8/V9 |
| T25 / R5/R19/R21/R24 | Same graph no-cache/cold/warm; private effect/absence/specialization edits, comment/location change, corrupt semantic/package bytes; reused product generic with added tests | Equal semantic decisions, current diagnostic locations and changed relevant artifact IDs; recompute dependent proofs, no Type merging across versions; measure lazy body loads/hash/read/retained memory | New `SemanticReuseTest`, extend `KotonohaSerializationTest`, `EmissionArtifactsTest`; V4/V8/V9/V11 |
| T26 / R9/R16/R23 | Inferred Unit through discarded selection/do, discarded Result versus effect-free value, final defer, covered/unreachable errors | Exact warning priority/count at source discard; no call-purity assumption; mandatory unused-body diagnostics before optimization | Extend `SpecRevisionAnalysisTest`, `ControlFlowConformanceTest`, `SpecReviewTest`; V4/V8 |
| T27 / R1/R2/R20 | §20.7.7 two-step model through private test doubles; cycle/missing prerequisite, semantic query then append, independent generated source context, changed observed comment | Once/order, query invalidation, final binding and original diagnostic provenance; no original source rewrite; external adapter tests withheld until G2 | New `ModPipelineTest`; V4/V8 |
| T28 / R2/R18/R22 | P9 below; invalid Test signature/Attribute, direct call/value acquisition, helper in TestSources versus product file, erroneous filtered-out body | Product membership unchanged; test bodies checked before list/filter; prohibited context/target fails correct parse/Binding stage; list has no generation/native side effect | New `TestMembershipTest`, `TestDiscoveryTest`; V4/V8/V10 |
| T29 / R5/R22/R24 | Add unrelated inline tests and test-only generic substitutions; change filter/case; budget zero/nonzero | Product lookup/layout/sharing/frames/budgets unchanged; current locations may change; one all-case artifact across filters, semantic-only list no generation | New `TestGenerationTest`; V4/V8/V10/V11 |
| T30 / R7/R22/R23 | P9; side-effecting condition, short circuit, lazy message with Move, nested failure, cleanup/message Abort before require reaches Abort; output/ID saturation, corrupt completion, descendant retains pipes | Basic failure before cleanup/message; once evaluation; actual termination retained, no enclosing defer after Abort; bounds omit storage only; recovery finite and failure never misclassified success | New `TestVerificationTest`, `TestRunnerTest`; V4/V8 internal subset, V10/V11 public subset blocked by G1 |
| T31 / R21/R23 | Fresh emit/build/run; failed build after previous success; corrupt executable/hash, changed output settings, mismatched backend/library directive; paths with spaces | No mixed publication; emit no tools, build no execution, run no rebuild; exact exit/cancellation/output contract and actual native member closure | Extend `EmissionArtifactsTest`, `NativeToolchainTest`, `ToolchainResolverTest`; backend CLI/artifact scripts; V4–V9 |
| T32 / R24 | Representative small/large sources, sparse large arrays, many-path DAG, generic recursion, cold and warm reruns, failed then successful analysis, reload dropping old tree | Measure time/allocations/peak and retained memory, no growth proportional to repeated warm attempts or unreferenced syntax; preserve relevant existing zero-allocation tests | Existing `AllocationMeasurementTest`, allocation cases and `Benchmark/Benchmarks/`; new benchmark cases only for uncovered workloads; V11 |

### Representative complete inputs

These are proposed test inputs, not claims they compile today. Comments state the intended check, and native assertions must use exact expected bytes/events. Additional family variants derive from their cited owning clauses.

**P1 — existing minimal native regression (T22).**

```kimi
::Core.writeLine("Hello, world!")
```

Expected program stdout is exactly `Hello, world!\n` (14 UTF-8 bytes), stderr empty, exit 0. Exclude CLI settings-summary output from the program-byte assertion.

**P2 — initialization history and static partial Move (T11).**

```kimi
var pair: (string, i32) = ("Alice", 30)
let name = pair.0
let age = pair.1
pair.0 = "Bob"
let all = pair
::Core.writeLine(name)
::Core.writeLine(all.0)
```

Expected stdout `Alice\nBob\n`, all remaining owned parts destroyed once. Negative variant inserts `let invalid = pair` before repair; it must reach ownership and fail for incomplete/Moved use at that identifier, not parsing or startup.

**P3 — executable Option payload (T13).**

```kimi
let value: Option<string> = .Some("payload")
match value
    .Some(let text) => ::Core.writeLine(text)
    .None => ()
```

Expected stdout `payload\n`, one string responsibility. Negative variant removes `.None`; require non-exhaustive Binding diagnostic at match. Do not add a fallback because only Some is constructed here.

**P4 — universal body checking (T15).**

```kimi
func transfer<T>(value: T) -> T => value
func twice<T>(value: T) -> (T, T)
    T is Copy
    return (value, value)
func invalidTwice<T>(value: T) -> (T, T)
    return (value, value)
()
```

The last function must fail definition checking at reuse even without calls; `()` supplies valid startup. Remove it for positive tests invoking transfer with i32 and string and twice with i32. Existing successful instantiations cannot certify the negative generic body.

**P5 — Slice Loan conflict (T19).**

```kimi
var values: [3 of i32] = [1, 2, 3]
let shared = values[..]
values[0] = 10
let first = shared[0]
```

Require the mutation's Loan conflict after sequence Binding succeeds. Positive counterpart omits the mutation and checks shared[0] = 1 by an ordinary conditional output. Another negative writes `shared[0] = 20` and must fail read-only element permission, not the prior whole-array conflict.

**P6 — collection receiver activation (T21).**

```kimi
var values: Array<i32> = []
values.reserve(additional: 3)
values.append(10)
values.insert(^0, 30)
values.insert(^1, 20)
let last = values.remove(^1)
let first = values[0]
values.append(first)
```

Expect final order [10, 20, 10], last = 30, unchanged capacity for remove and in-capacity addition. Negative counterpart `values.append(values[0])` must reach Loan checking and fail because the receiver is already exclusively borrowed. Instrument internal allocation separately from argument/destructor costs.

**P7 — numeric conversion (T6/T22).**

```kimi
let value: f64 = 3.9
let result = value@i32
if result == 3 => ::Core.writeLine("ok")
```

Expected `ok\n`, empty stderr, exit 0. Replace value with a runtime expression yielding infinity to require conversion Abort at `value@i32`; direct literal overflow is a different Binding case.

**P8 — source dependency (T23).**

Use §18.4.2's `example.math` 1.2.0 Library with `public group Arithmetic` and `public func twice(value: i32) -> i32 => value + value`. The consuming Project declares `Dependencies.Math={PackageId="example.math" PackageVersion="1.2.0" Project="../Math/Math.kimiproj"}` and target `x86_64-pc-windows-msvc`; source:

```kimi
alias Math.Arithmetic
if twice(3) == 6 => ::Core.writeLine("ok")
```

Restore fixes the graph; check succeeds without native tools; build/run later print `ok\n`. All fixture projects are created in a unique test workspace, not by altering repository examples.

**P9 — require Abort (T28/T30), in a test build with no top-level runtime body.**

```kimi
group Verification
    #Test
    func stops()
        defer => ::Core.writeLine("cleanup")
        $expect(false, message: "first")
        $require(false, message: "second")
        ::Core.writeLine("after")
```

Two failures are retained in order. Neither `cleanup\n` nor `after\n` is printed. Require's termination is reported separately from failure records; no normal completion. Public wire bytes/exit allocation await G1; internal semantic assertions do not invent them.

### Verification procedures

All commands use the repository working directory from §2. `Debug`/`Release` below are compiler configurations; language `--Debug true`/default Release and native `O0`/`O2` are separate axes. Existing command signatures were inspected; only V1–V2 probes were attempted. Commands for **new** targets are explicitly proposed. Stop on failures; do not allow a downstream step to consume older successful outputs.

| ID / kind | Commands, prerequisites and exact target | Success criteria / ordering and output constraints |
| --- | --- | --- |
| V1 / plan and source audit | `git rev-parse HEAD`; `git status --short`; `rg --files`; targeted file reads; final `git diff --check` and plan-reference audit | Current commit and changes recorded; all existing references resolve; new items labeled; requirement/test coverage complete; only PLAN.md changed in planning |
| V2 / environment | `dotnet --info`; `dotnet test --help`; `./toolchain/opt.exe --version`; `./toolchain/llc.exe --version`; `./toolchain/clang.exe --version`; `./toolchain/lld-link.exe --version`; `Get-FileHash -Algorithm SHA256 -LiteralPath toolchain/windows_x64/kimi_backend_windows_x64_v1.lib,toolchain/llvm-dlltool.exe` | SDK/runner/profile/hashes verified separately. Help may evaluate projects: after G3 use explicit project, `--no-build --no-restore --help` to obtain extension/filter syntax; no tests or zero-count inference from help. Recheck every actual required tool before native acceptance |
| V3 / compiler and test build | If needed in implementation environment: `dotnet restore Kimigayo.slnx`. Then `dotnet build Kimigayo.slnx -c Debug --no-restore -v:minimal`; later same with `-c Release` | .NET 10, package access/restore assets required. Build exit 0; record warnings/errors and compiler DLL hash/MVID. Do not use IsAotCompatible as a request to publish/test NativeAOT. Serialize configurations sharing outputs |
| V4 / managed checks and fixture generation | `dotnet test --project xUnitTest/xUnitTest.csproj -c Debug --no-build --no-restore --minimum-expected-tests 1`; repeat for Release only after that V3 build. Focused **proposed** suffix `--filter-class 'XunitTest.FloatConversionEmissionTest'` (or exact class in the T row) must be confirmed by V2 extension help before use | Correct configuration/assembly, nonzero selected count, no failures/unexpected skips; retain counts and targeted class list. First focused checks per I item, then impacted families, full suite at integration. Base MTP options verified by help; class-filter spelling still unverified. No VSTest filter assumption |
| V5 / LLVM verification | Example after current Float conversion fixtures: `./toolchain/opt.exe -passes=verify -disable-output bin/scalar-fixtures/FloatConvertRoundTrip.ll`. Repeat for each current fixture and optimized IR. Prerequisites V3/V4, G4 resolved | Pinned LLVM 22.1.8 accepts exact hash-matched IR; does not prove execution, layout or C ABI. Record fixture hashes and source/configuration. No fixture generator running concurrently |
| V6 / ordinary native fixture execution | `pwsh -NoProfile -File backend/windows-x64/test-scalars.ps1 -ToolchainRoot ./toolchain -FixturePattern 'FloatConvert*.ll'`; use exact family patterns after verifying their generated membership; final `-FixturePattern '*.ll'` only against audited current inventory | Script verifies, optimizes, emits objects, inspects dependencies, links /NODEFAULTLIB and runs O0/O2. Compare stdout/stderr/exit/destruction order; 30-second normal fixture deadline, per-fixture `.timeout` for divergence and process-tree kill. Count expected fixtures × 2; zero runs fail. Output `bin/scalar-native` shared across configurations |
| V7 / runtime/backend/layout/ABI | `pwsh -NoProfile -File backend/windows-x64/build.ps1 -ToolchainRoot ./toolchain`; then `pwsh -NoProfile -File backend/windows-x64/test-emission.ps1 -ToolchainRoot ./toolchain -Configuration Release` (or Debug following its fixture generation) | Inspected scripts require tested pinned backend candidate under `backend/windows-x64/bin/verification.json`, matching archive and current configuration emission fixtures. Capture C ABI/layout comparisons, unwind/undefined-symbol checks and fault adapters. Rebuild script can install a matching verified backend reproduction; inspect these writes in implementation phase. G4 currently blocks |
| V8 / serialization/rebinding/reuse | V4 focused on `KotonohaSerializationTest`, `ReceiverShorthandTest`, `FloatConversionEmissionTest`, `EmissionPlanTest`, and **new** SemanticReuse/GenericGeneration/Mod/TestGeneration cases once added | Save/load creates fresh contexts; rebind invalidates ownership/emission until rerun; corrupt plans reject before text publication; no-cache/cold/warm and load-order acceptance equal. Record exact test list/filter after V2; no separate native claim |
| V9 / CLI and artifacts | `pwsh -NoProfile -File backend/windows-x64/test-cli.ps1 -ToolchainRoot ./toolchain -Configuration Release`; `pwsh -NoProfile -File backend/windows-x64/test-artifact-paths.ps1`; after V3: `dotnet Kimi/bin/Release/net10.0/Kimi.dll build examples/Hello/Hello.kimiproj --ToolchainRoot ./toolchain`, then `dotnet Kimi/bin/Release/net10.0/Kimi.dll run examples/Hello/Hello.kimiproj` | CLI script creates unique workspaces, compiler-call deadline 60 seconds; capture JSON and hashes, expected summary separate from child output. New restore/check/pack/publish/store and Library/FFI cases extend harness after implementation. Run only local fixture stores; public publication unnecessary. Existing .exe alone is not a fresh build certificate |
| V10 / language tests | **Proposed after I31/I33:** `dotnet Kimi/bin/Release/net10.0/Kimi.dll test <fixture-project> --list`; same with `--filter Arithmetic`; `--case <CaseId>` obtained from verified list; `--no-parallel`; `--jobs 2`. Concrete fixture path/IDs recorded at execution, not invented now | G1-resolved options/protocol and fresh compiler/artifact required. Verify all bodies before selection, bounded logs/descendants/recovery and identity, once-per-case isolation, failure continues other cases, unchanged all-case artifact across filters. Additional deadline/budget option commands remain blocked pending contract |
| V11 / performance | Existing command pattern: `dotnet run --project Benchmark/Benchmark.csproj -c Release -- --filter '*BindingBenchmark*'`; analogous exact registered `*OwnershipAnalysisBenchmark*` / `*FrontEndBenchmark*`. Allocation assertions via V4; **new** collection/generic/cache/runner/native performance harnesses require I items and baseline workloads first | Record environment, cold/warm setup, source size/hash, warmup, repeats, time dispersion, allocated bytes, retained/peak memory, code size and program time. M3/M14 also bytes read/hash passes/judgments/bodies; M11 operation/allocation counters; M16 startup/drain/recovery cost. Run benchmarks alone, same inputs/config/toolchain, preserve required checks. No speedup assertion from one noisy run |
| V12 / final integration and documentation | V1 coverage audit, one justified full V3/V4 per configuration, fresh fixture inventory then V5–V10, V11 representative baselines; review STATUS/SPEC index/README/examples | No unresolved in-scope clause hidden by a subset filter; all outputs traceable; expected rejection cases distinguish deferred rules from temporary implementation gates; documentation reports actual support. NativeAOT is NOT_APPLICABLE, not a missing mandatory gate |

**Artifact integrity protocol (applies V3–V12).** Record commit plus relevant uncommitted diff, SDK, compiler configuration/DLL identity, semantic target/mode/settings, fixture source, expected files and SHA-256, tool/runtime/backend identities. `bin/scalar-fixtures` is shared by Debug/Release and tests; `bin/emission-fixtures/<Configuration>` is configuration-specific. Before a focused generation, identify exactly which filenames the tests write and isolate only the prior family outputs or use a scoped inventory; do not include stale matching fixtures. Any move/delete needs resolved paths within that owned output directory. After generation, inventory `.ll`, `.stdout`, `.stderr`, `.exit`, optional `.timeout` and audit variants; verify hashes immediately before downstream use. Serialize generation/native execution/rebuild. Retain immutable per-run summaries under a new implementation-time subdirectory of ignored `TestResults/` or `bin/`; never treat directory existence as success.

**Process discipline.** NativeToolchain has five-minute native-tool bounds; script compiler calls and runtime fixtures have their own bounds above. Several direct script tool invocations and stream EOF reads are not independently bounded; I2 must add an outer process-tree deadline/drain bound before testing hostile/hanging inputs. Managed full runs and benchmarks need an explicit recorded harness deadline sized from their baseline (proposed initial full-run ceiling: 15 minutes; benchmark ceiling set per selected job), async output draining and tree termination on timeout/cancellation. Such harness limits are not product execution semantics. Never run unbounded `ReadToEnd` on descendants' retained pipes during recovery tests.

### Current verification records

This execution uses `TestResults/requirement-type-parsing/<run>/`; the preceding constraint group uses `TestResults/projection-constraint-access/<run>/`; the preceding Property group uses `TestResults/projection-property-access/<run>/`; the preceding function group uses `TestResults/projection-function-access/<run>/`; older execution evidence uses `TestResults/verification/<run>/` with exact arguments, deadline, stdout/stderr, and exit status. Historical planning rows below remain historical.

| Run / IDs | Target and state | Result / evidence |
| --- | --- | --- |
| `20260916T0010054833843Z`, `20260916T0011399640536Z` / I3-R1/R2 / V3 | `dotnet build Kimigayo.slnx -c Debug/Release --no-restore -v:minimal`, serial bounded runs | PASS; zero warnings/errors; HEAD b0096b5 plus preserved corrections and current parser/tests |
| `20260916T0010497415743Z` / T1/T3/T4/V4/V8/V11 | `dotnet test --project xUnitTest/xUnitTest.csproj -c Debug --no-build --no-restore --minimum-expected-tests 39 --filter-class XunitTest.RequirementTypeParsingTest` | PASS 39; syntax round-trip/reload, arity/spans/parent links, precedence, identity proofs, API access, malformed-source errors and zero measured warm Binding allocations |
| `20260916T0011215669206Z`, `20260916T0012221392180Z` / V4/V8 | `dotnet test --project xUnitTest/xUnitTest.csproj -c Debug/Release --no-build --no-restore --minimum-expected-tests 5690`, serial bounded runs | PASS 5,690 each; zero failed/skipped; exact compiler/test DLL hashes recorded and checked |
| Current I3-R1/R2 / V5–V7 | Parser forms reuse existing Type/projection/constraint representations; constrained-function generation remains gated in LlvmEmitter.CheckInputs | NOT_RUN LLVM/native execution and NativeAOT; no native support claim |
| `20260916T0000302057101Z`, `20260916T0001338310362Z` / I3-C1/C2 / V3 | `dotnet build Kimigayo.slnx -c Debug/Release --no-restore -v:minimal`, serial bounded runs | PASS; zero warnings/errors; current HEAD b0096b5 plus final test corrections |
| `20260916T0001010176762Z` / T3/T4/V4/V8/V11 | `dotnet test --project xUnitTest/xUnitTest.csproj -c Debug --no-build --no-restore --minimum-expected-tests 41 --filter-class XunitTest.ConstraintProjectionApiAccessBindingTest` | PASS 41; 39 Binding cases and two parser/emission rejection boundaries; reload/replacement and zero measured warm allocations |
| `20260916T0001164944818Z`, `20260916T0002129715655Z` / V4/V8 | `dotnet test --project xUnitTest/xUnitTest.csproj -c Debug/Release --no-build --no-restore --minimum-expected-tests 5651`, serial bounded runs | PASS 5,651 each; zero failed/skipped; exact DLL hashes recorded and checked; prior function/Property regressions included |
| Historical I3-C1/C2 / V5–V7 | Declaration rejection only; no runtime representation or execution path enabled | NOT_RUN LLVM/native execution and NativeAOT |
| `20260915T2340458071365Z`, `20260915T2341552324382Z` / V3 | I3-P1/P2 at `f762c3c` plus preserved prior function changes/current diff; `dotnet build Kimigayo.slnx -c Debug/Release --no-restore -v:minimal`, serial bounded runs | PASS; zero warnings/errors |
| `20260915T2341255975549Z` / T3/T4/V4/V8/V11 | `dotnet test --project xUnitTest/xUnitTest.csproj -c Debug --no-build --no-restore --minimum-expected-tests 37 --filter-class XunitTest.PropertyProjectionApiAccessBindingTest` | PASS 37; explicit header/accessor domains, nested/constructed projections, receiver normalization, private/body controls, witness rejection, reload/replacement and zero measured warm allocations |
| `20260915T2341388819076Z`, `20260915T2342547212319Z` / V4/V8 | `dotnet test --project xUnitTest/xUnitTest.csproj -c Debug/Release --no-build --no-restore --minimum-expected-tests 5610`, serial bounded runs | PASS 5,610 each; zero failed/skipped. Exact DLL hashes recorded and rechecked; prior function projection regressions included |
| Historical I3-P1/P2 / V5–V7 | Declaration rejection only; existing runtime/representation boundaries retained | NOT_RUN LLVM/native execution; no NativeAOT. Declaration-constraint and inferred projection provenance remain open |
| `20260915T2332112352107Z`, `20260915T2333221184218Z` / V3 | Function projection API exposure at `f762c3c` plus current diff; `dotnet build Kimigayo.slnx -c Debug/Release --no-restore -v:minimal`, serial bounded runs | PASS; zero warnings/errors. Refreshed restore assets select the committed dependencies |
| `20260915T2332526722452Z` / T3/T4/V4/V8/V11 | `dotnet test --project xUnitTest/xUnitTest.csproj -c Debug --no-build --no-restore --minimum-expected-tests 29 --filter-class XunitTest.ProjectionApiAccessBindingTest` | PASS 29; qualifier/requirement/concrete binding controls, nested syntax, effective domains, dependent/source-local/body controls, reload/replacement and zero measured warm allocations |
| `20260915T2333059353467Z`, `20260915T2334229656971Z` / V4/V8 | `dotnet test --project xUnitTest/xUnitTest.csproj -c Debug/Release --no-build --no-restore --minimum-expected-tests 5573`, serial bounded runs | PASS 5,573 each; zero failed/skipped. Compiler/test DLL hashes retained and checked before/after each run |
| Current function projection group / V5–V7 | Declaration rejection only; existing runtime and representation boundaries retained | NOT_RUN LLVM/native execution; no NativeAOT. Property/accessor and declaration-constraint projection audits remain open |
| `20260915T2221442727545Z`, `20260915T2222286926272Z` / V3 | Accessor receiver formation/API access at `889d7c9` plus preserved earlier edits; `dotnet build Kimigayo.slnx -c Debug/Release --no-restore -v:minimal`, serial bounded runs | PASS; both configurations built with zero warnings/errors |
| `20260915T2222104993739Z` / T3/T4/V4/V8/V11 | `dotnet test --project xUnitTest/xUnitTest.csproj -c Debug --no-build --no-restore --minimum-expected-tests 30 --filter-class XunitTest.AccessorReceiverBindingTest` | PASS; 30 tests, zero failed/skipped. Receiver formation/domain, reload/replacement and zero measured warm Binding allocations |
| `20260915T2222136123741Z`, `20260915T2222566679925Z` / V4/V8 | `dotnet test --project xUnitTest/xUnitTest.csproj -c Debug/Release --no-build --no-restore --minimum-expected-tests 5544`, serial bounded runs | PASS; 5,544 tests each, zero failed/skipped. Compiler/test DLL hashes retained and rechecked; conditional-member regressions resolved |
| Current accessor receiver group / V5–V7 | Declaration rejection only, no new runtime path or representation | NOT_RUN for LLVM/native execution. Broader access audits remain open. No NativeAOT |
| `20260915T2158583783905Z`, `20260915T2159462589180Z` / V3 | Property header/storage API access at `889d7c9` plus preserved earlier edits; `dotnet build Kimigayo.slnx -c Debug/Release --no-restore -v:minimal`, serial bounded runs | PASS; both configurations built with zero warnings/errors |
| `20260915T2159204223146Z` / T3/T4/V4/V8/V11 | `dotnet test --project xUnitTest/xUnitTest.csproj -c Debug --no-build --no-restore --minimum-expected-tests 28 --filter-class XunitTest.PropertyApiAccessBindingTest` | PASS; 28 tests, zero failed/skipped, recursive header/payload and domain controls, reload/replacement and zero measured warm Binding allocations |
| `20260915T2159336595257Z`, `20260915T2200140986792Z` / V4/V8 | `dotnet test --project xUnitTest/xUnitTest.csproj -c Debug/Release --no-build --no-restore --minimum-expected-tests 5514`, serial bounded runs | PASS; 5,514 tests each, zero failed/skipped. Compiler/test DLL hashes retained and rechecked |
| Current Property header API-access group / V5–V7 | Declaration rejection only, no new runtime path or representation | NOT_RUN for LLVM/native execution. Receiver-signature audit remains open. No NativeAOT |
| `20260915T2149125904046Z`, `20260915T2149583261205Z` / V3 | Grouped nested-array Type arguments at `889d7c9` plus preserved earlier edits; `dotnet build Kimigayo.slnx -c Debug/Release --no-restore -v:minimal`, serial bounded runs | PASS; both solution configurations built with zero warnings/errors |
| `20260915T2149377427030Z` / T3/T4/V4/V8/V11 | `dotnet test --project xUnitTest/xUnitTest.csproj -c Debug --no-build --no-restore --minimum-expected-tests 25 --filter-class XunitTest.GenericTypeArgumentBindingTest` | PASS; 25 tests, zero failed/skipped. Includes constructed/explicit function arguments, nested Types/Origins, invalid-role controls, reload/writing, numeric length syntax and zero measured warm Binding allocations |
| `20260915T2149469509462Z`, `20260915T2150317208255Z` / V4/V8 | `dotnet test --project xUnitTest/xUnitTest.csproj -c Debug/Release --no-build --no-restore --minimum-expected-tests 5486`, serial bounded runs | PASS; 5,486 tests each, zero failed/skipped. Current compiler/test DLL hashes retained and rechecked |
| Current grouped Type-argument group / V5–V7 | Parser disambiguation only; grouped arguments resolve to the existing ungrouped Type identities | NOT_RUN for LLVM/native execution; no new representation or runtime operation. Explicit length-call support remains outside the group. No NativeAOT |
| `20260915T2140026185388Z`, `20260915T2140511402807Z` / V3 | Pair annotation representation/certificate safety at `889d7c9` plus preserved earlier edits; `dotnet build Kimigayo.slnx -c Debug/Release --no-restore -v:minimal`, serial bounded runs | PASS; both solution configurations built with zero warnings/errors |
| `20260915T2140269765261Z` / T3/T4/V4/V8 | `dotnet test --project xUnitTest/xUnitTest.csproj -c Debug --no-build --no-restore --minimum-expected-tests 24 --filter-class XunitTest.PairAnnotationBindingTest` | PASS; 24 tests, zero failed/skipped. Covers normalized annotated WholeType identity, pending proofs, unsafe/owner certificate rejection, valid unannotated controls, nested syntax, independent Origins and reload/rebind/writing |
| `20260915T2140376504451Z`, `20260915T2141229963219Z` / V4/V8 | `dotnet test --project xUnitTest/xUnitTest.csproj -c Debug/Release --no-build --no-restore --minimum-expected-tests 5461`, serial bounded runs | PASS; 5,461 tests each, zero failed/skipped. Current compiler/test DLL hashes retained and rechecked; existing warm allocation tests pass |
| Current pair annotation group / V5–V7 | Corrected semantic representation and rejected invalid proof certificates; valid annotation support remains incomplete | NOT_RUN for LLVM/native execution; no runtime path enabled. No NativeAOT or new annotation-path performance measurement |
| `20260915T1702209854921Z`, `20260915T1703199528465Z` / V3 | Grouped Semantics-pair reconstruction at `889d7c9` plus preserved earlier edits; `dotnet build Kimigayo.slnx -c Debug/Release --no-restore -v:minimal`, serial bounded runs | PASS; both solution configurations built with zero warnings/errors |
| `20260915T1702431694822Z` / T3/T4/V4/V8/V11 | `dotnet test --project xUnitTest/xUnitTest.csproj -c Debug --no-build --no-restore --minimum-expected-tests 23 --filter-class XunitTest.PairGroupingBindingTest` | PASS; 23 tests, zero failed/skipped, including exact WholeType identity, nested Types, call Origins, rejection boundaries, source replacement, reload/writing and zero measured warm Binding allocations |
| `20260915T1703029481531Z`, `20260915T1703481189435Z` / V4/V8 | `dotnet test --project xUnitTest/xUnitTest.csproj -c Debug/Release --no-build --no-restore --minimum-expected-tests 5437`, serial bounded runs | PASS; 5,437 tests each, zero failed/skipped. Current compiler/test DLL hashes retained and rechecked |
| Current grouped pair reconstruction / V5–V7 | Grouping changes only semantic reconstruction; resulting Types are identical to the existing ungrouped form | NOT_RUN for LLVM/native execution; a focused test retains the generic-generation rejection boundary. No NativeAOT |
| `20260915T1653069921318Z`, `20260915T1654050324070Z` / V3 | Origin fragment audit at `889d7c9` with preserved Layout/arity edits and 20 new tests; `dotnet build Kimigayo.slnx -c Debug/Release --no-restore -v:minimal`, serial bounded runs | PASS; both solution configurations built with zero warnings/errors |
| `20260915T1653379481962Z` / T4/V4/V8/V11 | `dotnet test --project xUnitTest/xUnitTest.csproj -c Debug --no-build --no-restore --minimum-expected-tests 20 --filter-class XunitTest.OriginFragmentBindingTest` | PASS; 20 tests, zero failed/skipped, including exact bound identities, rejection boundaries, source order, reload/writing and zero measured warm unbounded-fragment Binding allocations |
| `20260915T1653527504869Z`, `20260915T1654386127636Z` / V4/V8 | `dotnet test --project xUnitTest/xUnitTest.csproj -c Debug/Release --no-build --no-restore --minimum-expected-tests 5414`, serial bounded runs | PASS; 5,414 tests each, zero failed/skipped. Current compiler/test DLL hashes retained and rechecked |
| Current Origin fragment audit / V5–V7 | Tests/documentation only; compiler behavior unchanged | NOT_RUN for LLVM/native execution; bounded declarations remain uncertified pending M4/I7 proofs. No NativeAOT |
| `20260915T1643222117552Z`, `20260915T1645180389231Z` / V3 | Final Type arity group at `889d7c9` plus preserved Layout and current arity diffs; `dotnet build Kimigayo.slnx -c Debug/Release --no-restore -v:minimal`, serial bounded runs | PASS; both solution configurations built with zero warnings/errors |
| `20260915T1643575478773Z` / T3/T4/V4/V8/V11 | Final Debug `dotnet test --project xUnitTest/xUnitTest.csproj -c Debug --no-build --no-restore --minimum-expected-tests 28 --filter-class XunitTest.TypeArityBindingTest` | PASS; 28 tests, zero failed/skipped; includes distinct symbols, matching fragments, scope commitment, import ambiguity, generic member/base paths, Layout separation, reload/writing/replacement and zero measured warm Binding allocations. Compiler/test DLL hashes retained and rechecked |
| `20260915T1644104323883Z`, `20260915T1646004102416Z` / V4/V8 | Final Debug/Release `dotnet test --project xUnitTest/xUnitTest.csproj -c <configuration> --no-build --no-restore --minimum-expected-tests 5394` | PASS; 5,394 tests each, zero failed/skipped. Includes six new module/default-alias arity cases, prior Layout coverage and corrected parser/conformance regression fixtures. Current compiler/test DLL hashes retained and rechecked |
| Current Type arity group / V5–V7 | Static Type identity/lookup and parser collection changes; no new native representation or runtime operation | NOT_RUN for LLVM/ordinary native execution; nominal/generic emission gates remain. No NativeAOT. Previous native evidence stays historical |
| `20260915T1626556557375Z`, `20260915T1628343214985Z` / V3 | Final M2/I3 Layout group at `889d7c9` plus this execution's source diff; `dotnet build Kimigayo.slnx -c Debug/Release --no-restore -v:minimal`, serial bounded runs | PASS; both solution configurations built with zero warnings/errors |
| `20260915T1628133043503Z` / T4/V4/V8/V11 | Final Debug `LayoutAttributeBindingTest`, `dotnet test --project xUnitTest/xUnitTest.csproj -c Debug --no-build --no-restore --minimum-expected-tests 42 --filter-class XunitTest.LayoutAttributeBindingTest`; compiler/test DLL hashes retained in result.json | PASS; 42 tests, zero failed/skipped. Includes source/reload/mutation, canonical writing, skipped Test syntax, generic/base-header attachment, nominal-generation gate, and zero measured warm Binding allocations |
| `20260915T1628264583031Z`, `20260915T1629022464444Z` / V4/V8 | Final Debug/Release `dotnet test --project xUnitTest/xUnitTest.csproj -c <configuration> --no-build --no-restore --minimum-expected-tests 5360`; current compiler/test DLL hashes retained and rechecked | PASS; 5,360 tests per configuration, zero failed/skipped. Includes existing fragment, Property, module, product Test, directive, serialization, emission and allocation regressions |
| Current Layout group / V5–V7 | No new physical representation or runtime operation; existing nominal-generation rejection explicitly tested | NOT_RUN for LLVM/native execution in this source-validation group. Previous native runs remain historical. NativeAOT excluded and not run |
| `20260915T1244041748695Z` / V3 | `e47b667`, Debug solution, `dotnet build Kimigayo.slnx -c Debug --no-restore -v:minimal` | PASS; zero warnings/errors; existing restore assets suffice, no NuGet configuration changes |
| `20260915T1244313768811Z` / V2 | Debug project MTP help, no build/restore | PASS; confirms `--filter-class`, `--filter-method`, `--parallel none`, `--minimum-expected-tests`; G3 resolved for current assets |
| `20260915T1244453018691Z` / V4 | Debug full managed baseline, `--parallel none`, 900-second outer deadline | PASS; 4,870 passed, zero failed/skipped; 11.668 seconds reported |
| `20260915T1247131750639Z` / T6/V4 | Six positive numeric reproducers before compiler changes | FAIL as intended; all six reach unsupported numeric Binding, establishing the implementation gap |
| `20260915T1249203053488Z`, `20260915T1249384769009Z` / T6/V4–V6 | First six numeric cases after implementation | PASS; 6 managed / 12 ordinary native O0/O2 executions; LLVM verification and actual object dependency checks included |
| `20260915T1252113639620Z`, `20260915T1252300005384Z` / T6/V4–V6 | Debug numeric, float conversion, integer conversion and division families | PASS; 335 managed tests; 193 numeric fixtures / 386 native O0/O2 executions |
| `20260915T1255206785509Z` / T6/V4/V8/V11 | Expanded numeric tests including reload/corrupt-plan rejection and warm allocation assertion | PASS; 65 managed cases; warm ownership analysis and IR writing allocate zero measured bytes for the new numeric workload. This is not a throughput or cold-allocation claim. |
| `20260915T1255483323158Z` / T6/V6 | New narrowing-overflow diagnostic fixture | FAIL in expected column only: test expected 30, source `x@f32` starts at 31 and emitted diagnostic reports 31. Corrected the test expectation from source; rerun required. |
| `20260915T1256284287548Z`, `20260915T1256454839265Z` / V3/V4 | Debug solution and full managed regression after compiler changes | PASS; zero build warnings/errors; 4,935 tests passed, zero failed/skipped. The later test-only duplicate-boundary suppression was rerun in the next focused record. |
| `20260915T1302268092996Z`, `20260915T1302479900143Z` / T6/V3/V4/V8/V11 | Final Debug numeric test source; freshly removed only old owned NumericConvert fixture files before generation | PASS; zero build warnings/errors; 65 focused tests. Compiler DLL, test DLL and test source hashes recorded. Duplicate representable boundary values are covered once, retaining distinct signed-zero bits. |
| `20260915T1303113297573Z` / T6/V5/V6 | Final fresh Debug numeric inventory, LLVM 22.1.8, Windows x64 O0/O2 | PASS; 193 fixtures / 386 native executions, each with LLVM verification and actual undefined-symbol inspection. All 965 fixture/expectation files plus compiler/test DLLs, test source, script, profile, tools and backend archive hashes recorded and checked unchanged. Corrected column-31 overflow fixtures pass. |
| `20260915T1307147006463Z`, `20260915T1307460191308Z` / V3/V4/V8/V11 | Release solution and full managed suite, final sources | PASS; zero build warnings/errors; 4,935 tests passed, zero failed/skipped. Includes new warm zero-allocation assertion and existing allocation regressions. |
| `20260915T1307460191308Z/numeric-artifact-comparison.json` / V5/V6 artifact identity | Release-regenerated fixtures versus native-verified Debug artifacts | PASS; all 965 files match SHA-256, including all 193 IR modules. Native execution was performed on the identical Debug-generated bytes; no separate Release native execution is claimed or needed for this artifact comparison. |
| `test-verification.ps1` / I2/V2 | Windows verification harness, final scripts | PASS; exact stdout/stderr bytes, exit 7 propagation, two-second deadline killing the target, and an exited parent with a descendant retaining pipes. Retained-pipe failure reaches the five-second drain check, and the descendant is gone after job closure. Records are under `TestResults/verification-check-*`. |
| `20260915T1313170426639Z`, `20260915T1323029876760Z` / I4/T6/T11/V4/V8/V11 | Owned Identity Acquisition before/after implementation | Eight initial positives failed the existing Binding gate. Final focused 322 conversion cases PASS, including 34 identity cases, partial moves/repair, cleanup responsibility, evaluation order, grouped shorthand, corrupt-plan rejection, reload and warm zero measured analysis/writing allocations. |
| `20260915T1323588238015Z` / I4/V5/V6 | Fresh identity fixture inventory | PASS; 29 IR modules / 58 native O0/O2 executions with LLVM verification, destruction audits and exact unchanged tool/backend/compiler/test/fixture hashes. |
| `20260915T1324469428991Z`, `20260915T1324528303336Z`, `20260915T1325531022080Z`, `20260915T1326142216457Z` / V3/V4 | Identity checkpoint, Debug and Release solution builds/full managed suites | PASS; zero warnings/errors; 4,969 tests each. The previous full run's one obsolete unsupported string-identity assertion was changed to use-after-acquisition rejection under §13.5.3. Both configurations cover the replacement. |
| `20260915T1326142216457Z/artifact-comparison.json` / V5/V6 | Regenerated numeric and identity artifacts | PASS; all 965 numeric and 145 identity files match native-verified bytes. |
| `20260915T1328338623600Z`, `20260915T1337213225628Z` / I3/T2/V4/V8/V11 | Constant-readable lengths before/after implementation | Seven initial positives failed as expected. Final 48 focused cases PASS: all 12 arithmetic Types, 128-bit intermediates, final/isize bounds, overflow/zero division, excluded bindings/accessors, cycles, qualification/access, private constant expansion, symbolic lengths, changed initializer, reload and zero measured warm Binding allocations. |
| `20260915T1337518097535Z` / I3/V5/V6 | Fresh constant-length fixtures | PASS; 2 IR modules / 4 native O0/O2 executions. Exact input inventory and unchanged hashes recorded. Static Property generation is still gated; only local fixed-array programs run natively. |
| `20260915T1338010167722Z`, `20260915T1338124601486Z`, `20260915T1338347609054Z`, `20260915T1340057185502Z` / V3/V4 | Length checkpoint, Debug and Release solution builds/full managed suites | PASS; zero warnings/errors; 5,017 tests each. `20260915T1340057185502Z/artifact-comparison.json` confirms all 1,120 numeric/identity/length artifact files still match native-verified bytes. |
| `20260915T1350006895537Z`, `20260915T1350428392310Z` / I3/T3/V4 | Generic declaration API constraint reproducers and current Debug regression | Before the constraint fix, 3 rejected-source cases incorrectly passed Binding while 28 API cases passed. After implementation, all 5,048 managed tests PASS. Latest test-only reload/allocation additions still need final verification. A prior method-only filter selected zero tests and was rejected by the nonzero-count policy; it is not evidence. |
| `20260915T1353077838198Z`, `20260915T1353294779544Z`, `20260915T1354149952470Z` / I3/T3/V3/V4/V8/V11 | API slice focused tests, Release solution build/full managed suite | PASS; 35 focused cases including reload/replacement and zero measured warm Binding allocations; Release build zero warnings/errors and 5,052 full managed passes. No new executable representation is admitted, so no new API-native fixture is claimed. |
| `20260915T1355355529635Z`, `20260915T1359124991298Z` / I3/T5/V4/V8/V11 | Static default-argument Binding | Before the fix, five preceding-parameter positives failed and three negatives passed. Final 14 focused cases PASS: optional/required initializer checks, preceding/internal parameter names, ordering, caller-scope exclusion, no generic inference from defaults, symbol identity through rebind/reload, and zero measured warm Binding allocations. OwnershipAnalysis's default execution gate remains intact for M4. |

| `20260915T1400177006720Z`, `20260915T1402288148173Z` / I3/T5/V3/V4 | Default Binding full regression | PASS: Debug and Release 5,066 tests each, with fresh zero-warning/error solution builds. |
| `20260915T1403380183161Z`, `20260915T1407325063188Z` / I3/T8/V4/V8/V11 | Local fixed-array element inference | Before implementation, nine positives failed. Final 30 focused tests PASS: established element Types/literal fitting, nested fixed shapes, Copy/Move, cleanup order, evaluation count, rebind/reload, and zero measured warm Binding allocations. Untyped control-flow collections and generic inference remain outside this completed subrange. |
| `20260915T1407555505830Z` / I3/T8/V5/V6 | Array inference ordinary Windows native | PASS: 21 modules, 42 O0/O2 executions, exact output/exit expectations, LLVM verification and hashed inputs. |
| `20260915T1408406656418Z`, `20260915T1409358955905Z`, `20260915T1410022621065Z` / V3/V4 | Array checkpoint full regression | PASS: fresh Debug/Release solution builds, zero warnings/errors, 5,096 managed tests per configuration. Release artifact-comparison.json confirms all 1,225 numeric/identity/length/array files match native inputs. |

### Remaining work and resumption checkpoint

#### Independent verification pass I3-V1 (2026-09-16, after commit `680ed55`)

- **Scope.** Re-verified the state recorded for I3-G24/G25/G26/G27 and I3-A8/A9/A10 against the current source and SPEC, rather than reusing their recorded results. Working tree was clean at `680ed55`; no unrelated user changes existed to preserve.
- **Recorded gates reproduced.** `dotnet build Kimigayo.slnx -c Debug/Release --no-restore -v:minimal` PASS with zero warnings/errors; `dotnet test --project xUnitTest/xUnitTest.csproj -c Debug/Release --no-build --no-restore --minimum-expected-tests 6418` PASS 6,418 each, zero failed/skipped. The recorded 6,418 count and the 134-case delta from I3-G23's 6,284 both hold.
- **Direct source verification.** Traced the committed Binding changes: the conditional-premise branch in `ValidateConstraintProjectionAccess` adds a check where the previous switch reached `default: continue`, so it removes no earlier validation; `ContractConstraint` tolerates a null Self through `EnclosingContractSelf`; premise operands are always a `SyntaxFormKoto` by construction in `DeclarationContainerKoto`, so the cast in the member-premise pass cannot fault; `AccessCovers` union domains implement the Type/Contract exposure intersection required by SPEC 9.3.2/9.3.4; `InternConstraint` keys are struct-based, so the added normalization stays allocation-free on warm paths, as the unit allocation tests measure.
- **Behavioral probes (temporary, not retained).** Bypass routes for associated-projection subject clauses (parameter, field, base, enum payload, local, result, associated specification, conditional premise requirement) all reject the refuted construction; conditional member applicability with caller evidence, self-recursive and cross-block calls, and inapplicable-overload skipping all behave as SPEC 8.4.8.2 requires; 20 malformed conditional-conformance sources produce diagnostics without a crash; 60 top-level declaration permutations of an accepted conditional/projection program give identical results.
- **Defect found and fixed.** SPEC 8.2 requires every Constraint Clause of a declaration to hold. `BindConstraint` admitted any associated-projection subject inside a Contract, so a clause rooted in an unrelated concrete Type, such as `Source.Origin.Item is string` in `contract R`, was accepted silently and asserted as an environment premise; only a later conformance attempt failed. Struct, enum and function declarations already reject such closed subjects. The `scope.Owner is ContractKoto` disjunct now also requires the projection root to be a Contract, so these clauses are rejected as `InvalidConstraint` at the declaration and never become facts. Contract- and Self-rooted premises, which every implementation must discharge, are unchanged.
- **Regression coverage.** `ContractConstraintSubjectBindingTest` adds 12 cases: contract-rooted and Self-rooted premises still constrain implementations both ways, concrete-rooted subjects are rejected for identity/Type/capability requirements, an invalid subject cannot certify conformance, struct/enum controls stay invalid, subject replacement revokes and restores the Contract, and warm checks measure zero allocations. Five of them fail on the pre-fix compiler and pass after it.
- **Final verification.** Debug and Release builds PASS with zero warnings/errors; full Debug and Release suites PASS 6,430 each, zero failed/skipped. Native, LLVM and CLI levels NOT_RUN; NativeAOT excluded.
- **Boundaries.** The general clause-subject rule of SPEC 8.2, which also admits complete Types such as `Source is Origin` as subjects, is still narrower in the implementation than in the specification for every declaration kind; this pass made Contracts consistent with that existing boundary rather than widening it. Recorded in STATUS.

#### Current timed execution (started 2026-09-16T05:25:36Z; soft deadline 06:25:36Z)

- **I3-G24 DONE:** reproduced the recorded valid projected-constraint call limitation with an explicit Unit body; candidate proof Types now use existing associated-Type normalization even with no receiver Self. Positive/refuted constraints, nested Types, dependent evidence, late-invalid witnesses, rebind/reload and warm allocation coverage pass. Generic native generation remains separately gated; no new runtime representation or allocation-bearing cache.
- Initial repository/instruction/specification inspection and checkpoint comparison completed; evidence root `TestResults/timed-execution-20260916-052536/`. No blocker for this unit. Subsequent units below cover conditional conformance premise validity.
- **I3-G24 baseline:** `unit1-projected-calls/20260916T0530065000677Z` FAIL all three intended positive reproducers (direct, Tuple and fixed-array requirements remain unresolved). Corrected test-only API names before baseline; baseline build had one nullable warning, now fixed. Two sandbox build attempts failed with no diagnostics; the authorized bounded build outside the sandbox reports actual results. Implementation now uses existing `ContractConstraint` normalization even when there is no receiver Self; verification appears below.
- **I3-G24 focused/Debug verification:** Debug build `20260916T0533401065343Z` PASS, zero warnings/errors; focused `20260916T0534162651986Z` PASS 20; full Debug `20260916T0534371177301Z` PASS 6,304, zero failed/skipped. Corrected an intermediate test-only diagnostic enum name before these checks. STATUS updated; SPEC 8.1.1/8.2/8.4.3 already requires normalized identities, so no specification edit. Native NOT_RUN; no NativeAOT.
- **I3-G24 final verification / boundary:** Release build `20260916T0535001258108Z` PASS, zero warnings/errors; full Release `20260916T0536127888590Z` PASS 6,304, zero failed/skipped. At `05:36:40Z`, about 11 minutes elapsed. Unit complete; no blocker. Existing DLL hashes are retained in each test record.
- **I3-G25 DONE:** retained projection inputs in conditional conformance premises satisfy SPEC 8.4.8 before certification. Normalized Types cannot conceal invalid qualifiers or late-invalid witnesses. Ordinary/intrinsic certificates, committed calls, independent paths and containing-Type formation separation are verified. API exposure of conditional premises is a separate following audit.
- **I3-G25 baseline:** `unit2-conditional-inputs/20260916T0538109186267Z` FAIL both stale-certificate reproducers after clean Debug build `20260916T0537305952383Z`. Retained projection validation now identifies a premise's existing conditional target and also invalidates its block for member applicability. The existing conformance and Copy target checks remain unchanged; no new retained storage. Expanded verification appears below.
- **I3-G25 necessary interaction:** focused run `20260916T0542340899864Z` passed 12/14 but exposed stale already-selected calls through conditional members and Copy constraints. Final call completion previously rechecked Type formation only, not the selected function's constraints/member premises. It now rechecks the committed candidate's existing substitutions and conditions without rerunning lookup or overload selection. Required to close this unit's certificate invariant; both failures were resolved by the subsequent rerun below.
- **I3-G25 Debug verification:** final Debug build `20260916T0543473329928Z` PASS, zero warnings/errors; focused `20260916T0544505142306Z` PASS 18, resolving both interaction failures; full Debug `20260916T0545075759337Z` PASS 6,322, zero failed/skipped. Release results appear below. STATUS updated; SPEC 8.4.8 already requires these proofs. No new runtime representation; native NOT_RUN and no NativeAOT.
- **I3-G25 final verification / boundary:** Release build `20260916T0545318775368Z` PASS, zero warnings/errors; full Release `20260916T0547022512737Z` PASS 6,322, zero failed/skipped. At `05:47:38Z`, elapsed **22m02s**. Unit complete and no blocker. Historical intermediate pending states above are resolved by this checkpoint.
- **I3-A8 DONE:** conditional conformance premise API exposure follows SPEC 9.3.2/9.3.4 and 8.4.8. Ordinary requirement Types/Contracts and retained projection domains cover each conformance's Type/Contract intersection, including ancestors; unrelated paths remain independent. Conditional members' potentially wider published domains will be audited separately.
- **I3-A8 baseline / implementation:** clean Debug baseline build `unit3-conditional-access/20260916T0548288083302Z`; baseline `20260916T0549023563269Z` FAIL all three false-success cases (Contract, direct projection, nested projection). Existing access helpers now accept the Type/Contract intersection; conformance verification checks ordinary premises and the retained projection pass checks every matching ancestor path. Debug build `20260916T0549509661009Z` and initial full Debug `20260916T0550403740690Z` PASS 6,325, zero warnings/errors/failures/skips. Expanded lifecycle/domain evidence appears below; no new cache or specification change.
- **I3-A8 Debug verification:** final Debug build `20260916T0552074798409Z` PASS, zero warnings/errors; focused `20260916T0553048842490Z` PASS 23; full Debug `20260916T0553261169477Z` PASS 6,345, zero failed/skipped. Release results appear below. STATUS updated; SPEC already owns these domains. Native NOT_RUN; no NativeAOT.
- **I3-A8 final verification / boundary:** Release build `20260916T0554099636135Z` PASS, zero warnings/errors; full Release `20260916T0555237556152Z` PASS 6,345, zero failed/skipped. Boundary `05:55:50Z`, about 30 minutes elapsed; unit complete, no blocker.
- **I3-A9 DONE:** published P preconditions of conditional functions/computed members cover each member's effective API domain (SPEC 8.4.8.1/9.3.2). A narrower conformance domain does not narrow a public block member. Member-only failures remain separate from independent members and conformances.
- **I3-A9 baseline:** clean Debug build `unit4-member-access/20260916T0556306118439Z`; baseline `20260916T0557232646008Z` FAIL all four false-success cases for functions/computed Properties with Contract or projected premises. Ordinary and retained-projection API passes now check each member's published P against its own domain, invalidating the member only. A local-name collision in the first implementation build was corrected before verification; expanded evidence appears below.
- **I3-A9 Debug verification:** Debug build `20260916T0559508265939Z` PASS, zero warnings/errors; focused `20260916T0601037465095Z` PASS 22, including independent conformances/siblings, member/witness/call certificates, effective domains, repair/reload and zero warm allocations. Final full-suite results appear below; STATUS updated, SPEC already correct. Native NOT_RUN; no NativeAOT.
- **I3-A9 final verification / boundary:** full Debug `20260916T0601349756463Z`, Release build `20260916T0602087259454Z`, full Release `20260916T0603200296516Z` PASS; builds zero warnings/errors, suites 6,367 each with zero failed/skipped. At `06:03:55Z`, elapsed **38m19s**. Unit complete and no blocker.
- **I3-G26 DONE:** constructed structs/enums enforce associated-projection subject clauses in `CheckConstraints` after substitution, including `T.Origin.Item is i32` (SPEC 8.1.3/8.2). Separate Self-conformance and associated-specification responsibilities remain; direct/nested uses, dependent evidence and certificates are verified.
- **I3-G26 baseline:** `unit5-associated-subjects/20260916T0604415375995Z` clean Debug build; `20260916T0605343465306Z` FAIL both false-success cases. The constructed-Type input filter now includes associated-projection subjects; the existing substitution/normalization/proof engine discharges them. No new cache, representation or inference rule. Focused and regression evidence appears below.
- **I3-G26 Debug verification:** final Debug build `20260916T0608184650131Z` PASS, zero warnings/errors; focused `20260916T0609098419293Z` PASS 26. Initial full Debug `20260916T0606530082789Z` passed 6,369 before expanding coverage; final Debug/Release results appear below. STATUS updated; SPEC 8.1.3/8.2 already requires all clauses. Native NOT_RUN; no NativeAOT.
- **I3-G26 final verification / boundary:** full Debug `20260916T0609599747339Z`, Release build `20260916T0610410043231Z`, full Release `20260916T0611589379775Z` PASS; zero build warnings/errors, 6,393 tests each, zero failed/skipped. Boundary `06:12:48Z`, elapsed **47m12s**; unit complete, no blocker.
- **I3-A10 DONE:** delivered projected calls, conditional premise validity/API domains, published member conditions and associated-subject Type arguments are verified across source modules, both alias routes and source reparsing. Library-only declaration rejection and independent certificates are preserved. No production change was required.
- **I3-A10 focused/Debug verification:** `unit6-module-integration/20260916T0613519915952Z` Debug build PASS, zero warnings/errors; focused module suite `20260916T0615108435702Z` PASS 153, including 20 new alias/reparse cases. No production change was required. Full Debug/Release results and cumulative review appear below; native NOT_RUN and no NativeAOT.
- **I3-A10 final verification / boundary:** full Debug `20260916T0615478422119Z`, Release build `20260916T0616267475172Z`, full Release `20260916T0617432355860Z` PASS; zero build warnings/errors, 6,413 tests each, zero failed/skipped. Boundary `06:18:17Z`, elapsed **52m41s**. Cumulative production diff reviewed; no unwanted runtime/representation changes.
- **I3-G27 investigation:** final bounded unit checks computed Property certificates inside invalid conditional blocks. Inspection shows `InvalidDeclarationContext` recognizes DeclarationKoto ancestors but not the conditional scope that owns P. Reproduce stale certificates first, then extend that shared guard if confirmed; preserve unrelated outer Properties, recovery and zero warm allocations. Finish this unit and checkpoint at the soft deadline without starting another unit.
- **I3-G27 baseline / implementation:** clean Debug build `unit7-conditional-properties/20260916T0619017924229Z`; baseline `20260916T0620438705520Z` FAIL the two new stale Property certificates (20 other cases PASS). The shared declaration-context guard now recognizes invalid conditional scopes. Reuses the existing parent walk, with no new state or allocation. Added reload/rebind, witness repair and warm Property allocation checks; final results follow. No further unit will begin in this execution.
- **I3-G27 Debug verification:** Debug build `20260916T0621345856895Z` PASS, zero warnings/errors; focused `20260916T0623227480217Z` PASS 23, resolving the two baseline failures and covering recovery, independent outer Properties and zero warm allocations. Final full-suite gates passed as recorded below. STATUS updated; SPEC 8.4.8/11.2 already requires valid enclosing premises. Native NOT_RUN; no NativeAOT.
- **I3-G27 DONE:** final full Debug `unit7-conditional-properties/20260916T0623392479376Z` PASS 6,418; final Release build `20260916T0624140286118Z` PASS with zero warnings/errors; final full Release `20260916T0629100521256Z` PASS 6,418, zero failed/skipped. Five new Property cases bring this execution to 134 additions. Both baseline failures are resolved; current DLL hashes identify the full-suite inputs. No new unit started after 06:25:36Z; only this unit's bounded verification and checkpoint continued.
- **Final disposition:** all seven selected units DONE; no incomplete implementation or verification remains within them. M2/I3 and the compiler completion plan remain IN_PROGRESS. SPEC.md needs no edit because its owning normative rules already require these behaviors; STATUS.md describes the implemented slices. No draft edits, commits, LLVM/native verification or NativeAOT. All 52 preceding checkpoint source files outside this execution's changes remain hash-identical. Evidence includes retained failures, verification-index.json, final-source-hashes.json, preserved-source-comparison.json, final-artifact-comparison.json, final.diff and final-checkpoint.json under this execution's evidence root.
- **Next action:** fresh-read the repository, then establish positive/negative/dependent/reload reproducers for equivalent concrete-Type versus associated-Type requirements in Binding.Prove under SPEC 8.7. No defect or completion is asserted for that unstarted audit; keep general proof/inference expansion outside this completed group.

#### Completed timed execution (started 2026-09-16T04:11:41Z; soft deadline 05:11:41Z)

Final checkpoint `2026-09-16T05:12:31Z`, elapsed **60m50s**. Eight units, 153 added managed tests; final manifests: `final-source-hashes.json`, `final.diff`, `checkpoint-comparison.json`, `verification-index.json`, `final-artifact-comparison.json`, `final-checkpoint.json`.

Fresh repository, instructions and plan inspection completed; all 55 preceding checkpoint hashes match. Evidence root: `TestResults/timed-execution-20260916-041141/` (initial diff, source hashes and comparison retained). I3-G18 selects related expression certificates whose explicit Type syntax may normalize away invalid projection inputs. Baseline reproduction is pending; no defect assumed. General Origin/body proofs and G1/G2 remain separate.

- **I3-G18 DONE:** six baseline cases proved that invalid projection qualifiers in explicit generic calls and runtime-test targets left queryable certificates despite overall Binding rejection. The existing retained projection list now validates those expression owners after final declaration/constraint validation. No new cache or allocations; ordinary normalized Type identity remains unchanged. SPEC 8.1.3/13.6.1 already require valid inputs; no specification change.
- **Evidence:** `unit1-expression-inputs/`: baseline build `20260916T0415479769247Z` PASS; baseline focused `20260916T0416462370148Z` FAIL 6/6 stale certificates. Fixed Debug/Release builds `20260916T0419259512949Z` / `20260916T0421390380066Z` PASS, zero warnings/errors; focused `20260916T0420368990090Z` PASS 15 (nested Tuple/array/function Types, caller evidence, reload, replacement recovery and zero measured warm allocations). Full Debug/Release `20260916T0420517832780Z` / `20260916T0422531435086Z` PASS 6,146 each, zero failed/skipped. Source hashes retained. Boundary elapsed 11m32s. Native NOT_RUN for metadata/rejection changes; NativeAOT excluded.
- **I3-G19 DONE:** both baseline expressions retained a normalized certificate after their function conformance witness failed API access validation. `CheckProjectionInputs` now combines complete qualifier constraints with the final conformance proof. No new cache. Valid nested projections, independent member errors, reload, witness repair and zero measured warm allocations are covered. `unit2-projection-certificates/`: baseline build `20260916T0424131722894Z` PASS; focused baseline `20260916T0425048424150Z` FAIL 2/2 stale certificates. Fixed Debug/Release builds `20260916T0426025388402Z` / `20260916T0427321003680Z` PASS, zero warnings/errors; focused `20260916T0426530942779Z` PASS 11; full Debug/Release `20260916T0427085608397Z` / `20260916T0428219987119Z` PASS 6,157 each, zero failed/skipped. Source hashes retained. Boundary elapsed about 17m. Native NOT_RUN; no NativeAOT.
- **I3-A6 DONE:** both expression forms are covered across module boundaries with explicit/default aliases, constrained projection inputs, late-invalid library witnesses and reparsing. Added 16 cases to existing module fixtures. `unit3-module-projections/`: Debug/Release builds `20260916T0429520205851Z` / `20260916T0431063819467Z` PASS, zero warnings/errors; focused module `20260916T0430316103909Z` PASS 105; full Debug/Release `20260916T0430455052482Z` / `20260916T0432150170656Z` PASS 6,173 each, zero failed/skipped. Source hashes retained. Boundary elapsed 20m52s; no production change needed. Native NOT_RUN; no NativeAOT.
- **I3-G20 DONE:** final signature projection inputs now require conformance proof, and changed declaration states trigger revalidation of existing Property/conformance certificates until stable. Common valid Binding takes one pass; no new cache or allocation. `unit4-signature-projections/`: corrected baseline build `20260916T0434408960053Z` PASS; baseline `20260916T0435257049806Z` FAIL both stale certificates. The first baseline `20260916T0434036563908Z` also exposed an invalid test-only Property requirement spelling; corrected to `property ... has get` before establishing its baseline. Fixed Debug/Release builds `20260916T0437546513558Z` / `20260916T0439533910729Z` PASS, zero warnings/errors; focused `20260916T0438406411556Z` PASS 16 (multi-hop/reversed source order, self projections, direct calls, reload, repair and zero warm allocations); full Debug/Release `20260916T0438551418554Z` / `20260916T0440411163266Z` PASS 6,189 each, zero failed/skipped. Source hashes retained. Boundary elapsed 29m20s. Native NOT_RUN; no NativeAOT.
- **I3-G21 DONE:** the existing declaration propagation pass now includes enum payload owners and direct bases; changed bases use existing recursive inheritance validation before recertification. `unit5-aggregate-projections/`: baseline build `20260916T0441543100736Z` PASS; focused baseline `20260916T0442333316956Z` FAIL all six stale certificates. Fixed Debug/Release builds `20260916T0444595309833Z` / `20260916T0446371515827Z` PASS, zero warnings/errors; a prior successful build reported SA1204, corrected by placing the static helper first. Focused `20260916T0445418427757Z` PASS 20 (both declaration orders, projected base arguments/Types, enum constructions, descendants, reload, repair and zero warm allocations); full Debug/Release `20260916T0445586764788Z` / `20260916T0447254795360Z` PASS 6,209 each, zero failed/skipped. Source hashes retained. Boundary elapsed about 36m. Native NOT_RUN; no NativeAOT.
- **I3-G22 DONE:** associated specifications now join existing late declaration propagation. Invalid fixed associated definitions invalidate their Contract; ordinary/conditional specifications invalidate their matching conformance, preserving unrelated paths. Ordinary conformance verification now observes invalid matching associated clauses, as conditional paths already did. `unit6-associated-projections/`: baseline build `20260916T0448396728891Z` PASS; baseline `20260916T0449130860328Z` FAIL all four stale certificates. Fixed Debug/Release builds `20260916T0450543734538Z` / `20260916T0452333829290Z` PASS, zero warnings/errors; focused `20260916T0451375358833Z` PASS 18 (fixed/inherited/conditional, nested Types, reversed source order, independent paths, self references, reload, repair and zero warm allocations); full Debug/Release `20260916T0451568561222Z` / `20260916T0453350154322Z` PASS 6,227 each, zero failed/skipped. Source hashes retained. Boundary elapsed about 42m. Native NOT_RUN; no NativeAOT.
- **I3-A7 DONE:** added 28 module integration cases for function/Property signatures, enum payloads, base arguments, ordinary/fixed/conditional associated specifications, both alias routes and reparsing against valid/late-invalid library witnesses. `unit7-module-declarations/`: Debug/Release builds `20260916T0454535468490Z` / `20260916T0456456209002Z` PASS, zero warnings/errors; focused module `20260916T0455565178173Z` PASS 133; full Debug/Release `20260916T0456043435918Z` / `20260916T0457293202719Z` PASS 6,255 each, zero failed/skipped. Source hashes retained. Boundary elapsed about 46m. No production change needed. Native NOT_RUN; no NativeAOT.
- **I3-G23 DONE:** ordinary function/struct/enum/Contract constraint projections now participate in declaration validity propagation. Baseline `unit8-constraint-projections/20260916T0459056345690Z` reproduced stale struct/enum/Contract conformance certificates (3 failures); the function-call case was inconclusive because its positive form also stays unresolved. Focused/full verification found this separate call limitation: `take<T>()` constrained by `T is Source.Origin.Item`, called as `take<i32>()`, remains unresolved even with a valid public witness (same result with Unit body or bare return). This unit therefore verifies unused function declaration validity using the existing supported fixture shape. Do not claim projected-constraint calls complete. Next investigation: inspect `CheckConstraints`, substitution and candidate proof normalization for that minimal valid call. Conditional premise projection validity also remains unaudited. Final verification is recorded below.
- **I3-G23 final evidence:** `unit8-constraint-projections/`: final Debug/Release builds `20260916T0508292885777Z` / `20260916T0510197154933Z` PASS, zero warnings/errors; final full Debug/Release `20260916T0509372847394Z` / `20260916T0510571560336Z` PASS 6,284 each, zero failed/skipped. The 29 new cases cover declaration validity, forward references, dependent evidence, repair/reload, zero warm allocations and provisional/final lifecycle. The focused 21-case matrix passed before adding lifecycle controls; all 29 pass in both full suites. A test initially equated provisional validity with IsComplete; corrected against BindingResult's explicit Final-only completion rule. No production change was needed for that test correction.
- **Execution closure:** all eight selected units are DONE; M2/I3 remains IN_PROGRESS. Confirmed unresolved projected-constraint calls and unaudited conditional premise projections remain next. G1/G2 and broader M4/M7 proofs remain separate. Existing normative rules already require these input proofs, so SPEC.md needs no change. No draft edits, NativeAOT, native execution or commits. The final source/diff/verification/artifact manifests under the current evidence root identify the checkpoint; all four current full-suite DLL input hashes match.

#### Completed timed execution (started 2026-09-16T03:11:39Z; soft deadline 04:11:39Z)

Final checkpoint `2026-09-16T04:11:15Z`, elapsed **59m36s**. `final-source-hashes.json`, `final.diff`, `checkpoint-comparison.json`, `verification-index.json`, `final-artifact-comparison.json` and `final-checkpoint.json` under the current evidence root identify this verified boundary.

Fresh repository/instruction/plan reads completed. HEAD remains b0096b50801843b45244eda30a18b5c5d3f92db4; all 45 preceding checkpoint hashes match. Initial diff, hashes and comparison: `TestResults/timed-execution-20260916-031139/`. Selected first audit: associated-Type concrete definitions containing invalid nested generic Types; no defect assumed before baseline reproduction.

- **I3-G11 DONE:** conformance verification now proves nested input constraints for each normalized associated-Type definition, using that conformance path's declaration/conditional evidence. The existing normalization/Core identity stage remains unchanged; no extra cache or allocation is introduced.
- **Verification:** `unit1-associated-types/` under the current root: baseline build `20260916T0313040387767Z` PASS; focused baseline `20260916T0313462193889Z` FAIL all three stale conformance certificates. Final Debug/Release builds `20260916T0315054842806Z` / `20260916T0316425269624Z` PASS, zero warnings/errors; focused `20260916T0315461953359Z` PASS 15; full Debug/Release `20260916T0316010249491Z` / `20260916T0317460080589Z` PASS 6,005 each, zero failed/skipped. Fixed/inherited/conditional definitions, nested Tuple/array/unsafe components, valid dependent evidence, isolated siblings, reload/recovery and zero measured warm allocations covered. SPEC §§8.1.3/8.4.3 already require validity; no SPEC edit. Native NOT_RUN; no NativeAOT.
- **Boundary/next:** approximately six minutes elapsed. Next audit generic constraints on direct base Types: structural base validation runs before capability evidence, while final Type-use rejection may not invalidate derived declaration/conformance certificates. Establish order-independent reproducers before selecting the final validation point.
- **I3-G12 DONE:** base generic input constraints are checked with available capability evidence, then the existing inheritance walk propagates failures before Property/conformance certification. Early structural/cycle/access validation remains in place. No new cache or allocation introduced.
- **Verification:** `unit2-base-types/`: baseline build `20260916T0318521029474Z` PASS; baseline focus `20260916T0321133714712Z` FAIL both source-order cases (derived declarations incorrectly Resolved). Final Debug/Release builds `20260916T0323421172396Z` / `20260916T0325052795709Z` PASS, zero warnings/errors; focus `20260916T0324273495487Z` PASS 12; full Debug/Release `20260916T0324413852921Z` / `20260916T0325551268141Z` PASS 6,017 each, zero failed/skipped. Nested generic inputs, valid declaration premises, sibling isolation, provisional known failures, reload/recovery and zero measured warm allocations covered. One interim build failed on the test Bind entry point and a formatting warning, both corrected before tests. SPEC §6.2 already requires base constraints; unchanged. Native NOT_RUN; no NativeAOT.
- **Boundary/next:** approximately 15 minutes elapsed. Audit retained runtime-Type-test plans against nested generic constraints and late-invalid declaration contexts; establish baseline before changing acceptance or publication.
- **I3-G13 DONE:** runtime Type tests validate the complete operand and target before publishing a plan and recheck them at final Binding completion. Invalid plans are cleared and cannot be reported as bound by control-flow analysis. This narrows certificate publication only; no runtime execution capability is added.
- **Verification:** `unit3-runtime-tests/`: baseline build `20260916T0327247820928Z` PASS; baseline focus `20260916T0327573731140Z` FAIL both stale input plans. Final Debug/Release builds `20260916T0330423306900Z` / `20260916T0332025548440Z` PASS, zero warnings/errors; focus `20260916T0331262018083Z` PASS 15; full Debug/Release `20260916T0331410149012Z` / `20260916T0332470177824Z` PASS 6,032 each, zero failed/skipped. Nested target inputs, late API/base invalidity, Never, independent member errors, reload/recovery and zero warm allocations covered. One fixture originally expected an unrelated member API error to invalidate the entire struct Type; corrected to a declaration-constraint API error and retained the member-error case as a positive isolation check. SPEC §§8.1.3/13.6.1 already require valid Types; unchanged. Native NOT_RUN; no NativeAOT.
- **Boundary/next:** approximately 22 minutes elapsed. Audit the settled §6.2 inherited Value-role Name prohibition. Existing lookup tests permit private derived declarations to hide accessible base names; declaration validity must be established independently of use-site lookup. Preserve lookup failure coverage and document any obsolete expectation correction.
- **I3-B2 DONE:** the existing inheritance validation now rejects a derived Value-role Name when normal member lookup finds an ancestor member accessible from the derived declaration. Signatures, conditional premises and narrower derived access do not exempt Names. Protected lookup uses the derived receiver and Property access rather than accessor access. Constructors/destructors/specializations do not introduce ordinary reserved Names here.
- **Verification:** `unit4-inherited-names/`: baseline build `20260916T0334430324405Z` PASS; baseline focus `20260916T0335294433714Z` FAIL all three incorrectly accepted declarations. Final Debug/Release builds `20260916T0339005984376Z` / `20260916T0340123742873Z` PASS, zero warnings/errors; focused `20260916T0338002799509Z` PASS 19; full Debug/Release `20260916T0339337508107Z` / `20260916T0341093646370Z` PASS 6,052 each, zero failed/skipped. Same-module access forms, private-name reuse, same-layer overloads, generic/transitive ancestors, conditional members, fragments, reload/replacement, descendant conformance rejection and zero measured warm allocations covered. Native NOT_RUN; no NativeAOT.
- **Deviation:** two old InheritedReceiverBindingTest expectations predated SPEC §6.2's inherited-Name prohibition: private derived hiding is now rejected at the derived declaration, and a conformance test observes this earlier Duplicate diagnostic. A new private-base case preserves incompatible private implementation coverage. The first full Debug run exposed the latter expectation; both corrected cases pass the final suites. One initial test build failed for a missing namespace import, corrected before baseline execution. SPEC already states the rule; unchanged.
- **Boundary/next:** approximately 30 minutes elapsed. Next verify all four current behavior changes across module boundaries, including explicit/default aliases, reparse and cross-module protected/internal access distinctions.
- **I3-A5 DONE:** added 24 cross-module cases for associated-Type inputs, direct-base constraints, runtime-test targets and inherited Names. Explicit/default aliases and reparsing all modules preserve declaration identity and validation. Cross-module public/protected/protected-internal collisions reject; private/internal/private-protected Names may be reused.
- **Verification:** `unit5-module-integration/`: Debug/Release builds `20260916T0342543007343Z` / `20260916T0344353257180Z` PASS, zero warnings/errors; module focus `20260916T0343521615907Z` PASS 89; full Debug/Release `20260916T0344131155999Z` / `20260916T0345066869323Z` PASS 6,076 each, zero failed/skipped. No production or SPEC changes required. Native NOT_RUN; no NativeAOT.
- **Boundary/next:** approximately 34 minutes elapsed. Audit associated-Type definition normalization where a projection qualifier itself contains invalid generic inputs but normalizes to a valid Core. Establish the missing-certificate baseline before choosing a fix.
- **I3-G14 DONE:** conformance verification checks retained pre-normalization associated definition candidates as well as normalized results. This prevents a valid normalized Core from hiding invalid constructed projection qualifiers, using existing candidate storage and declaration/conditional evidence.
- **Verification:** `unit6-associated-inputs/`: baseline build `20260916T0346209869340Z` PASS; baseline focus `20260916T0346598084623Z` FAIL both retained invalid certificates. Final Debug/Release builds `20260916T0348032453347Z` / `20260916T0349277248546Z` PASS, zero warnings/errors; focus `20260916T0348399272396Z` PASS 13; full Debug/Release `20260916T0348537194797Z` / `20260916T0350127439155Z` PASS 6,089 each, zero failed/skipped. Direct/Tuple/array projection inputs, fixed/inherited/conditional definitions, valid dependent evidence, reload/recovery and zero measured warm allocations covered. SPEC §§8.1.3/8.4 remains unchanged; native NOT_RUN; no NativeAOT.
- **Boundary/next:** approximately 39 minutes elapsed. Next audit the same retained projection input obligation in direct base syntax: normalization to an unconstrained open struct must not hide an invalid qualifier. Keep this unit limited to base inputs and descendant certificates.
- **I3-G15 DONE:** base projection validation now checks retained qualifier input constraints in the late evidence-ready phase, alongside the normalized base checks. The existing inheritance walk propagates failures to descendants before certification; early projection access validation remains separate from these proofs.
- **Verification:** `unit7-base-inputs/`: baseline build `20260916T0351217221948Z` PASS; baseline focus `20260916T0351540586621Z` FAIL both source-order cases with incorrectly Resolved descendants. Final Debug/Release builds `20260916T0352560993555Z` / `20260916T0354278361723Z` PASS, zero warnings/errors; focus `20260916T0353423304176Z` PASS 12; full Debug/Release `20260916T0354003875482Z` / `20260916T0355138739795Z` PASS 6,101 each, zero failed/skipped. Nested Tuple/array inputs, valid dependent bases, reload/recovery and zero measured warm allocations covered. No SPEC change; native NOT_RUN; no NativeAOT.
- **Boundary/next:** approximately 44 minutes elapsed. Next check normalized projection qualifiers in function and Property/accessor signatures, using existing retained signature-owner validation. Keep this unit bounded to those signature certificate owners.
- **I3-G16 DONE:** existing function and Property/accessor signature-owner validation checks retained projection qualifier input constraints before final certificates. Late call completion observes invalid function declarations, and Property/conformance verification rejects invalid signatures. No extra storage or allocation introduced.
- **Verification:** `unit8-signature-inputs/`: baseline build `20260916T0356245205197Z` PASS; baseline focus `20260916T0357030909441Z` FAIL both stale call/Property certificates. Final Debug/Release builds `20260916T0400050539313Z` / `20260916T0401575242437Z` PASS, zero warnings/errors; focus `20260916T0401017677348Z` PASS 20; full Debug/Release `20260916T0401202921999Z` / `20260916T0403060216810Z` PASS 6,121 each, zero failed/skipped. Function inputs/results, nested Property headers, getter/setter signatures, witnesses, dependent evidence, reload/recovery and zero measured warm allocations covered. One interim build failed because a test supplied a parameter descriptor instead of its owning Function to KotoHelper.Replace; corrected before test execution. No SPEC change; native NOT_RUN; no NativeAOT.
- **Boundary/next:** approximately 52 minutes elapsed. The final selected unit audits retained enum payload projection qualifier inputs, then checkpoints and stops. No work unit will start after the 60-minute deadline.
- **I3-G17 DONE:** retained enum payload projection inputs are checked after semantic evidence and API validation, before final Property/conformance certificates. Invalid enum declarations also invalidate retained construction plans through existing completion checks. Early enum projection access validation remains in place.
- **Verification:** `unit9-enum-inputs/`: baseline build `20260916T0404171632035Z` PASS; baseline focus `20260916T0405033849144Z` FAIL with a stale conformance certificate. Final Debug/Release builds `20260916T0406044754139Z` / `20260916T0408061638637Z` PASS, zero warnings/errors; focus `20260916T0406588309435Z` PASS 10; full Debug/Release `20260916T0407289164662Z` / `20260916T0409232498047Z` PASS 6,131 each, zero failed/skipped. Construction plans, unused/nested payloads, valid dependent inputs, reload and zero measured warm allocations covered. No SPEC change; native NOT_RUN; no NativeAOT.
- **Final disposition:** all nine selected units are DONE, adding 141 managed cases. M2/I3 remains IN_PROGRESS. No unfinished implementation or verification within those units and no new blocker. Existing G1/G2 public-interface blockers and M4/M7-dependent Origin/role/body proofs remain open. No later milestone began. Prior changes were preserved; no draft edits or commits. Two obsolete inherited-Name expectations were corrected to SPEC 6.2 with private implementation coverage retained; other interim failures were corrected fixture/build issues documented above. No out-of-scope findings added.
- **Concrete next action:** fresh-read the repository and this checkpoint, then reproduce normalized projection inputs in explicit generic call arguments (`take<Source<string>.C.Item>()`) and runtime-test target arguments (`value is Target<Source<string>.C.Item>`), with Source constrained to i32. Inspect retained projection uses, call argument formation and runtime-test completion against SPEC 8.1.3/13.6.1. Verify valid/dependent, reload and recovery controls before choosing a fix. This is an unstarted audit, not a confirmed defect; do not infer completion of general generic/Origin proofs.

#### Completed timed execution (started 2026-09-16T02:12:09Z; soft deadline 03:12:09Z)

Final checkpoint `2026-09-16T03:11:09Z`, elapsed **59m00s**. The current `final-source-hashes.json`, `final.diff`, `checkpoint-comparison.json`, `verification-index.json`, `final-artifact-comparison.json` and `final-checkpoint.json` identify the preserved workspace and successful checks. The nine completed units stop at this execution boundary; no subsequent audit has begun.

Fresh HEAD/repository/plan/instruction reads completed. All 35 prior checkpoint hashes match. Initial diff/hashes and comparison: `TestResults/timed-execution-20260916-021209/`. Initial audit covered explicit generic function argument validity and BoundCall publication semantics; reproduced defects and their verification are recorded below. M2/I3 remains IN_PROGRESS.

- **I3-G3 DONE:** call candidate applicability checks every completed generic substitution with the existing recursive Type-constraint proof, independently of whether the function constrains or uses that slot. Invalid explicit/inferred arguments cannot publish BoundCall; the current BindingState guard keeps retained storage unavailable until a valid rebind.
- **Verification:** `unit1-generic-calls/` under this execution root: baseline build `20260916T0213314295109Z` PASS; baseline focused `20260916T0214053573203Z` FAIL all three retained invalid call plans. Final Debug/Release builds `20260916T0215186260822Z` / `20260916T0217209063388Z` PASS, zero warnings/errors; focused `20260916T0216386435165Z` PASS 16; full Debug/Release `20260916T0216574613227Z` / `20260916T0217588183375Z` PASS 5,907 each, zero failed/skipped. Nested Tuple/array/pointer/function Types, valid/inferred/dependent arguments, declaration contexts, reload, same-plan recovery and zero measured warm allocations covered. Native NOT_RUN for rejection-only validation; no NativeAOT. SPEC §8.1.3 already owns the rule; unchanged.
- **Boundary/next:** approximately six minutes elapsed. Next audit nongeneric call signatures and declaring contexts: a resolved input/result Type can contain invalid constructed constraints even with no generic function slots. Establish direct signature and invalid enclosing-group reproducers, then select the smallest coherent validation change.
- **I3-G4 DONE:** call candidates check enclosing declaration validity and definition-side signature-Type constraints. This prevents nongeneric calls from bypassing nested constructed constraints; checks use the function's own evidence and preserve independent valid sibling calls.
- **Verification:** `unit2-call-signatures/`: baseline build `20260916T0219309272170Z` PASS; focused baseline `20260916T0220079916444Z` reproduced two retained invalid plans (wrapped signature and enclosing group); the direct constructed result was already invalidated by the final Type-use pass. Final Debug/Release builds `20260916T0221156166120Z` / `20260916T0222336258072Z` PASS, zero warnings/errors; focused `20260916T0221553838161Z` PASS 13; full Debug/Release `20260916T0222132950818Z` / `20260916T0223275227763Z` PASS 5,920 each. Input/result wrappers, dependent declaration evidence, valid siblings, reload, same-plan recovery and zero measured warm allocations covered. Native NOT_RUN for rejection-only changes; no NativeAOT.
- **Boundary/next:** about eleven minutes elapsed; no blocker. Next inspect call plans after late API/access and constraint validation, which runs after initial call selection. Reproduce an API-invalid callee retaining an earlier BoundCall before adding any completion pass.
- **I3-G5 DONE:** the existing final node-completion scan rejects retained call plans whose target declaration/context became invalid after candidate selection. This covers normalized signature projections and public generic constraint exposure without adding another tree scan or allocation.
- **Verification:** `unit3-call-completion/`: baseline build `20260916T0224499985241Z` PASS; baseline focused `20260916T0225308380770Z` FAIL all three stale plans. Final Debug/Release builds `20260916T0229415169989Z` / `20260916T0231012864126Z` PASS, zero warnings/errors; focused `20260916T0230231160270Z` PASS 8; full Debug/Release `20260916T0230393572902Z` / `20260916T0231386564566Z` PASS 5,928 each. Positive effective-domain controls, unrelated failures, reload, same-plan correction/recovery and zero measured warm allocations covered. Native NOT_RUN for rejection-only metadata validation; no NativeAOT. SPEC remains unchanged.
- **Boundary/next:** approximately twenty minutes elapsed, no blocker. Next audit Pattern plan completion when subject/payload Types are invalid: verify that whole/discard patterns cannot retain a successful coverage certificate after invalid Type formation. This is an investigation, not yet a confirmed defect.
- **I3-G6 DONE:** Pattern positions validate complete subject/payload Type constraints before coverage and recheck after late declaration validation. Wildcard/Binding paths no longer bypass this check. Existing positional storage and declaration evidence are reused; invalid Types suppress exhaustiveness/unreachable cascades (§14.8).
- **Verification:** `unit4-pattern-types/`: initial baseline helper failed to find root-body functions, followed by a helper compile error; neither is compiler defect evidence. Corrected baseline build `20260916T0235123671224Z` PASS and focused `20260916T0235435494652Z` reproduced four invalid subjects retaining Exhaustive coverage. Final Debug/Release builds `20260916T0238086146849Z` / `20260916T0239317750733Z` PASS, zero warnings/errors; focused `20260916T0238500718109Z` PASS 15; full Debug/Release `20260916T0239039055061Z` / `20260916T0240167582832Z` PASS 5,943 each. Includes nested/discard/binding/wrapped Types, declaration contexts, late enum API failure, dependent evidence, valid siblings, reload, retained-plan recovery and zero measured warm allocations. Native NOT_RUN: rejection-only Binding metadata; no NativeAOT or SPEC change.
- **Boundary/next:** 28m25s elapsed. Next audit instantiated call-plan Types after late API validation, and constrained declaring Types on member calls. The target-only completion guard does not itself revalidate these Types; establish reproducers before extending it.
- **I3-G7 DONE:** final call validation covers complete substituted generic arguments, result, receiver/argument operations and declaring/conforming Types after late API checks. Candidate applicability also proves constraints on the instantiated declaring Type. Shared source/parameter identities are checked once per operation; retained storage remains reusable.
- **Verification:** `unit5-call-types/`: baseline build `20260916T0242247354091Z` PASS; focused baseline `20260916T0243149385867Z` FAIL all three stale late-invalid argument/constrained-receiver plans. Final Debug/Release builds `20260916T0245256848269Z` / `20260916T0246526743742Z` PASS, zero warnings/errors; focused `20260916T0246150245090Z` PASS 13; full Debug/Release `20260916T0246319986836Z` / `20260916T0247291237889Z` PASS 5,956 each. Valid concrete/dependent receivers, inferred/wrapped Types, reload, isolated failure recovery and zero measured warm allocations covered. Native NOT_RUN for rejection-only Binding metadata; no NativeAOT or SPEC change.
- **Boundary/next:** 35m38s elapsed. Next audit Property certificates when signature Types fail late API validation; inspect validation ordering and reproduce before changing it. Existing certificates should not remain verified merely because initial validation preceded the Type declaration failure.
- **I3-G8 DONE:** moved existing Property validation after Origin/API validation and before final conformance checks. Origin analysis and API validation consume bound Types, not Property certificates, so this preserves prerequisites and avoids another scan. Late-invalid nested Types, owners and static borrow storage cannot remain certified.
- **Verification:** `unit6-property-completion/`: baseline build `20260916T0248552221411Z` PASS; focused baseline `20260916T0249386733931Z` FAIL all three stale certificates. Final Debug/Release builds `20260916T0250350631585Z` / `20260916T0252088334356Z` PASS, zero warnings/errors; focused `20260916T0251133229475Z` PASS 11; full Debug/Release `20260916T0251294231509Z` / `20260916T0253027620551Z` PASS 5,967 each. Positive/dependent controls, reload, same-certificate recovery, unrelated invalid Types and zero measured warm allocations covered. Native NOT_RUN for rejection-only signature certification; no NativeAOT or SPEC change.
- **Boundary/next:** approximately 41 minutes elapsed. Next verify the delivered Type/certificate behavior across source-module boundaries, explicit/default aliases and module reparsing using the existing module test infrastructure. This is focused integration coverage of the completed changes, not a new public feature.
- **I3-A4 DONE:** added twelve module integration cases for explicit/default alias lookup, complete generic arguments and late library API invalidation flowing into calls, Property certificates and Pattern coverage. Every case also reparses all source modules and checks fresh metadata. No production change was required.
- **Verification:** `unit7-module-integration/`: Debug/Release builds `20260916T0254378701184Z` / `20260916T0256372834805Z` PASS, zero warnings/errors; focused module suite `20260916T0255561294812Z` PASS 65; full Debug/Release `20260916T0256160731047Z` / `20260916T0258000883244Z` PASS 5,979 each. The existing module/native-generation boundary remains unchanged; native NOT_RUN, no NativeAOT.
- **Boundary/next:** approximately 46 minutes elapsed. Review found that Pattern warnings are produced before the late Type checks delivered in I3-G6. Next reproduce whether a late-invalid enum leaves an unreachable-arm warning, then move warning publication to the existing completion pass if confirmed. Keep broader coverage/error ordering outside this small follow-up unless required for coherence.
- **I3-G9 DONE:** moved unreachable Pattern warning creation into the existing completion pass after late Type validation. Reuses the same containment calculation and list; valid coverage warnings remain independent of body errors and retain once-only reporting.
- **Verification:** `unit8-pattern-warnings/`: baseline build `20260916T0258564747646Z` PASS; baseline focused `20260916T0259414184388Z` FAIL both misleading late-invalid Type warnings. Final Debug/Release builds `20260916T0300406714358Z` / `20260916T0302395579397Z` PASS, zero warnings/errors; focused `20260916T0301483306009Z` PASS 7; full Debug/Release `20260916T0302096079084Z` / `20260916T0304038018815Z` PASS 5,986 each. Rebind/reload, valid duplicate cases/wildcards, nonexhaustive valid patterns, independent body errors and zero measured warm allocations covered. Native NOT_RUN; no NativeAOT or SPEC change.
- **Boundary/next:** approximately 52 minutes elapsed. One final narrow diagnostic unit audits nonexhaustiveness errors produced before late Type rejection, using the same completion boundary. Do not expand into generic proof machinery or additional milestones.
- **I3-G10 DONE:** nonexhaustiveness diagnostics now wait for the same completion boundary as late Type validation. A match lacking coverage remains unresolved/unavailable until completion; a late Type failure suppresses the secondary missing-coverage error. Valid Types still require exactly the same exhaustiveness proof.
- **Verification:** `unit9-pattern-coverage/`: baseline build `20260916T0305019954225Z` PASS; baseline focused `20260916T0305454384129Z` FAIL with a misleading NonExhaustiveMatch diagnostic beside the Type/API failure. Final Debug/Release builds `20260916T0306464359150Z` / `20260916T0308200450211Z` PASS, zero warnings/errors; focused `20260916T0307256173551Z` PASS 4; full Debug/Release `20260916T0307478191318Z` / `20260916T0309223521295Z` PASS 5,990 each, zero failed/skipped. Valid missing coverage, completed coverage, CheckBound, rebind/reload and zero measured warm allocations covered. Native NOT_RUN; no NativeAOT or SPEC change.
- **Execution disposition:** nine coherent M2/I3 units completed (I3-G3/G4/G5/G6/G7/G8/G9/G10 and I3-A4), adding 99 tests. No selected implementation or verification remains unfinished. M2/I3 and the overall plan remain IN_PROGRESS. No new blocker, specification deviation or out-of-scope defect; G1/G2 and later milestone prerequisites remain unchanged. All 31 initial files outside the four intentional overlap files retain their initial hashes.
- **Concrete next action:** fresh-read the repository and latest checkpoint, then audit associated-Type concrete definitions containing invalid nested generic Types in Binding.AssociatedTypes.cs and Binding.ContractMatching.cs against §§8.1.3/8.4. Establish positive, negative, dependent and rebind/reload reproducers before choosing a fix. This audit is unstarted and is not a confirmed defect. General Origin/annotation/body proofs remain M4/M7 work; do not infer their completion from these units.

#### Completed timed execution (started 2026-09-16T01:14:34Z; soft deadline 02:14:34Z)

Fresh repository/plan/instruction inspection and initial snapshot completed; all previous checkpoint hashes matched. Prior completed work is preserved. Final checkpoint `2026-09-16T02:11:39Z`, elapsed **57m05s**. Stop at the verified boundary; the next audit remains unstarted. Final Debug/Release DLL hashes match their successful suite records (`final-artifact-comparison.json`); `final-source-hashes.json`, `final.diff`, `checkpoint-comparison.json` and `verification-index.json` identify the final workspace/evidence.

- **I3-U1 DONE:** unknown selected Attributes invalidate their target declaration during indexing, before inheritance and conformance checks. Attribute-chain traversal is shared with Layout; marker syntax and existing unresolved diagnostics remain available. No marker registry or Mod API; recognized LibraryImport remains its separate unimplemented boundary.
- **Verification:** `TestResults/timed-execution-20260916-011434/unit1-unknown-attributes/`: baseline build `20260916T0116041518058Z` PASS; baseline focused `20260916T0116501139461Z` FAIL all four stale certificates. Final Debug/Release builds `20260916T0118172062912Z` / `20260916T0119372013505Z` PASS, zero warnings/errors; focused `20260916T0118584083334Z` PASS 13; full Debug/Release `20260916T0119130669034Z` / `20260916T0120381898578Z` PASS 5,819 each. Provisional syntax remains readable, errors are not published provisionally, excluded markers remain excluded, removal restores conformance, warm Binding measures zero allocations. Native NOT_RUN for declaration validation only; NativeAOT excluded.
- **Boundary/next:** approximately six minutes elapsed; no blocker or specification change. Next inspect certificates owned by declarations nested in an invalid enclosing Container; reproduce before changing propagation. Broader M2/I3 remains IN_PROGRESS.


- **I3-U2 DONE:** conformance and Property/accessor verification walk existing parent links to check enclosing declaration validity. Invalid nested Types/Contracts and Properties cannot certify; unrelated sibling definitions and independent body checks continue. No retained context table or per-check allocation.
- **Verification:** `unit2-declaration-context/` under this execution root: baseline build `20260916T0122270628905Z` PASS; baseline `20260916T0123219688787Z` FAIL all three stale nested certificates. Final Debug/Release builds `20260916T0124395551472Z` / `20260916T0126059326625Z` PASS, zero warnings/errors; focused `20260916T0125202238080Z` PASS 9; full Debug/Release `20260916T0125421411420Z` / `20260916T0126391304107Z` PASS 5,828 each. Covers nested contracts, Property/accessor invalidation, sibling validity, independent body diagnostics, reload/removal recovery and zero measured warm allocations. Native NOT_RUN for certificate-only changes; no NativeAOT.
- **Boundary/next:** about twelve minutes elapsed; no blocker. Next verify enum construction plans whose owner Type is nested inside an invalid Container, reusing the same context check if a reproducer confirms the gap.
- **I3-U3 DONE:** final enum acquisition completion rejects plans whose owner Type has an invalid enclosing declaration, using the context guard from I3-U2. Invalid plans are unavailable through TryGetEnumConstruction; unrelated valid plans remain queryable for independent analysis.
- **Verification:** `unit3-enum-context/`: corrected baseline build `20260916T0128532930146Z` PASS; focused baseline `20260916T0129261504982Z` FAIL all three retained plans. Initial fixtures incorrectly searched source-local functions among root members; moved consumers into explicit groups without changing expected behavior. Final Debug/Release builds `20260916T0130337397276Z` / `20260916T0132193455775Z` PASS, zero warnings/errors; focused `20260916T0131128998896Z` PASS 10; full Debug/Release `20260916T0131497767497Z` / `20260916T0132494240253Z` PASS 5,838 each. Covers nullary/payload/contextual/constructed enums, early Property initializers, reload, same-plan recovery, sibling isolation and zero measured warm allocations. Native NOT_RUN for rejected declaration context; no NativeAOT.
- **Boundary/next:** about nineteen minutes elapsed; no blocker. Continue the M2/I3 explicit generic argument formation audit from §8.1.3, comparing ordinary Type, original Semantics-pair and fixed-length slots before selecting a demonstrated gap.
- **I3-U4 DONE:** constraint atom validation uses the shared enclosing-declaration guard for both recursively nested Type components and required Contracts. Invalid declaration context produces Error before identity, capability or exact-assumption success; no new cache or allocations.
- **Verification:** `unit4-constraint-context/`: baseline build `20260916T0137180750668Z` PASS; baseline focused `20260916T0137446003108Z` FAIL all three (Proven instead of Error). Final Debug/Release builds `20260916T0138441605990Z` / `20260916T0139560122615Z` PASS, zero warnings/errors; focused `20260916T0139230835111Z` PASS 11; full Debug/Release `20260916T0139365951323Z` / `20260916T0140280619477Z` PASS 5,849 each. Nested generic/Tuple/pointer arguments, invalid Contract assumptions/negation/disjunction, removal recovery, reload, valid siblings and warm zero allocations covered. Native NOT_RUN for proof rejection; NativeAOT excluded.
- **Boundary/next:** about 26 minutes elapsed; no blocker. Explicit length-call candidates remain Pending under existing M7 support boundaries. Next inspect late constructed-Type constraint failures against Property certificates and conformance witnesses; reproduce before changing ordering or propagation.
- **I3-G1 DONE:** Property/accessor and function-witness verification checks constructed-Type input constraints recursively within complete signatures using the existing proof engine, before certificate publication. Global pass ordering is preserved. Invalid generic uses cannot certify even though their BoundType remains readable for recovery.
- **Verification:** `unit5-generic-certificates/`: isolated baseline `20260916T0143586354333Z` reproduced stale direct/nested Property and public function-witness certificates. The fourth fixture exposed missing-getter recovery instead; its initial `has set` form is malformed under §11.4. Corrected the witness fixture to mandatory get/set and retained separate malformed-source regressions. Binding now marks absent mandatory getters invalid without inventing an accessor or indexing a null syntax node. Final Debug/Release builds `20260916T0149276900609Z` / `20260916T0150534970016Z` PASS, zero warnings/errors; focused `20260916T0149556987228Z` PASS 13; full Debug/Release `20260916T0150132213734Z` / `20260916T0151422859891Z` PASS 5,862 each. Positive controls, nested Tuple/array/pointer/function Types, input/result signatures, dependent evidence, reload, same-certificate recovery and warm zero allocations covered.
- **Verification deviations:** initial test compilation needed the Kimi namespace and the parser Diagnostic.Entry.Name API. An accidentally dispatched no-build run `20260916T0148455315480Z` used the previous four-test binaries after the latter build failure; its minimum-count failure is retained but is not final verification evidence. Subsequent tests used the successful current build. Native NOT_RUN for rejection/certificate checks; no NativeAOT or specification change.
- **Boundary/next:** about 37 minutes elapsed; no blocker. Next audit constructed enum plan Types with nested generic arguments whose own input constraints fail; reproduce before extending the existing final acquisition validation.
- **I3-G2 DONE:** final enum plan completion checks nested constructed-Type constraints in the result and payload Types before completing acquisitions. Reuses I3-G1 proof traversal; invalid or unproved plans cannot be queried as committed constructions.
- **Verification:** `unit6-enum-generic-constraints/`: baseline build `20260916T0152573629079Z` PASS; baseline focused `20260916T0153270880002Z` FAIL all three retained invalid plans. Final Debug/Release builds `20260916T0154502986257Z` / `20260916T0156080089673Z` PASS, zero warnings/errors; focused `20260916T0155302144653Z` PASS 11; full Debug/Release `20260916T0155491901379Z` / `20260916T0156434704430Z` PASS 5,873 each. Explicit/contextual/nullary/payload/nested arguments, positive dependent evidence, reload, reusable plan recovery and zero measured warm allocations covered. Native NOT_RUN for validation-only changes; no NativeAOT.
- **Boundary/next:** about 42 minutes elapsed; no blocker. Return to the selected Attribute audit: the wrong-target Test branch fails only the marker and returns without traversing preceding markers. Reproduce declaration certificate and chained-marker behavior before selecting a fix; product-excluded Test functions remain excluded.
- **I3-U5 DONE:** wrong-target Test markers invalidate the target before certification. Both index and expression Binding traverse prior Attribute-chain entries independently, including when the current built-in marker is already completed or invalid. Valid product-excluded Test functions retain their separate path.
- **Verification:** `unit7-test-attribute/`: baseline build `20260916T0157570034530Z` PASS; baseline focused `20260916T0158287401334Z` FAIL four certificate/marker checks. The initial fix exposed the expression Binding early-return suppression; fixed that path rather than changing diagnostic assertions. Final Debug/Release builds `20260916T0201211331963Z` / `20260916T0202575583038Z` PASS, zero warnings/errors; focused `20260916T0202190386127Z` PASS 10; full Debug/Release `20260916T0202367691195Z` / `20260916T0203447111219Z` PASS 5,883 each. Both marker orders, Layout interaction, nested certificates, reload/removal and product exclusion covered. Native NOT_RUN for declaration validation; NativeAOT excluded.
- **Boundary/next:** about 49 minutes elapsed. Next narrow audit: LibraryImport on non-function targets under §22.3.1, without implementing foreign functions or changing M13 dependencies.
- **I3-U6 DONE:** selected LibraryImport markers on non-function targets invalidate declarations before certification under §22.3.1. Function-target handling retains the existing unsupported foreign-function boundary; no ABI or M13 implementation was added.
- **Verification:** `unit8-libraryimport-target/`: baseline build `20260916T0205043069863Z` PASS; baseline focused `20260916T0205426342397Z` FAIL all three stale certificates. Final Debug/Release builds `20260916T0206501543827Z` / `20260916T0208533991443Z` PASS, zero warnings/errors; focused `20260916T0208115493649Z` PASS 8; full Debug/Release `20260916T0208309003708Z` / `20260916T0209392730329Z` PASS 5,891 each. Struct/Contract/Property/group/enum targets, both Layout-chain orders, reload/removal recovery and existing foreign-function rejection covered. Native NOT_RUN for declaration rejection only; NativeAOT excluded.
- **Final scope:** eight coherent units DONE, adding 85 tests to the initial 5,806-test suite. All final tests passed with zero failed/skipped. No incomplete implementation remains inside these selected units. M2/I3 remains IN_PROGRESS; other milestones and G1/G2 remain unchanged. No new external blocker or out-of-scope product finding.
- **Preservation/review:** reviewed production/test diffs; initial-file hash comparison shows only Binding.Attributes.cs and Binding.cs were intentionally extended among the prior C# edits; every other pre-existing C# hash matches the execution start. PLAN/STATUS advanced. No commits, draft edits, specification changes or NativeAOT. Final diff/hash/checkpoint evidence is under this execution root.
- **Next action:** fresh-read the repository and this checkpoint, then audit explicit generic function argument validity in Binding.Calls.cs against §8.1.3 and the BoundCall contract. Establish whether invalid nested argument formation can leave a publishable call selection, preserving valid inference/dependent evidence and existing length/specialization Pending boundaries. Do not treat this proposed audit as a confirmed defect.

#### Completed timed execution (started 2026-09-16T00:14:52Z; soft stop 01:14:52Z)

- **I3-A1 DONE, boundary check 00:21:21Z (6m29s elapsed):** reused retained projection uses for ordinary/conditional associated specifications; each applicable path checks the qualifier, selected Contract and defining requirement against the Type/Contract intersection. Ancestor paths retain their own wider domains; block specifications remain confined to their root path. Invalid clauses/paths report Access and cannot certify. No new retained collection or per-use allocation.
- **I3-A1 verification:** evidence root `TestResults/timed-execution-20260916/unit1-conformance-access/`. Baseline build `20260916T0016258006506Z` PASS; baseline `20260916T0017123261070Z` FAIL both reproducers because invalid Binding completed. Final Debug build `20260916T0018357616546Z` and Release build `20260916T0019596874099Z` PASS, zero warnings/errors; focused `20260916T0019139582607Z` PASS 22; full Debug `20260916T0019417980426Z` / Release `20260916T0020386433757Z` PASS 5,712 each, zero failed/skipped. Bounded harness records exact arguments and DLL hashes. Reload/rebind, narrow domains, conditional isolation, ancestor checks, nested Types, no-invalid-certificate and zero measured warm allocations covered.
- **Documentation/deviation:** STATUS updated; §§9.3.2/9.3.4 already specify the behavior, so SPEC unchanged. Prior parser/test changes preserved and reverified. No blocker or new out-of-scope finding. LLVM/native execution NOT_RUN for declaration rejection only; NativeAOT excluded.
- **Next unit:** inspect inferred Property/storage API exposure and distinguish the inferred complete Type from initializer-only projection syntax; retain source-local/body privacy. Establish concrete reproducers before selecting a fix or closing that audit. Time remains, so continue only after recording this completed unit.


- **I3-A2 DONE, boundary check 00:28:31Z (13m39s elapsed):** inferred Property access already checks normalized complete Types recursively while preserving private initializer implementation details (§9.3.2/9.3.3). Added 13 tests for projected conversion/private helper results, inferred nested Tuple/fixed-array Types, narrower domains, rebind/reload, initializer replacement and zero measured warm Binding allocations. No production change was needed; this closes the selected inferred-provenance audit without imposing a new initializer access restriction.
- **I3-A2 verification:** evidence root `TestResults/timed-execution-20260916/unit2-inferred-access/`. Final Debug build `20260916T0026135426619Z`, Release build `20260916T0027372499872Z` PASS (zero warnings/errors); focused `20260916T0026526619174Z` PASS 13; full Debug `20260916T0027165336927Z` and Release `20260916T0028227684128Z` PASS 5,725 each, zero failed/skipped. Earlier fixture warnings were corrected by declaration ordering. Unannotated literals construct dynamic Array (§4.3), so fixed-array coverage now uses typed helper results; dynamic Array remains M11 work.
- **I3-A2 finding / next unit:** metadata-preserving `KotoHelper.Replace` overwrites the source metadata read by `NumberLiteralKoto.SourceSpelling`, leaving it inconsistent with cached numeric payload. Replacing `Hidden.A` with parsed numeric `1` reproduces InvalidLiteral after rebind; a Boolean replacement independently confirms Property invalidation works. Numeric relocation is recorded as I3-N1, not silently discarded. Next implement exact literal spelling preservation during metadata relocation and verify fitting/writing/rebind/emission; no general Mod public API is added. Native checks were not required for I3-A2's test-only access audit.

- **I3-N1 DONE, boundary check 00:37:10Z (22m18s elapsed):** `NumberLiteralKoto` now has an optional preserved-spelling slot. `KotoHelper.Replace` materializes that spelling once before changing source metadata; ordinary parsed literals continue to borrow immutable source text, and unchanged metadata does not allocate. Exact fitting and normalized writing use consistent digits while diagnostics retain the replaced location. No new Mod interface or syntax serialization promise.
- **I3-N1 verification:** evidence root `TestResults/timed-execution-20260916/unit3-numeric-replacement/`. Baseline build `20260916T0030217293376Z` PASS; baseline `20260916T0030536934713Z` FAIL all three reproducers. Final Debug build `20260916T0034041546650Z`, Release build `20260916T0036063187762Z` PASS (zero warnings/errors); focused `20260916T0034439733870Z` PASS 15; full Debug `20260916T0035442768458Z` / Release `20260916T0037013686651Z` PASS 5,740 each, zero failed/skipped. Native run `20260916T0035058036837Z` PASS five fixtures, LLVM verification and ten ordinary O0/O2 executions with exact expected output. Final generated files match recorded native input hashes. Covers decimal/hex/wide integers, lazy float parsing, exact float conversion rounding, invalid fits, relocated diagnostics, repeated replacement, writing/rebinding and zero measured warm allocations.
- **I3-N1 deviations/next:** test fixtures were corrected to use explicit integer-to-float conversion and the expression-body API; no numeric fitting rule changed. The I3-A2 numeric replacement finding is resolved. STATUS updated; specification already requires consistent numeric values and source diagnostics. Next audit enum payload projection provenance under §9.3.2; retained checks currently cover functions, Properties and constraints, so establish a minimal unused-enum reproducer before choosing the next change. No NativeAOT.

- **I3-E1 DONE, boundary check 00:47:36Z (32m44s elapsed):** enum payload projections retain qualifier/defining-requirement domains against the effective enum domain before capabilities/conformance/construction certification. A scan of retained uses also covers payloads bound early by inferred headers. Invalid payloads invalidate their case and enum; existing construction completion rejects earlier plans. No new retained table or per-use allocation.
- **I3-E1 verification:** `TestResults/timed-execution-20260916/unit4-enum-projection/`: baseline build PASS and focused `20260916T0042028370017Z` FAIL both intended false-success reproducers. Final Debug build `20260916T0045099819938Z`, Release build `20260916T0046164037644Z` PASS, zero warnings/errors; focused `20260916T0045428315126Z` PASS 19; full Debug `20260916T0045578588290Z` and Release `20260916T0046586487818Z` PASS 5,759 each. Covers nested/dependent Types, effective domains, invalid construction/conformance, reload/replacement and zero measured warm Binding allocations. Initial test traversal/formatting and generic constraint syntax errors were corrected without changing acceptance criteria. LLVM/native NOT_RUN for declaration rejection only; no NativeAOT. STATUS updated; normative text already correct.
- **Next unit:** audit direct-base projection access against the derived Type domain (§9.3.2), establish normalized-base reproducers, then implement and verify only the demonstrated gap. Broader M2/I3 remains IN_PROGRESS.
- **I3-B1 DONE, boundary check 00:53:19Z (38m27s elapsed):** direct-base Types are bound before a single retained-projection scan checks qualifier/requirement access against the effective derived domain. The existing recursive inheritance validation then propagates invalid bases through descendants before certification. This avoids scanning every projection separately for every base and adds no retained table or per-use allocation.
- **I3-B1 verification:** `TestResults/timed-execution-20260916/unit5-base-projection/`: baseline build `20260916T0048039522373Z` PASS; baseline focused `20260916T0048469450802Z` FAIL both incorrect-acceptance reproducers. Final Debug build `20260916T0050295921232Z`, Release build `20260916T0052015676738Z` PASS, zero warnings/errors; focused `20260916T0051204088120Z` PASS 17; full Debug `20260916T0051336483611Z` and Release `20260916T0052390258021Z` PASS 5,776 each. Covers short/qualified and nested projections, constructed qualifier arguments, effective domains, descendant invalidation in both source orders, conformance rejection, reload/replacement and zero measured warm Binding allocations. No blocker or weakened expectation. Native NOT_RUN for declaration rejection; no NativeAOT.
- **Alias inspection:** §18.1 permits Container imports only, not source Type aliases. Alias lookup returns the original declaration Symbol, so there is no independent normalized-Type alias record to change. Existing module/arity suites cover this lookup. No source alias extension is authorized. Next focused unit I3-A3 checks protected API domain inclusion: AccessCovers currently handles private/module domains but appears to omit protected-domain subset relationships.
- **I3-A3 DONE, boundary check 00:59:57Z (45m05s elapsed):** added 14 cases to ModuleBindingTest for unused dependency function/Property/enum/constraint signatures, explicit/default import projections, original Symbol domains and module reload. The existing whole-graph Binding behavior and prior projection fixes satisfy these cases; no production change.
- **I3-A3 verification:** `TestResults/timed-execution-20260916/unit6-module-access/`: final Debug build `20260916T0057293617346Z`, Release build `20260916T0058528135482Z` PASS, zero warnings/errors; focused `20260916T0058075495307Z` PASS 53; full Debug `20260916T0058232696132Z` / Release `20260916T0059237018163Z` PASS 5,790 each. Narrow-domain fixtures explicitly use internal Contracts; default private nested Contracts correctly failed because their lexical domains are narrower. Native NOT_RUN for test-only semantic coverage; no NativeAOT.
- **I3-A3 scope correction:** the initial protected nested-Type hypothesis was invalid: §§6.1.1/9.3 forbid nested Containers in structs. The parser correctly rejected all three exploratory fixtures (`unit6-protected-access/20260916T0055013273271Z`); they were removed, and no protected-domain change or false bug claim was made. §18.1 also excludes source Type aliases. The completed audit is the concrete module coverage above, not general access completion.
- **Next unit:** audit whether invalid selected Layout declarations can retain verified conformance definitions; reproduce before changing validation order or owner invalidation. Keep wider Attribute/foreign-function/Mod work separate.
- **I3-L2 DONE, boundary check 01:06:02Z (51m10s elapsed):** malformed/wrong-target Layout attributes invalidate their target, and duplicate/conflicting/split-storage Layout declarations invalidate the merged struct before inheritance and conformance validation. Invalid bases propagate to descendants. Existing source-specific Layout diagnostics remain; owner InvalidTypeFormation prevents certificates. No nominal layout/ABI support added.
- **I3-L2 verification:** `TestResults/timed-execution-20260916/unit7-layout-certificates/`: baseline build `20260916T0100294293951Z` PASS; baseline focused `20260916T0101053727524Z` FAIL three incorrectly retained certificates. Final Debug build `20260916T0102563487079Z`, Release build `20260916T0104502814643Z` PASS, zero warnings/errors; focused `20260916T0103446062308Z` PASS 10; full Debug `20260916T0104003765566Z` / Release `20260916T0105268612245Z` PASS 5,800 each. Reload/rebind, wrong function/Property/Contract targets, descendants in either source order, and restored certification after removing a conflict are covered. Existing warm Layout allocation checks pass in the full suites. Native NOT_RUN for validation/certificates only; no NativeAOT.
- **Next unit:** inspect late named-function API access validation relative to final conformance certification, establish a restricted-projection implementation reproducer, and move the existing check only if the stale-certificate gap is demonstrated.
- **I3-F2 DONE, final verification/review boundary 01:13:09Z (58m17s elapsed):** moved the existing API exposure check immediately before final conformance certification, after body/Property/Origin validation. Invalid named-function signatures can no longer leave a final verified witness. No new scan, retained state, acceptance rule or runtime representation.
- **I3-F2 verification:** `TestResults/timed-execution-20260916/unit8-function-certificates/`: baseline build `20260916T0106374094311Z` PASS; baseline focused `20260916T0107360721367Z` FAIL both stale-certificate reproducers. Final Debug build `20260916T0109490968256Z`, Release build `20260916T0111000812410Z` PASS, zero warnings/errors; focused `20260916T0110217486245Z` PASS 6; full Debug `20260916T0110382840011Z` / Release `20260916T0111315058608Z` PASS 5,806 each, zero failed/skipped. Invalid parameter/result projections, positive effective domains, reload, replacement invalidation/recovery and zero measured warm Binding allocations covered. Initial recovery test attempted a read-only modifier setter; corrected to the existing Koto replacement API. Native NOT_RUN for certificate ordering only; no NativeAOT.
- **Final verification scope:** all eight units are covered by the final managed builds/suites. Numeric native evidence remains exact: final 25 fixture/expectation files match the recorded native inputs. `preserved-source-comparison.json` confirms the four pre-existing changed C# files are byte-for-byte preserved. Final source hashes/diff and unit evidence remain under `TestResults/timed-execution-20260916/`. No draft/specification edits, commits, new dependencies, or NativeAOT runs. SPEC already states the relevant rules; STATUS reflects the delivered support.
- **Stop checkpoint:** 2026-09-16T01:14:07Z; elapsed 59m15s. Final diff review and documentation synchronization complete; no incomplete selected unit, unexplained test failure or active verification process. Stop near the soft limit without beginning another unit.
- **Remaining/next:** broader M2/I3 and the compiler plan remain IN_PROGRESS; no selected implementation is left unverified. Next investigate selected unknown Attribute owner/witness invalidation with fresh reproducers, then continue independent static/access/formation work. G1/G2 external interfaces remain blocked as before; they did not block these units. No new out-of-scope finding or required user decision.
#### Previous execution: Tuple requirements and constructed-qualifier projections (I3-R1/R2 DONE)

- **Selected boundary/state:** exactly the two parser items identified by the preceding checkpoint. Reread HEAD `b0096b5`, instructions, current plan, local diffs, §§3.2, 8.3/8.4.3/8.7 and 9.3.2, and parser/Binding/test infrastructure. Preserved preceding uncommitted corrections in PLAN/STATUS/ConstraintProjectionApiAccessBindingTest. Broader M2/I3 remains IN_PROGRESS; no next item was started.
- **Baseline:** initial diff/source hashes are in `TestResults/requirement-type-parsing/`. Baseline build `20260916T0007230682213Z` PASS with zero warnings/errors. Focused baseline `20260916T0007562041922Z` FAIL all eight valid-source reproducers with parse diagnostics: Unit/one-element/nested Tuples, constructed/nested qualifiers, and Boolean combinations. An earlier test-only build failure from a missing namespace import was corrected before the baseline; its record remains available.
- **Implementation/authority:** §3.2 defines Unit/Tuple punctuation and named constructed Types; §§8.3/8.7 retain Boolean requirement precedence and atomic Type identity, and §§8.4.3/9.3.2 govern projection resolution/access. `IsTupleRequirement` uses allocation-free token lookahead to distinguish Unit or a comma at the current parenthesis level from Boolean grouping; nested arrays and generic arguments own their commas. It delegates Type construction to existing `ParseDeclarationType`. Requirement name parsing now continues through generic arguments and member suffixes instead of stopping at the first constructed Type. Existing syntax classes, source spans, parent adoption, writer, Binding and serialization are reused; no new runtime representation or Binding exception.
- **Verification:** final build/focused/full commands and run IDs are above. Debug/Release builds PASS, zero warnings/errors; 39 focused tests PASS; full Debug/Release PASS 5,690 each, zero failed/skipped. Covers nested/root-qualified forms, Boolean precedence, Tuple arity/spans, canonical writing/reload/parent links, positive and negative substituted identity proofs, qualifier/Contract access after normalization, rebind/reload, eight malformed-source cases with emission rejection, and zero measured warm Binding allocations. The existing 41 constraint-access cases also pass in both full suites. No measured parser throughput claim.
- **Deviations/documentation:** the previous two parse-error expectations deliberately recorded missing support. They now require successful parsing and Binding; malformed spellings and inaccessible APIs retain explicit negative tests. This implements the owning rules rather than relaxing acceptance criteria. STATUS reflects resolved syntax limits; SPEC already states the required behavior and needed no change. No blocker or new out-of-scope finding. LLVM/native execution NOT_RUN: generic/constrained functions still fail the existing emission support gate. No NativeAOT or draft edits.
- **Remaining/concrete next action:** stop at I3-R1/R2. Next execution: reread current state and audit conformance-specific associated specifications under §9.3.4's Type/Contract intersection domain. Inspect `BindAssociatedSpecification`, conditional associated specifications and retained projection access checks; establish public/internal/private qualifier and defining-requirement reproducers for ordinary and conditional specifications before selecting that small coherent group. Inferred projection provenance and broader access/formation work remain open; do not start them here.

#### Previous execution: declaration-constraint projection API exposure (I3-C1/C2 DONE)

- **Boundary:** only named member-function/Contract function-requirement constraints and struct/enum/Contract declaration constraints in currently parsed forms. Broader M2/I3 remains IN_PROGRESS. Self conformances and implementation associated specifications retain their separate Type/Contract intersection domains.
- **Baseline/state reconciliation:** started at `f762c3c` with prior function/Property work uncommitted. Initial diffs/hashes are in `TestResults/projection-constraint-access/`. Baseline Debug build `20260915T2346355346354Z` PASS; baseline focused run `20260915T2347187476434Z` FAIL all four invalid-public-exposure reproducers because Binding incorrectly completed. An external commit advanced HEAD to `b0096b5` during execution; reread that commit and preserved its implementation. Final corrections affect test diagnostics and documentation.
- **Implementation/clauses:** §§8.4.3, 9.3.2/9.3.4 require exposure checks on projection qualifiers, defining requirements and concrete bindings. `ValidateConstraintProjectionAccess` reuses retained projection identities and existing recursive domain checks; it associates each use with its owning constraint declaration and rejects the clause and owner before Contract/conformance certification. Called at the end of `BindConstraints`, after associated preparation. No new retained tables or per-use allocations. Existing semantic Type/constraint checks still enforce concrete bindings.
- **Verification:** final build/focused/full run IDs and commands are recorded above. Debug/Release builds PASS with zero warnings/errors; focused PASS 41; full Debug/Release PASS 5,651 each, zero failed/skipped. Coverage includes short/qualified and compound requirements, nested arrays/Type arguments, dependent subjects, effective internal/private/public domains, function requirements, associated/refinement constraints, invalid conformance certification, local/body/specification controls, reload/rebind/replacement, and zero measured warm Binding allocations. Prior function/Property sources remain unchanged and their tests passed in both full suites.
- **Test corrections/deviations:** two exploratory nested spellings did not reach the access check. The initial 37-case run passed 35 and failed these two expectations. Replaced those access-check fixtures with accepted nested spellings while retaining both parser-boundary cases; no intended access check was removed. The first boundary assertions read diagnostics from synthesized Containers, whose contexts inherit the parent context; they were incorrect. Isolated parsing proves errors, and final tests additionally verify emission rejection. Shared Binding test diagnostics now use the actual `Hello.kimi` collection. Intermediate failed runs and the one transient test-variable build error remain in the evidence directory; final checks supersede them. No production parser changes or specification changes were made, and no language requirement was relaxed.
- **Out-of-scope finding / remaining work:** `T is (i32, [1 of S.C.Element])` and `T is Box<i32>.C.Element` currently produce parse errors. `ParseTypeConstraint.ParsePrimary` treats leading parentheses as Boolean requirement grouping and returns immediately after generic arguments, without consuming a following projection suffix. These forms therefore do not exercise projection API access. Audit their permitted grammar under §§8.3/8.4.3 and complete valid forms separately, preserving Boolean precedence and parser error recovery. This is a parser coverage limitation, not an access-check regression. Inferred projection provenance, conformance-specific projection exposure and broader access audits also remain unfinished. No blocker remains for I3-C1/C2's selected Binding work.
- **Concrete next action:** reread current state, then select the two related parser items: Tuple-Type requirements versus Boolean grouping, and associated projections following constructed qualifiers in requirement expressions. Establish isolated parse/write/reload and Binding-access reproducers before implementing; retain malformed-source rejection tests and run focused plus full Debug/Release verification. Do not begin those items during this execution. No draft edits or NativeAOT; LLVM/native checks NOT_RUN because this group only changes declaration rejection.

#### Previous execution: Property/accessor projection API exposure (I3-P1/P2 DONE)

- **Boundary:** two cohesive I3 subitems only: explicit Property header projections and explicit accessor receiver/input/result projections. Both depend on retained projection uses and must be checked before Property/conformance verification. Declaration-constraint and inferred projection provenance remain outside this execution.
- **Baseline:** HEAD `f762c3c30d611c441012ef7c2c48fe26f96e2bff` with preceding function-projection edits uncommitted. Reread and preserved those changes; original function test source hash is unchanged. Initial diff/hashes are retained in `TestResults/projection-property-access/`. Baseline Debug build `20260915T2338023290028Z` PASS, zero warnings/errors; focused baseline `20260915T2338341736660Z` FAIL all four assertions because invalid stored/computed headers and additional getter/setter signatures incorrectly completed Binding.
- **Implementation/clauses:** §§9.3.2, 9.3.4 and 11.2 distinguish the Property header domain from additional accessor components and inherit the Contract domain for requirements. `PropertySignatureOwner` walks existing syntax parents, excluding initializers and implementation bodies. `ValidateProperties` checks recorded projections before computing `IsVerified`; conformance witnesses cannot publish invalid Property signatures. `ProjectionAccessCovers` shares the existing qualifier/selected-Contract/defining-requirement checks with function signatures. Existing normalized complete-Type checks retain concrete binding enforcement. No new retained storage or per-use allocation.
- **Verification:** Debug build `20260915T2340458071365Z` PASS, zero warnings/errors; focused `20260915T2341255975549Z` PASS 37; full Debug `20260915T2341388819076Z` PASS 5,610, zero failed/skipped. Release build `20260915T2341552324382Z` PASS with zero warnings/errors; full Release `20260915T2342547212319Z` PASS 5,610, zero failed/skipped. Exact compiler/test DLL hashes were recorded and rechecked by the bounded harness. Tests cover restricted headers, nested/constructed qualifiers, requirement domains, accessor receiver/input/result domains, implementation-only uses, invalid witness rejection, reload/rebind/replacement, and zero measured warm Binding allocations.
- **Documentation/deviations/blockers:** no specification change required: the owning rules already require these checks. No blocker, scope expansion or new out-of-scope finding. Existing function-projection work is preserved and reverified by the full suite. V5–V7 LLVM/native execution NOT_RUN: only declaration rejection changes; no runtime path/representation is enabled. No NativeAOT; no draft edits.
- **Remaining/next action:** this execution stops with I3-P1/P2 verified and DONE. Following execution: audit declaration-constraint projection exposure via `ConstraintAccessCovers`, retained projection uses and declaration domains; reproduce subject/requirement normalization cases before selecting its next small cohesive scope. Inferred projection provenance and broader access audits remain open.
#### Previous execution: named function projection API exposure (M2/I3 subrange DONE)

- **Selected boundary:** only named function parameters/results, including nested Type syntax and Contract function requirements. Property/accessor and declaration-constraint projection provenance remain for the next group.
- **Baseline:** clean HEAD `f762c3c30d611c441012ef7c2c48fe26f96e2bff`; prior groups are committed. Initial no-restore build failed because assets selected Arc.Unit 0.47.0 while the committed project requires 0.48.0. Restore `20260915T2324091239424Z` refreshed declared dependencies without source/package-version changes. Debug baseline build `20260915T2324290233038Z` passed, zero warnings/errors.
- **Reproduction:** initial fixtures incorrectly used source-local functions (§6.1.1), so their failures did not prove exported API exposure. Corrected to explicit named public Containers. Unchanged production build `20260915T2329518335235Z` PASS; focused baseline `20260915T2330362862119Z` FAIL: 15 invalid-exposure/rebind assertions failed, 13 controls passed. Two controls now assert the earlier conformance/refinement Access diagnostics required by §§9.3.2/9.3.4. A new source-local positive preserves that boundary. Restoring the implementation preserved an older timestamp; run `20260915T2331535711756Z` reused the baseline compiler SHA-256 and is not implementation evidence. The source timestamp was refreshed before rebuilding.
- **Implementation:** `ValidateApiAccess` reuses `projectionUses` and parent links to identify parameter/result exposure. Checks the original qualifier Type, selected Contract and defining associated requirement using the function or requirement Contract domain. Existing normalized-Type checks still own concrete-binding exposure. No new retained objects or per-use allocation.
- **Verification:** final Debug build `20260915T2332112352107Z` and Release build `20260915T2333221184218Z` PASS with zero warnings/errors. Focused Debug `20260915T2332526722452Z` PASS 29. Full Debug `20260915T2333059353467Z` and Release `20260915T2334229656971Z` PASS 5,573 each, zero failed/skipped. Includes reload/rebinding, signature replacement and zero measured warm Binding allocations. V5–V7 LLVM/native verification is NOT_RUN: no runtime behavior/representation or generation acceptance is added. NativeAOT remains excluded.
- **Documentation/deviations/blockers:** STATUS now states the implemented function-signature range and remaining projection boundaries. SPEC/owning §9.3.2 already specifies the behavior, so no specification edit is needed. Restored the committed dependency versions, corrected source-local fixtures per §6.1.1, and corrected conformance/refinement diagnostic sites; no requirements were relaxed. No blocker remains for this selected group. No new out-of-scope product defect was established.
- **Remaining/next action:** this execution stops at the verified function-signature group. Next execution: resume M2/I3 Property/accessor projection exposure under §9.3.2; inspect `ValidateProperties`, accessor domains and `projectionUses`, then reproduce normalized header/input/result projections using explicit Containers and select only that coherent group. Declaration-constraint/inferred projection provenance remains a later audit; do not treat this as whole-family completion.

**Previous M2/I3 accessor receiver checkpoint (2026-09-16):** DONE only for accessor receiver formation and API-access validation. M2/I3 and broader access audits remain IN_PROGRESS; execution stops at this group.

- **Clause/implementation mapping:** §§7.3 and 11.2/11.4 require instance accessor receivers to obey ordinary Self receiver contracts; static accessors have none. §9.3.2 includes receiver Types in API exposure. `IsReceiverType` shares the existing ordinary-function predicate with `ValidateAccessor`, rejecting unrelated targets, extra reference layers, raw pointers and unconstrained generic Types. Stored accessors retain their narrower ref/uniq rules. Accessor receiver Types now participate in the existing recursive domain check. The declaring Container, not a conditional-conformance environment, supplies receiver identity. No new Type representation or retained state was added.
- **Reproduction/verification:** baseline build `20260915T2204226943286Z` PASS. Baseline focused `20260915T2204448275696Z` failed all five assertions: four invalid declarations incorrectly completed Binding; the nested-reference case failed earlier for a missing nested Origin and was corrected to use `from static`, retaining its receiver-rejection assertion and requiring a bound receiver Type. Initial implementation full Debug `20260915T2221087228368Z` found two conditional-member regressions (5,542/5,544 passed); replacing the conditional environment with the member's declaring owner fixed their cause. Final Debug build `20260915T2221442727545Z` and Release build `20260915T2222286926272Z` PASS, zero warnings/errors. Focused Debug `20260915T2222104993739Z` PASS 30; full Debug `20260915T2222136123741Z` and Release `20260915T2222566679925Z` PASS 5,544 each, zero failed/skipped. Exact arguments, bounded execution and compiler/test DLL hashes are retained/rechecked by the harness. Source hashes and initial/final diffs are retained in `TestResults/accessor-receivers/`.
- **Acceptance coverage:** unused computed/Contract getter and setter errors; unrelated public/private, primitive, generic, pointer and fully formed nested-reference receivers; stored/computed static rejection; owning/ref/uniq and supported object Self forms; generic Self, explicit Origins, shorthand, Contract and receiverless static controls; accessor-input access domains; `IsVerified` rejection; rebind/reload and static-accessor replacement in both directions; zero measured warm Binding allocations. Existing conditional-conformance, Property, ordinary-function receiver and constructor suites pass in both full configurations.
- **Decisions/deviations/blockers:** the receiver-access audit uncovered missing receiver formation as a prerequisite; merely checking visibility would still accept public unrelated receivers. Reused the ordinary receiver predicate and existing diagnostics rather than introducing a new receiver model. Inspection confirmed function receivers already participate in parameter API checks and constructors have no declared receiver. No blocker or specification change; `SPEC.md` already requires this behavior, and `STATUS.md` records the verified subset. Earlier uncommitted changes are preserved. No new out-of-scope finding.
- **Remaining/boundary:** associated-Type projection exposure and broader access/fragment/module audits remain open. V5–V7 LLVM/native execution was NOT_RUN because only invalid declarations are newly rejected and no runtime path or representation was enabled. NativeAOT was not run. Do not begin another group or milestone in this execution.
- **Concrete next action:** resume M2/I3 §9.3.2 associated-Type projection API exposure. Read the owning projection/domain rules, inspect `TypeAccessCovers`, projection Binding and existing Contract/API tests, and reproduce qualifier/defining-requirement/concrete-binding exposure cases before selecting one coherent group.

**Previous M2/I3 Property header API-access checkpoint (2026-09-16):** DONE only for Property header/storage Type accessibility independently of restricted accessors. M2/I3 and the broader access audit remain IN_PROGRESS. Execution stops at this group.

- **Clause/implementation mapping:** §9.3.2 assigns the header/storage Type to the Property domain and additional signature components to each accessor domain; §9.3.1 makes requirements inherit their Contract domain. `ValidateProperties` now checks the fully bound Property Type before validating accessors and computing `IsVerified`. Standard accessors reuse this stronger check instead of repeating it in narrower domains. Existing `TypeAccessCovers` supplies recursive complete-Type and enclosing-domain checks. No new representation, allocation or retained state was introduced.
- **Reproduction/verification:** baseline build `20260915T2156365298501Z` PASS; baseline focused run `20260915T2157008805326Z` failed all four reproducers because public stored, computed and inferred-Type Properties with restricted accessors incorrectly completed Binding. Final Debug build `20260915T2158583783905Z` and Release build `20260915T2159462589180Z` PASS with zero warnings/errors. Focused Debug `20260915T2159204223146Z` PASS 28; full Debug `20260915T2159336595257Z` and Release `20260915T2200140986792Z` PASS 5,514 each, zero failed/skipped. An intermediate xUnit analyzer warning was corrected before final verification. Exact arguments and compiler/test DLL hashes are retained and rechecked by the harness. Source hashes and initial/final diffs are retained in `TestResults/property-api-access/`.
- **Acceptance coverage:** restricted get/both accessors; stored/computed/inferred headers; nested Tuple/array/constructed/Function Types; internal/protected/private and enclosing domains; symbolic generic Types; opaque private implementation storage; Contract requirement headers; recursive enum payload controls; invalid Property verification state; rebind/reload and replacement in both directions; zero measured warm Binding allocations. Existing PropertyBinding accessor-input-domain controls pass in both full suites.
- **Decisions/deviations/blockers:** the initial enum/Property/accessor audit was narrowed to the reproduced missing Property-domain check. Inspection confirmed enum payload validation already uses the enum domain in `PrepareStorage`, and custom accessor inputs/results already use their own domain in `ValidateAccessor`; those paths were retained. No blocker, specification change or new out-of-scope finding. `SPEC.md` already requires this behavior; `STATUS.md` describes the verified subset. Prior uncommitted work is preserved.
- **Remaining/boundary:** receiver-signature and broader access/fragment/module audits remain open. V5–V7 LLVM/native execution was NOT_RUN because this group only rejects invalid declaration APIs and introduces no runtime path or representation. NativeAOT was not run. No next group or milestone began.
- **Concrete next action:** resume M2/I3 §9.3.2 receiver-signature accessibility. Read the owning function/constructor/accessor receiver rules; inspect `ValidateApiAccess`, `ValidateAccessor`, receiver binding and existing domain tests; establish minimal accepted/rejected receiver exposure cases, then select one coherent group. Do not begin that work in this execution.

**Previous M2/I3 grouped Type-argument checkpoint (2026-09-16):** DONE only for disambiguating grouped Type arguments containing fixed arrays; M2/I3 and the broader argument-validity audit remain IN_PROGRESS. Execution stops at this group.

- **Clause/implementation mapping:** §8.1.3 permits complete nested Types as generic arguments; §9.6 resolves explicit Type arguments at the use site. `Parser.Syntax.cs` previously treated a numeric token anywhere inside parentheses as evidence of a length expression. `IsLengthExpression` now stops at an opening bracket, which starts fixed-array Type syntax and is not part of the supported length-expression grammar. Existing Type parsing and Binding perform the remaining validation. The change adds no allocation, retained state, Type representation or inference rule.
- **Reproduction/verification:** baseline `20260915T2147101583501Z` failed three of four cases: grouped arrays and a Tuple starting with an array became error/length syntax; an ungrouped array passed. Final Debug build `20260915T2149125904046Z` and Release build `20260915T2149583261205Z` PASS with zero warnings/errors. Focused Debug `20260915T2149377427030Z` PASS 25; full Debug `20260915T2149469509462Z` and Release `20260915T2150317208255Z` PASS 5,486 each, zero failed/skipped. Compiler/test DLL hashes are recorded and rechecked. A test diagnostic-name typo and comment-spacing warning in an intermediate build were corrected before final verification. Final diff/whitespace review PASS; source hashes, initial/final diffs and previous untracked-file integrity checks are recorded in `TestResults/type-argument-formation/`.
- **Acceptance coverage:** direct/repeated grouping; zero/nested/arithmetic-length arrays; pointer, Tuple, nominal and common Function Type nesting; complete-Type identity shared by constructed Types and explicit function arguments; explicit borrow Origins; non-Type groups/Contracts/numeric arguments and incomplete nested generic Types; source serialization/rebind/canonical writing; unchanged numeric length-expression syntax; zero measured warm Binding allocations. Length-expression controls verify parsing only and do not claim general explicit length-call support.
- **Decisions/blockers:** no blocker or specification change. The broad §8.1.3 audit was narrowed to this demonstrated parser defect. `SPEC.md` already requires the behavior; `STATUS.md` records the implemented subset. Broader generic argument validity, constraints, inference and full annotation/role proofs remain open under their assigned dependencies. Prior uncommitted work is preserved. No out-of-scope finding was established.
- **Remaining/boundary:** V5–V7 LLVM/native execution was NOT_RUN because the corrected grouping resolves to existing Types and changes no representation/runtime operation. NativeAOT was not run. Do not continue into another implementation group here.
- **Concrete next action:** resume M2/I3's declaration API-access audit for enum payloads and Property/accessor Types under §9.3.2. Read the owning domain rules, inspect `ValidateApiAccess` and existing ApiAccessBinding/Property/Enum cases, reproduce any uncovered exposure cases, then select one coherent group. Do not start that audit in this execution.

**Previous M2/I3 pair annotation checkpoint (2026-09-16):** DONE only for annotation representation and certificate safety. Full annotation support and M2/I3 remain IN_PROGRESS. No next group or milestone began.

- **Clause/implementation mapping:** §8.1.2 original-pair reconstruction preserves WholeType identity and replaces only the explicit outer Origin; Type formation still requires its own proof. `Binding.Types.cs` no longer treats a transparent wrapper's forwarded Semantics name as another application. `Binding.ConstraintValidation.cs` restricts the existing owner/unsafe application certificate to actual SemanticsApplication Types with no outer Origin. An annotated original WholeType remains a Parameter with a separate unresolved annotation obligation. This uses existing interned Types and adds no retained state.
- **Reproduction/verification:** baseline `20260915T2137442152733Z` failed four of five cases: two grouped forms incorrectly became SemanticsApplication, and two invalid owner/unsafe-constrained annotations were accepted. Final Debug build `20260915T2140026185388Z` and Release build `20260915T2140511402807Z` PASS with zero warnings/errors. Focused Debug `20260915T2140269765261Z` PASS 24; full Debug `20260915T2140376504451Z` and Release `20260915T2141229963219Z` PASS 5,461 each, zero failed/skipped. Compiler/test DLL identities are recorded and rechecked by the harness. Final diff/whitespace review PASS; initial/final diffs, SDK, source hashes and prior-untracked-file integrity checks are retained in `TestResults/pair-annotations/`.
- **Acceptance coverage:** ungrouped/grouped/repeated/owner-wrapped annotation identity, nested pointer/array Types, separate Origin annotations without mutation of other WholeType uses, required failure for insufficient owner/unsafe proof, ordinary invalid-name/mapping diagnostics, positive unannotated original/arbitrary-application controls, and serialization/rebind/canonical writing. Valid ref-constrained annotation cases deliberately remain incomplete with a retained definition obligation; these tests verify the unsupported boundary, not language-feature completion.
- **Decisions/deviations:** the selected safety fix also excludes annotated arbitrary applications from the same origin-free certificate, because its proof was equally insufficient there. No general borrow proof or substitution/Loan implementation was attempted. No specification change: `SPEC.md` and §8.1.2 already state the required behavior. `STATUS.md` separates delivered safety checks from unfinished support. Prior uncommitted files are preserved.
- **Remaining/blockers:** no blocker for this selected group. Completing annotation support needs a declared borrow-role proof, substitution of the new Origin without requiring an old-to-new relation merely for formation, inner dependency checks, and separate value fitting/Loan obligations. Those M4/I7–I8/M7 prerequisites remain open. V5–V7 LLVM/native checks were NOT_RUN because no new runtime path is enabled; NativeAOT was not run. Existing allocation regressions pass, but no new annotated-path performance claim is made. No out-of-scope findings.
- **Concrete next action:** resume the independent M2/I3 §8.1.3 generic Type-argument validity audit. Inspect `BindConstructedType`, explicit function Type arguments and existing constraint/type tests; establish reproducers for non-Type arguments and incomplete nested Types, then select one coherent implementation group. Do not start that audit in this execution.

**Previous M2/I3 grouped Semantics-pair checkpoint (2026-09-16):** DONE for the selected grouping-preserving original-pair reconstruction group; M2/I3 remain IN_PROGRESS. No next group or milestone began.

- **Clause/implementation mapping:** §§8.1.1–8.1.2 normalize grouping and identify original `s/T` by declared bindings. `Binding.Types.cs` now recognizes parenthesized, unannotated SemanticsTarget syntax before imposing a standalone complete-Type role. Every grouping node retains the projection's bound identity/type; the original pair returns the existing WholeType. Other targets retain their application obligation. Targets carrying an Origin annotation follow ordinary Type validation instead of being silently treated as an unannotated projection. No new semantic object graph, cache, or runtime representation was introduced.
- **Reproduction/verification:** baseline `20260915T1700139892836Z` failed two of three cases: `s/(T)` and `s/((T))` incorrectly produced `UnprovenConstraint`, while `s/T` passed. Final Debug build `20260915T1702209854921Z` and Release build `20260915T1703199528465Z` PASS with zero warnings/errors. Focused Debug `20260915T1702431694822Z` PASS 23. Full Debug `20260915T1703029481531Z` and Release `20260915T1703481189435Z` PASS 5,437 each, zero failed/skipped; per-run compiler/test DLL hashes retained and rechecked. An initial nullable warning in the new test helper and a local-name collision during the fix were corrected before final builds. Final diff/whitespace review PASS; source hashes and the final diff are recorded in `TestResults/pair-grouping/`.
- **Acceptance coverage:** direct/multiple grouping, complete nested owner/ref/unsafe/array/Tuple/generic Types, preserved call-result Origin identity, separate target binders despite equal constraints, duplicate-signature equivalence, invalid inner Origin annotations, standalone target/unrelated application proof requirements, syntax replacement, serialization/rebind/canonical writing, and zero measured warm Binding allocations. A focused check confirms generic generation remains unavailable; this group does not certify ownership or native generic execution.
- **Deviations/blockers:** no blocker or specification change. The broad Type-formation audit was narrowed to this demonstrated grouping defect as authorized. The annotation guard is necessary to prevent the grouping fix from erasing inner annotations. Earlier uncommitted changes are preserved. No out-of-scope defect was established.
- **Remaining/boundary:** general SemanticsTarget role/application proofs, explicit Origin annotations on original pairs, dependent object targets and universal generic verification remain unfinished. V5–V7 LLVM/native execution was NOT_RUN because this introduces no new representation or execution path; NativeAOT was not run. `SPEC.md` already states the required behavior and remains unchanged; `STATUS.md` describes the delivered range.
- **Concrete next action:** resume M2/I3 by comparing explicit original-pair Origin forms `s/T from a` and `(s/T) from a` against §8.1.2. Inspect transparent wrappers, `CompleteOrigins`, `ProveTypeRole` and substitution; establish minimal positive/negative reproducers and identify M4 proof dependencies before selecting one coherent group. Do not start that audit in this execution.

**Previous M2/I3 Origin fragment audit checkpoint (2026-09-16):** DONE for the selected current-grammar audit, with 20 added regression cases and no compiler behavior changes. M2/I3 remain IN_PROGRESS. Execution stops at this boundary.

- **Clause mapping/evidence:** §6.1.1 permits nested Containers only in groups, and §6.1's groups have no Origins. §15.3 restricts bound targets to `static` or visible abstract Origins and binds the entire list first; type parameters/imported Types occupy a different namespace. Thus a valid split struct's bound resolves to its own ordered Origin slots or `static`. Matching §6.1.2 count/order/names plus target spelling already identifies the same binder/slot in this grammar. `OriginNameList.SameParameters` and `Binding.BindSchemas` were inspected against that rule. No source-local alias can supply a different abstract Origin binder. This is a scoped sufficiency argument, not a spelling-based identity rule for general Origin expressions.
- **Tests:** `OriginFragmentBindingTest` checks no-bound/static/backward/forward/reflexive/cyclic bounds, matching sources in both orders with different aliases and a same-spelled generic Type parameter, exact bound object identity, different-arity binder separation, mismatched names/order/count/bounds, duplicate/unknown targets, adding an incompatible source, serialization/rebinding/canonical writing, and zero measured warm allocations for unbounded fragment Binding. Bound declarations explicitly remain incomplete with `Unsupported`; unknown targets and duplicate declarations retain their specific failures. Passing these tests does not establish bound proof support.
- **Verification:** final Debug build `20260915T1653069921318Z` and Release build `20260915T1654050324070Z` PASS, zero warnings/errors. Focused Debug `20260915T1653379481962Z` PASS 20. Full Debug `20260915T1653527504869Z` and Release `20260915T1654386127636Z` PASS 5,414 each, zero failures/skips; compiler/test DLL hashes retained and rechecked. Two initial test-only build errors (`20260915T1652065177770Z`: internal enum in a public test signature; `20260915T1652375453150Z`: missing test namespace import) were corrected before these successful runs. No behavioral reproducer failed and no compiler fix was required. Final tracked diff and new-test whitespace review PASS; the final diff, source hashes, SDK and working-tree status are recorded in `TestResults/origin-fragment-audit/`.
- **Deviation:** the previous next-action premise about same-spelled source-local Origin binders was disproved for the current grammar. Retaining another fragment-header graph or adding alias resolution for bounds would introduce unnecessary state or incorrect semantics. `SPEC.md` and its owning chapters are already correct and remain unchanged; `STATUS.md` now separates verified header identity from unfinished bound proofs.
- **Remaining/blockers:** no blocker for this audit. General declaration/use-site bound proofs, normalization under established outlives facts and proof-dependent artifact invalidation remain M4/I7 and later artifact work. Generated-fragment/finalization contracts retain M15 dependencies. LLVM/native execution (V5–V7) was NOT_RUN because this group changes only tests/docs; NativeAOT was not run. No next milestone began.
- **Concrete next action:** resume M2/I3's complete nested Semantics/SemanticsTarget formation audit. Read §§8.1–8.2 and the owning Semantics rules, inspect `Binding.Types.cs` and existing TypeBinding/constraint cases, and establish a minimal reproducer before selecting the next coherent implementation group. Do not start that audit in this execution.

**Previous M2/I3 Type arity checkpoint (2026-09-16):** The selected group implements §6.1.2 distinct same-name Type arities and §9.6 explicit/zero-arity selection in the committed lookup stage. Parser name buckets retain a chain of distinct arities, and Binding reuses symbol chains with a stack-local candidate summary. Matching fragments merge only within the selected arity; writing/replacement retain every sibling. Qualified/rooted Types, explicit/default imports, module references, generic member/base lookup and source/reload/rebind paths are covered. The prior uncommitted Layout group is preserved. The group is DONE with final Debug/Release verification; M2/I3 remain IN_PROGRESS overall. Execution stopped at this boundary.

- **Verification:** baseline `20260915T1634008962215Z` reproduced eight failures out of nine cases. Full Debug `20260915T1639451675949Z` exposed legacy parser-helper compatibility plus two obsolete test fixtures. Fixed the helper without changing its by-name contract; final focused `20260915T1643575478773Z` PASS 28 and full Debug `20260915T1644104323883Z` PASS 5,394. Final Debug build `20260915T1643222117552Z` has zero warnings/errors. Release build `20260915T1645180389231Z` PASS with zero warnings/errors; final Release full `20260915T1646004102416Z` PASS 5,394 with zero failed/skipped tests. Final diff/whitespace review PASS; prior untracked Layout files match their initial SHA-256 values.
- **Deviations/decisions:** the rooted qualified-Type path left an intermediate member syntax node unverified; marking that successfully selected path resolved was necessary for rooted arity uses. `ParserRegressionTest.DiagnosesAndIgnoresTypeConstraintsFromLaterStructDefinitions` and `ConditionalConformanceBindingTest.DirectAndUnconditionalDeclarationsAreDuplicatesAfterFragmentMerging` incorrectly used a later zero-arity declaration as a fragment of a generic Type. Their later headers now repeat the original generic arity, as §6.1.2 requires; duplicate-constraint/conformance assertions remain intact. Positive member fixtures use public methods so access checks reach the intended arity test. No specification rule changed.
- **Remaining/boundary:** generic argument-kind/inference extensions, resolved Origin-bound fragment equivalence and broader I3 static/access checks remain open. Nominal/generic native generation remains gated; no runtime operation or new physical representation is introduced. Native/LLVM execution was not selected for this static group; NativeAOT was not run. No other milestone began.
- **Historical next action (superseded by the Origin audit above):** audit §6.1.2 Origin-bound fragment equivalence. The proposed same-spelled source-local-binder reproducer was subsequently shown inapplicable to the current grammar; see the current checkpoint for evidence and the next executable action.
- **Blockers:** none for this group; G1/G2 remain separate. **New out-of-scope findings:** none.

**Previous M2/I3 selected Layout checkpoint (2026-09-16; historical):** The selected group implements §§6.5/21.1.2 argument/target checks, retained fragment Attributes, explicit mode agreement and the C single-source-storage-fragment constraint. Fragment ordinals are parser-local integers on Attributes/Properties, not Type identities or physical order. The parser saves declaration context before generic/base parsing; literal Attribute arguments do not undergo ordinary lookup. Canonical writing collapses only valid matching Layout modes across different fragments. No new nominal generation path is admitted. This selected group is DONE; M2/I3 remain IN_PROGRESS overall. Execution stopped at the selected boundary after final verification.

- **Evidence so far:** baseline `20260915T1615597967039Z` reproduced five failures (four rejected positives and one accepted invalid later-fragment Attribute); `20260915T1624506723226Z` isolated the generic-header Attribute attachment bug. The subsequent 39-case focused run `20260915T1625515225665Z` and full Debug `20260915T1626056404141Z` passed 5,357 tests, before the final chain-linking simplification and three added regression cases. Final verification below covers those later changes.
- **Final evidence:** Debug/Release builds `20260915T1626556557375Z` / `20260915T1628343214985Z` PASS without warnings/errors; focused `20260915T1628133043503Z` PASS 42; final full Debug/Release `20260915T1628264583031Z` / `20260915T1629022464444Z` PASS 5,360 each with zero failures/skips and recorded DLL hashes. Warm Layout Binding allocates zero measured bytes; no throughput improvement is claimed. Final diff/whitespace review PASS. Verification records and exact commands are in the table above.
- **Boundaries/deviations:** C representation formation (§21.1.3: open/derived/empty/zero-sized/alignment cases), generated-fragment provenance and post-Mod finalization, and physical/instantiated layout remain M5/M8/M15 work. They are not completed by selected source Attribute validation. Existing unknown-Attribute diagnostics remain unchanged. Correct specification text needs no edit; STATUS.md records the narrower implementation range. No native or NativeAOT execution was selected: nominal emission remains gated and no runtime operation was added.
- **Concrete next action after this boundary:** resume M2/I3's §6.1.2 differing-generic-arity audit: reproduce `Box` versus `Box<T>` with the current name-keyed container table, inspect type lookup/fragment identity, then select a bounded implementation group and its tests. Remaining resolved Origin-bound/header/static/access/inference audits and I4 representations stay open; do not start them in this execution.
- **Blockers:** none for this group. G1/G2 remain unrelated interface blockers. **Out-of-scope findings:** none newly established.

**Previous I31/I3 checkpoint (2026-09-16; historical):** Ordinary product Binding excludes inline Test functions from symbols, startup, control flow, ownership and generation while retaining syntax-level eligibility/target diagnostics, including nested invalid Test targets. Test bodies do not resolve names or require test dependencies; unknown selected markers still fail final validation. Test discovery, full test compilation and public runner integration remain unfinished. Focused ProductTestMembershipTest/ModuleBindingTest pass 68 cases (`20260915T1554367178017Z`); warm product reanalysis measures zero allocations. Seven fresh product fixtures pass 14 O0/O2 native executions (`20260915T1555157315318Z`), including skipped abort/nonterminating bodies. Debug (`20260915T1556097646252Z`) and Release (`20260915T1600219293282Z`) pass all 5,318 tests; latest builds have zero warnings/errors. Release regeneration matches all 1,365 files from the six retained native runs. No NativeAOT. Its next action was I3 selected Layout Attribute and fragment validation. Correct normative text is unchanged; no nominal storage or multi-module emission guard was removed.
**Historical verification:** Debug `20260915T1536475309915Z` and Release `20260915T1537379150918Z` PASS all 5,282 tests. Release artifact comparison matches all 1,330 previously native-verified files. Bare-flag parser tests pass 7 cases; actual `check --locked` succeeds without LLVM (`20260915T1531059360840Z`) and rejects an unused dependency error at the correct source (`20260915T1531403673167Z`, expected exit 1), leaving the lock unchanged. Failed restore records both partitions unresolved (`20260915T1531425340537Z`, expected exit 1). The checked-in two-project example restores/checks successfully (`20260915T1536055317314Z`, `20260915T1536076149053Z`); its initial incorrect four-space Tinyhand indentation was corrected to the existing two-space format and protected by actual-file loading tests. No NativeAOT or new multi-module native execution is claimed. Next: I31 product filtering of inline Test functions, needed to complete M3 product membership, then M4 effect/ownership foundations. Persistent content/processing/semantic records remain M14 work.

**Historical M3 checkpoint (2026-09-16):** Project checks now load the exact resolver source bytes, bind each module in its own definition environment, and check all dependency bodies and Library startup restrictions. Explicit/default aliases retain separate precedence, aggregate ordinary function candidates without changing symbols, and do not expose transitive names. Generation still rejects multiple modules. `20260915T1526239463936Z` passes all 5,272 Debug tests; Debug/Release solution builds have zero warnings/errors. ModuleBindingTest has 32 cases, including access, settings, defaults, overloads, reparse/rebind and zero measured warm analysis allocations. Resolver has 22 cases; product-only resolution skips test dependencies. Release regression is running. CLI `20260915T1527221557916Z` exposed the bare `--locked` parser mismatch before semantic execution; fix it and rerun CLI positives/negatives. Then finish the M3 record/conformance audit and continue independent in-scope milestones. G1/G2 are unchanged.

**Historical checkpoints below:** pending statements describe their recorded intermediate state; the current checkpoint and item table supersede them.

I6 lock implementation is IN_PROGRESS: deterministic schemaVersion 1 JSON records separate path-free partitions; required-partition validation rejects missing nonempty, stale, unresolved, corrupt, duplicate-key and unreadable locks. Restore updates use exclusive file ownership, prior-byte comparison, same-directory atomic replacement, cancellation cleanup and no-op preservation. `20260915T1457413389306Z` passes 20 focused lock tests. The restore command is registered and product checks now validate existing locks even with empty dependency declarations; these latest integrations need verification. Nonempty graph semantic integration is still pending and remains gated. Resolver cycle adjustment passes 21 cases in `20260915T1453242842475Z`.

I5 resolver implementation is IN_PROGRESS: exact Project identities, target/language compatibility, iterative graph traversal, equivalent relocated product bytes/settings, direct-name edges, separate product/test partitions and attempt-shared configuration/source bytes. `20260915T1451545631787Z` passes 21 focused cases, including 128-node depth, diamonds, repeated reference names, distinct versions, same-release conflicts, relocated child revalidation, test-only failure/nonpropagation and source snapshots. Cycle diagnostics were then extended to identify the actual closed cycle; that latest adjustment needs verification. Resolver results are not yet connected to compilation or lock lifecycle, so the unresolved-graph guard is still required. Package resolution remains I27/M14 work.

Configuration/check regression complete: Debug `20260915T1442462413845Z` and Release `20260915T1444195641715Z` PASS 5,194 tests each; solution builds `20260915T1442154792999Z` and `20260915T1443529503970Z` have zero warnings/errors. Next is I5's exact Project graph resolution with separate product/test partitions, shared immutable input snapshots, conflict/cycle evidence and explicit Package boundary, followed by I6 lock lifecycle. The current unresolved-graph guard remains until graph semantics are integrated. Existing locks are not yet validated by check; that missing I6 obligation remains open.

M3 configuration/check checkpoint: `20260915T1434185533548Z` reproduced eleven false successes; `20260915T1440548349074Z` reproduced product reading a test-only file and accepting duplicate resolved TestSources paths. `20260915T1441547710948Z` passes all 52 DependencyConfiguration/SolutionInput cases, including exact spelling, duplicate decoded names, roundtrip, legacy migration, unresolved graph rejection, test-only reference separation, cancellation and zero measured warm configuration-validation allocations. CLI check accepts a valid source with a nonexistent toolchain (`20260915T1441583906311Z`, exit 0), rejects an unresolved call (`20260915T1442185890026Z`, expected exit 1), creates no artifacts and does not execute source. Full regression is in progress. Public parameterless Check APIs remain available; cancellation overloads were added and affected tests now pass their test cancellation token. No graph, lock, test runner, package loading or cross-module semantic support is claimed yet.

Array argument checkpoint: `20260915T1424404667008Z` reproduced eight rejected positives. `20260915T1428003270707Z` passes 31 focused cases, including candidate-order independence, fitting/range/shape ambiguity, nested arrays/Tuples, established inner expressions, named evaluation order, cleanup, reload and zero warm Binding allocations. A test helper that printed then returned the same owned string was corrected to return an integer, preserving the intended evaluation-order test under existing Move rules. Native `20260915T1428467250715Z` passes 21 modules / 42 O0/O2 executions. Fresh Debug/Release solution builds and full suites (`20260915T1429377072446Z`, `20260915T1430369177055Z`) PASS 5,156 each. The Release artifact comparison matches all 1,330 native input files. An earlier native invocation failed during input inventory because of a placeholder test-source path; it executed no native verification and is not PASS evidence.

Fragment checkpoint: §6.1.2 header kind/access/modifier matching, closed contracts, single defining base, and synthesized path-group access are implemented. `20260915T1417240489100Z` reproduced ten failures; `20260915T1420031482614Z` caught one additional intermediate path-kind conflict. `20260915T1420436256044Z` passes all 29 focused cases, including two-file base-definition aliases, reload, added conflicting sources and zero measured warm Binding allocations. Release solution build `20260915T1421050317828Z` has zero warnings/errors; full Release `20260915T1421245756977Z` passes 5,125. Final Debug regression is running. No new runtime representation is admitted. Different-arity declarations, full resolved Origin-bound fragment equivalence and storage/layout fragment rules remain open in their assigned milestones.

**Historical previous execution source:** HEAD remained `e47b667107249e05b7ff0f2bce4b8ada4b9d7491`, with this effort uncommitted. Repository state, prompt and plan were reread on resumption. Preserve unrelated `prompts/`; no draft or NativeAOT work.

- **Verified slices:** I2; I4 numeric conversions and owned primitive/Tuple/fixed-array Identity Acquisition, `@owner` and explicit owner numeric targets. The identity uses existing Copy/Move operations and a lazy reused validation list without an extra runtime transfer.
- **Verified length slice:** I3 §4.2 uses normal name/member Binding and a two-pass typed evaluator. UInt128 bits retain full 128-bit intermediates; arithmetic checks the established signed/unsigned Type, then the final length checks nonnegative target isize. Symbolic expressions keep the existing interned representation and expand constant references to values. No getter execution, static initialization, generic specialization, or extra ordinary/index constant folding is implied.
- **Verified API slice:** I3 §9.3.2 checks ordinary function APIs in Declaration Containers (including root main) and generic struct/enum constraints after Binding. Source-local functions retain their lexical domain. Reuses existing recursive Type/access-domain predicates; excludes Self conformance clauses from declaration-domain checks because their Type/Contract intersection has separate validation. Named function result omission remains Unit (§10.5); structs do not allow nested Type declarations (§6.1.1). Test fixtures were corrected to respect both syntax/scope boundaries.
- **Verified static defaults:** default expressions bind in their function's declaration environment with only preceding parameters visible. A nesting counter limits syntax ancestry checks to active default Binding, keeping ordinary parameter lookup on its existing fast path. The checked-in parameter representation is a FunctionParameterKoto record, so slot filtering follows the default expression's parent and the existing parameter list rather than introducing syntax nodes/scopes. Runtime default/pending-slot ownership remains M4 work and its Unsupported gate is preserved.
- **Verified local array inference:** explicit fixed-array shapes ending in `_` acquire element evidence from initializer elements or independently typed arrays. Direct literals fit the unique established Type; ordinary expressions bind independently. Existing array Binding and ownership enforce shape and acquisition. No syntax mutation or new runtime representation is introduced.
- **Remaining:** I1/G5 and I3's remaining access/static/inference audit; I4 other representations/Borrow/Reborrow; I5–I35 except recorded partial I35 work. M3–M17 remain unfinished. G1/G2 block only their external interfaces.
- **Verified fragments:** header kind/access/modifier matching, closed contracts, single defining base, and synthesized group accessibility. Debug full `20260915T1422019000579Z` and Release full `20260915T1421245756977Z` PASS 5,125 tests each with fresh solution builds. Different generic arities, resolved Origin-bound equivalence and storage/layout fragment rules remain explicit work.
- **Verified array arguments:** candidate-local fitting to fixed parameter shapes preserves independent element Types and numeric ambiguity. Only the selected candidate binds the aggregate shape; Copy/Move and cleanup use existing call operations. Explicit or other-argument Type evidence works; general length/element generic inference and dynamic collections retain later milestones.
- **Historical next action (superseded by the completed Project subranges above):** M3/I5–I6 configuration/check foundations could proceed with the completed identity/access subranges. Existing Project.Check performs semantics without emission; Solution.Check can currently succeed with no loaded projects, and legacy/nonempty dependency settings are not validated. Reproduce and close these false-success paths while adding the configuration graph representation. M2 remaining generic/Attribute/ownership-dependent audits remain open rather than being declared complete.

### Active clause inventory (I1 / G5)

| Clauses / requirement / tests | Inspected current range and work |
| --- | --- |
| §§8.1.3, 8.2, 8.4.8, 8.7, 9.3.2/9.3.4, 11.2, 18.1; R2/R4/R5/R8/R18; T3/T4/T5/T16/V4/V8/V11 | I3-G24/G25/G26/G27 and I3-A8/A9/A10 DONE for normalized projected call constraints, conditional premise validity and API domains, associated-projection subjects, computed Property certificates and explicit/default-alias module integration. Added 134 cases; final Debug/Release PASS 6,418 each, including reload/recovery and focused zero-allocation checks. Exact baselines and final evidence are in the current timed checkpoint; broader M2/M4/M7 work remains open. I3-V1 later re-verified this range against the source and SPEC, added §8.2 Contract constraint-subject rejection with 12 cases, and re-ran Debug/Release at 6,430 each. |
| §§3.2, 8.3/8.4.3/8.7, 9.3.2; R1/R2/R3/R5; T1/T3/T4/V4/V8/V11 | I3-R1/R2 Unit/Tuple requirements and associated projections after constructed qualifiers are parsed with preserved Boolean precedence and existing Type/projection nodes. RequirementTypeParsingTest PASS 39; full Debug/Release PASS 5,690 each, including prior constraint access tests. I3-A1/A2 later completed the conformance-specific and inferred Property exposure subranges; see the timed execution evidence. |
| §§9.3.2/9.3.4, 11.2; R2/R4/R8; T3/T4/V4/V8/V11 | I3-P1/P2 explicit Property header and accessor receiver/input/result projection exposure checked before IsVerified/witness publication. PropertyProjectionApiAccessBindingTest PASS 37; full Debug/Release PASS 5,610 each, with reload/replacement and zero measured warm allocations. Body/initializer use stays separate; declaration-constraint exposure was subsequently completed by I3-C1/C2 and syntax coverage by I3-R1/R2. Inferred projection provenance remains open. |
| §§6.1.1, 8.4.3, 9.3.2/9.3.4; R2/R4; T3/T4/V4/V8/V11 | Named member-function/Contract requirement parameter-result associated projections retain qualifier and defining-requirement domains through concrete normalization. Existing concrete-binding and conformance checks remain. ProjectionApiAccessBindingTest: 29 focused cases; full Debug/Release 5,573 each PASS, including reload/replacement and zero measured warm allocations. Source-local/body scopes stay distinct; subsequent I3-P1/P2 and I3-C1/C2 checkpoints verify Property/accessor and declaration-constraint projection exposure. |
| §§6.1.2, 9.2, 9.6; R2; T3/T4/V4/V8/V11 | Distinct Type arities, matching-fragment collection and explicit/zero-arity selection in committed stages; TypeArityBindingTest and added ModuleBindingTest cases cover declarations/diagnostics, scope priority, alias stages, rooted and qualified names, member/base paths, reload/writing/replacement and zero measured warm allocations. Omitted-argument inference and broader generic proof work remain open. |
| §§6.1.2, 6.5, 21.1.2; A.14 Layout; R1/R2; T4/V4/V8/V11 | Selected source Layout argument/target checks, fragment mode agreement and C single storage fragment; LayoutAttributeBindingTest covers positives/negatives, excluded declarations, generic/base headers, source order, source-local diagnostics, source serialization/reload, canonical writing, mutation/rebinding and warm allocations. Nominal representation rules and generated storage finalization remain assigned to M5/M8/M15; final 42-case focused and 5,360-case Debug/Release verification PASS; warm Layout Binding measures zero allocated bytes. |
| §§2.6, 13.5.2/13.5.4; R3; T2/T6 | `Binding.Conversions` limits target guidance to direct signed/parenthesized literals; preserve this. Implement exact UInt128 magnitude fitting to float without default i32 or intermediate float rounding; retain representation limit and implicit-fitting boundaries. |
| §13.5.4, A.14 Instructions; R3/R23; T6 | Float narrowing must preserve NaN/infinities/signed zero and allow gradual underflow; finite overflow must Abort. Float-to-integer checks the truncated mathematical value, including negative fractions to unsigned, before any poison-producing LLVM conversion. Cover both float formats and every supported integer width. |
| §§21.5.3–4, 22.5.4; R3/R21; T6/T7 | Typed i128/u128↔float remains diagnosed before optimization, including unused code. Current integer conversions retain their Abort catalog code; add a distinct float-overflow reason. No new native helper or backend ABI required for <=64-bit conversions. |
| §§13.5.2–3, 15; R3/R6; T6/T11 | Current owned primitive/Tuple/fixed-array Identity Acquisition and owner targets verified. Other representations and Borrow/Reborrow remain I4/I8 dependencies; this does not close all of G5 or M2. |
| §4.2, A.3; R3/R13; T7/T8 | Constant-readable local/static stored initializers, typed checked arithmetic, symbolic expansion and rebinding verified. Final nonnegative isize differs from intermediate arithmetic Type. No generic instantiation or separate-compilation artifact claim. |
| §§6.1.1, 9.3.1–4, 10.5; R2/R4/R5; T3/T5 | Ordinary named member APIs and generic struct/enum constraint exposure now checked at declaration. Local functions are source-local, result omission is Unit, and Self conformances use intersection domains. Existing lookup/protected receiver and conformance checks remain separately audited paths. |
| §7.2, §10.5; R4; T5/T9 | Static default expressions use the declaration environment and preceding parameter slots; check all defaults independently of supplied arguments and do not infer generic slots from them. Runtime once-only ordering, prepared-slot ownership, borrows and default transfer boundaries remain M4 verification obligations; no runtime support claim. |

### Historical planning verification

| Verification ID | Target and configuration | Relevant code or artifact state | Result | Evidence | Reverification needed |
| --- | --- | --- | --- | --- | --- |
| V1 | Baseline/source architecture/specification/plan audit | `7a83d6bc751f4d02815c3f0b2f1ecfa113672588`; initially clean; new PLAN.md only | PASS | Current git result; referenced test classes checked against inventory with additions marked new; 24 R / 17 M / 35 I / 32 T / 12 V IDs; English-only prose and no trailing whitespace; `git diff --no-index --check -- NUL PLAN.md` exits 0. This is a planning audit, not complete clause-level compiler verification | Each plan/code baseline change and G5 family expansion |
| V2 | .NET environment | Current host | PASS | `dotnet --info`: SDK `10.0.401`, MSBuild `18.9.11+e34a38d2a`, runtime `10.0.12`, win-x64. `global.json` selects Microsoft.Testing.Platform but pins no SDK version | Before executable baseline or environment change |
| V2 | MTP base help and extension discovery | No compiler/test build requested; help evaluated projects | BLOCKED | Base options printed; extension discovery produced NuGet access diagnostic in G3. Outer command exit 0 despite reported build failure. No test ran; no managed conformance failure inferred | Resolve G3, verify exact project-specific filters without implicit restore |
| V2 | Installed backend/dlltool hashes | Current installed files versus `backend/windows-x64/profile.json` | PASS | Backend SHA-256 `4ef5b90f70bf22dea6fd2a6fb495f11025e3ecf4b7afa68b356801c9252c5d26`; dlltool `3733b6d47353a29f18b0e543675d83c525655a7cdfa3fb9c06cfc5d4a1a4fdfc`; both match | Before native verification and any tool/backend change |
| V2 | opt/llc/clang/lld-link banners | Current toolchain files | BLOCKED | Permission denied for each attempted process; no version result established | Resolve G4, inspect every required actual tool |
| V3 | Debug/Release builds | Current checkout; outputs not freshly built | NOT_RUN | Planning did not run a build; help's failed evaluation is not a build result | Required before tests |
| V4 | Managed tests / fixtures | Existing binaries/fixtures not trusted as current | NOT_RUN | Test existence inspected only | Required after V3/G3 |
| V5–V7 | LLVM, native fixtures and runtime/backend | Hash match alone is insufficient | NOT_RUN | Planning did not execute verification/native tests; G4 prevents them in this sandbox | Required after current fixture generation/G4 |
| V8 | Reload/rebind/cache regressions | Current source inspected | NOT_RUN | Existing tests read, not executed | Run affected families after each semantic/cache change |
| V9 | CLI/artifact validation | Existing scripts inspected | NOT_RUN | No project build/run/emit/pack/publish performed | After fresh compiler/toolchain prerequisites |
| V10 | Language test public runner | No integrated implementation; G1 open | BLOCKED | Specified language/runner contracts exceed current code; public profile undefined | After M16 prerequisites and G1 |
| V11 | Allocation/throughput/retention | Current measurements unavailable | NOT_RUN | Existing tests and Benchmark configuration inspected; STATUS performance claims are historical | Establish baseline before tuning; repeat after relevant changes |
| V12 | Full completion | All I items unimplemented in this effort | NOT_RUN | Planning only | After milestone closure |

Historical evidence: STATUS.md §§7.2–7.7 reports previous Debug/Release managed passes and O0/O2 native fixture successes, including recent element/parameter Move work; its receiver-shorthand note reports 390 related passes. These are **historical**, not added to current totals. Do not reuse its source baseline or assume unchanged outputs prove this checkout. NativeAOT remains excluded and was not run.

## 9. Risks and Implementation Notes

| Cause / trigger | Impact | Mitigation | Verification / affected milestone |
| --- | --- | --- | --- |
| Early semantic success used as generation certificate | Invalid unused/captured/generic body emitted or silently omitted | Keep final obligations, all-body verification and freshly checked physical plans; narrow gates only after complete slice | T4/T13/T15/T26/T31, V4/V8; all M |
| Candidate reuse leaks tentative mappings or usage state | Different overload/Origin result by enumeration order | Candidate-local scratch, commit winner once, separate legality after selection | T5/T16/V4/V8; M2/M4/M7 |
| Effects/cyclic conformance use unfinished summaries | Unsound static reentry/ObjectCompatible proof | Bounded component fixed points, retained causes and dependencies, never default missing summary to empty | T10/T16/T18/T25; M4/M7/M10/M14 |
| Place identity tied to physical slot or general constant folding | Wrong disjointness/Move permission, zero-size alias confusion | Semantic integer path IDs, literal-only indices, sparse tracking, distinct logical initialization | T8/T11/T19; M4/M5/M11 |
| Added temporary or incorrect cleanup edge | Changed evaluation count/order, premature destructor or result escape | Reuse snapshots/Write/Deliver/Join validation; audit normal/Abort/divergent paths and nested defer lifetimes | T9/T12/T13/T17/T30; M4–M12/M16 |
| Physical sort or bytes mistaken for logical responsibility | Wrong field/base/Case destruction, padding reads | Layout offset maps plus separate logical order and initialized components | T12/T13/T22; M5/M6/M8 |
| General borrow emission adds unproved LLVM attributes | Optimizer miscompilation despite valid IR | Prove call-wide ref/uniq rules, no element noalias without premises, conservative attributes otherwise | T11/T16/T22, O0/O2; M4/M8/M13 |
| Generic metadata/frame proliferation | Excessive code, time, memory or wrong frame capacity | Worklist key interning, exact substitution frames, bounded optional exploration and measured budgets | T15/T29/T32/V11; M8/M17 |
| Object atomic protocol mistaken for source memory model | Unsafe release/upgrade or accidental concurrency promise | Implement specified arc ordering; separate runtime ordering review from source concurrency boundary | T18/V7; M10 |
| Collection capacity/churn design uses scratch/per-entry allocations | Violates normative no-allocation/amortization guarantees | Preallocated reusable indices/free lists, geometric growth, operation counters, failed shrink leaves placement intact | T21/V11; M11 |
| Cache uses path/mtime/hash equality as semantic proof | Stale aliases/effects/absence facts/diagnostic displays | Fixed bytes, structural/dependency checks, current source maps, no-cache oracle, bounded reverse invalidation | T23–T25/T27/T29; M3/M14–M16 |
| ZIP/publication or native supply checks inspect only indexes | Corrupt package, inconsistent release or unresolved runtime dependency | Validate actual snapshots, entire closure and atomic mappings; inspect actual selected COFF members and undefined symbols | T24/T25/T31/V9; M13–M14 |
| Test output or descendant handles never terminate | Hung runner, lost failure or false success | Independent control budget/channel, launch-time process management, monotonic deadlines and bounded drain/recovery | T30/V10; M16; blocked interface details G1 |
| Old fixtures or overlapping configurations | False native success for stale source | Inventory/hash protocol; generate then consume serially; exact nonzero count | V3–V9; M1/M17 |
| Warm allocation measurements hide retained memory or cold cost | Performance improvement claimed while total cost regresses | Separate cold/warm, failed runs and source reload; collect peak/retention plus throughput distributions | T32/V11; M1/M17 |

Allocation strategy: prefer existing interned Types/Origins/identifiers, structs and integer indices, reusable lists/bit lanes/adjacency tables and pooled physical layouts. Clear transient references on success and failure. Use streaming hashes and immutable shared content for packages. Avoid arbitrary pooling of long-lived syntax across contexts, forced inlining/full specialization everywhere, duplicate object graphs, or new abstractions without a measured or correctness need.

## 10. Execution Order and Next Actions

| Step | Preconditions | Changes and rationale | Verification | Completion criteria | Failure or blocking conditions |
| --- | --- | --- | --- | --- | --- |
| 1 | Subsequent implementation phase authorized; re-read current git state/rules | Resume M1/I1–I2; compare this baseline to current checkout and preserve new user changes | V1–V3 | Current baseline and runnable focused-check procedure; evidence inventory | G3/G4 prohibit tests/native; record exact limits without weakening gates |
| 2 | M1 mapping for first slice | M2/I4 first bounded feature: remaining numeric conversions using existing conversion plan; I3 audit in focused sub-slices | T2/T6/T7, V4–V8 | Each admitted conversion has static/runtime/error evidence; no premature expansion of borrowing | Stop at unsupported FFI/helper contract or numeric ambiguity; investigate owning clause |
| 3 | Source identity/access baseline | M3/I5–I6 Project graph and check/restore; Package consumers initially reject until M14 | T23/T24, V4/V8/V9 | Exact Project DAG and deterministic lock handling without native dependency | Graph conflict/invalid config is expected negative, not fallback selection |
| 4 | Committed call/module identities | M4 then M5/M6: effects/Loans → storage/deinit/static → enum execution | T9–T14, V4–V8 | General ownership and representation invariants proved for each added range | Unfinished effects/Origin proofs block that use; keep selected operation fixed |
| 5 | Ordinary semantic contracts complete | M7 universal proofs/specialization → M8 typed shared generation/frames | T14–T16/T29, V4–V8/V11 | Verified generic baseline at budget zero, then bounded optimization | No favorable-instantiation workaround for body errors |
| 6 | M4–M8 foundations | M9 callables; M10 objects; M11 sequence/Array slices; M12/I23 Core mappings before M11 Dictionary; M12 interpolation, in independently closed slices | T17–T22/T32, V4–V8/V11 | Each new source API reaches execution and preserves existing paths | Gaps in later element kinds recorded without declaring whole family done |
| 7 | Module/ABI/runtime capabilities | M13 native closure/Library/FFI; M14 package/store/reuse | T22–T25/T31, V4–V9/V11 | Actual native and no-cache/cached artifact evidence, matched inputs | G4; corrupt/stale outputs must reject, never use old success |
| 8 | Generation and membership foundations | M15/I29 and M16/I31–I32 internal semantics; resume I30/I33 only with separate finalized contracts | T27–T30, V4/V8 then V10 | Independent semantics complete; external integration only after G1/G2 | Keep interface blockers; no default invented from elapsed time |
| 9 | All applicable milestone exits | M17/I34–I35 final coverage/performance/doc closure | V12 | §11 satisfied or exact incomplete/blocked subset reported | Missing in-scope evidence prevents complete-compiler claim |

**Concrete next actions:** (1) Fresh-read the checkout and latest checkpoint, then reproduce whether Binding.Prove recognizes equivalent dependent requirements written using concrete Types and associated-Type projections under SPEC 8.7. This is an unstarted audit, not a confirmed defect; preserve four-valued proof rules without adding general satisfiability or inference. (2) Continue remaining SemanticsTarget/static/access/Attribute audits in small verified units. General Origin/annotation/body proofs remain under M4/M7, and G1/G2 interfaces remain separate. All seven units in this execution are DONE; no later milestone began.

**Resumption protocol:** read §§2, 5, 7, 8 and the active milestone; compare HEAD and diff; validate last output identities/configuration before reuse. Re-run only invalidated checks. Update time, item state, relevant evidence, unresolved decision effects and next action in the same change. If scope changes, retain old IDs with superseded disposition and rationale in §12; don't erase failures or weaken the Baseline to match incomplete implementation.

## 11. Definition of Done

- Every applicable finalized normative clause in R1–R24 has an explicit implemented range, stage-specific tests and current verification evidence; no temporary unsupported gate is counted as successful feature implementation.
- All selected source, unused bodies and generic definitions receive required semantic checks. Deferred obligations are legitimate representation/substitution work and discharged at the defined deadline. No Unknown/pending proof is accepted.
- Valid programs preserve specified evaluation, Copy/Move, Loans/Origins, result delivery, cleanup, Abort, ABI/runtime and artifact behavior at Debug/Release and O0/O2. Negative cases reach the intended check with correct reason/stage/source location.
- Core identities and all settled public operations are integrated. Source dependencies/packages/locks/publication and cache invalidation work from fixed snapshots, and no-cache compilation agrees with reuse.
- Managed build/tests, fixture generation, LLVM verification, ordinary native execution, relevant C/runtime/backend checks and measured performance each have their own current records and traceable artifacts. Nonzero counts and failure/cancellation cleanup are verified.
- Normative allocation/complexity guarantees are demonstrated; broader performance changes have reproducible measurements. Existing low-allocation paths remain protected. No unmeasured speedup or universal zero-allocation claim.
- G5 is closed; no unresolved in-scope mismatch remains. G1/G2-dependent integration cannot be declared complete while interfaces remain unsettled. A separately completed defined subset must state that boundary prominently.
- `STATUS.md`, specification index/implementation-defined documentation, usage and examples agree with actual support; this plan records execution history. Draft proposals and unrelated tooling remain separate.
- NativeAOT is not required and has not been run unless later explicitly requested. Execution changes remain within the authorized compiler plan; `draft/` remains untouched.

## 12. Plan Changes and Out-of-Scope Findings

### Material plan changes

| Date | Change | Rationale / disposition |
| --- | --- | --- |
| 2026-09-16 | Completed only I3-P1/P2 explicit Property/accessor projection API exposure | Split the oversized M2 milestone into two linked subitems and preserved the preceding uncommitted function work. Baseline reproduced four false successes. Shared projection checks run before Property verification; 37 focused and 5,610 Debug/Release tests pass, including rejected conformance witnesses. No specification change or blocker; declaration-constraint/inferred provenance remains open. Execution stops here. |
| 2026-09-16 | Completed only M2/I3 named function parameter/result projection API exposure | Reused retained projection uses and parent links so normalization cannot hide qualifier/requirement domains. Corrected source-local fixtures and established 15 baseline failures in explicit Container APIs. Final 29 focused and 5,573 Debug/Release tests pass. Refreshed stale restore assets without version changes. Property/accessor and declaration-constraint projection work remains open; execution stops at this group. |
| 2026-09-16 | Completed only M2/I3 accessor receiver formation/API access | Reused ordinary Self receiver validation and rejected static accessor receivers. Corrected conditional-environment versus declaring-owner handling after two regression failures. Final 30 focused and 5,544 Debug/Release tests pass. Broader access audits remain open; earlier work is preserved and execution stops at this group. |
| 2026-09-16 | Completed only M2/I3 Property header/storage API accessibility | Reproduced four incorrectly accepted declarations; check the Property domain before accessor validation and verification. Retained existing enum/accessor-input domains. Final 28 focused and 5,514 Debug/Release tests pass; receiver and broader access audits remain open. Preserved earlier edits and stopped at this group. |
| 2026-09-16 | Completed only M2/I3 grouped nested-array Type-argument disambiguation | Opening brackets stop numeric length lookahead so valid nested Type arguments reach ordinary Type parsing/Binding. Baseline reproduced three failures; final 25 focused and 5,486 Debug/Release tests pass. Preserved length syntax, invalid-role rejection and prior uncommitted work. Broader argument/proof audits remain open; stopped at this group. |
| 2026-09-16 | Completed only M2/I3 annotation representation/certificate safety | Transparent Origin wrappers retain the original WholeType; owner/unsafe application evidence cannot certify annotated Types. Baseline reproduced four failures; final 24 focused and 5,461 Debug/Release tests pass. Full annotation proofs remain open under M4/M7 prerequisites. Preserved prior uncommitted work and stopped at this group. |
| 2026-09-16 | Completed only the M2/I3 grouped original Semantics-pair reconstruction group | Reproduced incorrect standalone-Type obligations for grouped targets and fixed the existing Binding branch. Preserved annotation validation and unrelated target proofs; 23 focused and 5,437 Debug/Release tests pass. Earlier uncommitted work is preserved. Stopped before explicit Origin-annotation/general proof work. |
| 2026-09-16 | Resumed only the next M2/I3 Type arity group with prior Layout edits uncommitted | Preserved the current checkout and recorded initial diff/hashes. Reused parser name buckets and Binding symbol chains; selected arity only within the committed scope/import stage. Corrected two obsolete same-fragment tests from §6.1.2, retaining their rejection assertions. No later milestone or native-generation work was started. |
| 2026-09-16 | Completed only the M2/I3 current-grammar Origin fragment audit at `889d7c9` | Disproved the prior source-local abstract-binder premise using §§6.1.1/6.1.2/15.3 and 20 new regression cases. Retained compiler behavior; Debug/Release pass 5,414 each. Bound proofs stay unfinished under M4/I7. Preserved prior uncommitted groups and stopped before the next audit. |
| 2026-09-16 | Completed only the M2/I3 selected Layout/fragment validation group at `889d7c9` | Honored the one-group execution boundary. Retained selected Attributes and source fragment ordinals without a separate fragment object graph; saved declaration context before parsing generic/base headers. Added source validation and canonical writing with Debug/Release/reload/allocation evidence. Nominal representation formation and generated-fragment finalization retain later prerequisites; no M3 or later milestone work began. |
| 2026-09-15 | Created initial plan at specified `PLAN.md` | Entire compiler completion scope; clean initial checkout; no pre-existing plan to preserve |
| 2026-09-15 | Entered implementation at `e47b667107249e05b7ff0f2bce4b8ada4b9d7491` | User explicitly authorized unfinished in-scope implementation. Reconciled intervening package/API migrations; no initial uncommitted changes. G4 version probes succeed with authorized escalation; downstream LLVM/native verification remains required. |
| 2026-09-15 | I4 numeric implementation uses existing conversion plans and checked continuations | Float-to-integer checks source intervals equivalent to truncation then range checking, rejecting NaN before LLVM fptosi/fptoui. Narrowing excludes source infinities from finite-overflow Abort. Exact integer literal formatting uses 39 stack characters, then selected-precision parsing, avoiding double rounding and heap strings. |
| 2026-09-15 | Corrected obsolete numeric unsupported tests | §§13.5.4/21.5.3 require the newly implemented pairs. Their positive responsibility moves to NumericConversionEmissionTest; old negative slots now cover prohibited typed 128-bit conversions. The new float-overflow reason extends the derived internal Abort catalog; existing reason IDs and backend ABI remain unchanged. |
| 2026-09-15 | Completed I2 and I4's numeric subrange without closing their broader parent milestones | Current Debug/Release and exact native artifact evidence closes this slice only. I1/I3/general Identity Acquisition and M3–M17 remain open; neither historical evidence nor interface blockers are used to disguise them. |
| 2026-09-15 | Added launch-time Windows verification containment | A worker waits for JSON dispatch until assigned to a kill-on-close job. The controller owns deadlines and bounded drains; the native profile can now safely verify retained-pipe/descendant failures. This is a verification harness, not the G1-dependent public language test runner. |
| 2026-09-15 | Separated settled operations from undefined public interfaces | G1/G2 retain explicit blockers without excluding settled testing/Mod semantics or inventing APIs |
| 2026-09-15 | Recorded current tool/NuGet restrictions and inspected artifact side effects | Prevent historical reports/hash equality/help exit code from becoming false current execution evidence |
| 2026-09-15 | Chose incremental Koto/ownership/physical-plan extension and source-first module foundation | Reuses actual architecture and current working subset; no speculative Bound Tree/redesign |

No implementation work IDs were removed. The I3-A3 exploratory protected-Type hypothesis was discarded as invalid source; its evidence and replacement audit scope remain recorded above. Later changes retain IDs and their disposition here.

### Separate findings

- `STATUS.md` mentions incomplete LSP diagnostics and KimiCode host integration. They are not compiler-language completion prerequisites; only source provenance/invalidation behavior required by R1/R23 is included. Do not schedule an editor redesign.
- `autoframe/`, `obsolete/`, general IDE packaging and remote distribution are separate projects or historical material. No changes planned here.
- `.github/workflows/test.yml` currently builds Release then calls a configuration-unspecified `dotnet test`; current explicit MTP target/configuration and Windows native coverage need I2/I35. No CI workflow or NuGet publication was executed during planning.
- Comments/status can lag the finalized specification: G6 is a concrete example. Correct them with the relevant implementation, not by changing the language to match the old subset.
- Completion of this plan as a document does not establish compiler completion. M1/M2 execution has started; remaining milestones and G1/G2-dependent integration remain unfinished. No new out-of-scope product defect has been established during execution.
