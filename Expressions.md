# Kimigayo Expressions / Operators

## 1. この文書の位置付け

式と演算子の構文・意味を定める。[SPEC.md](SPEC.md) の型、Property、所有権、制御フローの規則を前提とし、未定義だった部分を補完する。SPEC.md 本体は変更しない。

確認対象は 2026-09-07 時点の実装（`fa03bc8` を基点とし、この文書に伴う優先順位の Parser 改訂を含む）。以下の区別を用いる。

| 表記 | 意味 |
| --- | --- |
| **構文実装済み** | Parser が構文木を作る。名前解決・型検査・実行まで保証する意味ではない。 |
| **既存仕様** | SPEC.md に定義されている規則。実装は一部に限られる。 |
| **補完仕様** | この文書で採用する設計。実装時の目標であり、現在の挙動の説明ではない。 |

特記のない構文は現在の Parser に基づく。評価順序、一般的な型規則、数値演算、呼び出し、代入の詳細は補完仕様である。現状との差は末尾にまとめる。コード中の業務用関数・型は説明用であり、標準 API の存在を示さない。

## 2. 分類

分類は利用者向けの文法上の整理であり、内部の Koto 継承関係とは一致しない。

```text
Expressions
├─ Primary Expression
│  ├─ Name
│  ├─ Literal
│  │  ├─ Number / Boolean / Character / String / Null / Unit
│  │  ├─ Interpolated String
│  │  └─ Tuple / Array / Dictionary
│  ├─ Parenthesized Expression
│  └─ Function Expression
├─ Member Access
├─ Application
│  ├─ Invocation（$panic(...) を含む）
│  └─ Generic Application
├─ Index / Slice Expression
├─ Conversion Expression
├─ Unary Expression
│  ├─ Sign / Logical Negation / Dereference / From-end Index
│  └─ Prefix / Postfix Increment and Decrement
├─ Binary Expression
│  ├─ Arithmetic / Shift / Bitwise
│  ├─ Comparison / Logical
│  └─ Type Requirement Test
├─ Range Expression
├─ Assignment Expression
│  ├─ Simple Assignment
│  └─ Compound Assignment
├─ Selection Expression
│  └─ if / match
├─ Iteration / Labeled Block Expression
│  └─ for / while / loop / Label:
└─ Control Transfer Expression
   └─ return / exit / continue / yield

Related Syntax
├─ Block Statement: unsafe: / defer:
├─ Compile-time Directive: #if / #case
├─ Attribute: #Name
└─ Composition Root: $（$panic を除く詳細は別途定義）
```

`unsafe:` と `defer:` は文であり、初期化式や引数には置けない。通常のインデント Block は構文の入れ物であり、単にインデントを増やして任意の値を返す式にはしない。複数の処理から値を得る場合は、選択式やラベル付き Block を使う。

## 3. 式に共通する規則

### 3.1. 値・場所・評価文脈

正常終了する式は型を持つ結果を返す。結果が不要な場合は Unit `()` を使う。`return` など正常終了しない式は Never 型を持ち、その後の通常の評価を行わない。

**場所（place）** は値を保持する記憶領域を指す。ローカル変数、組み込み配列の要素、タプルの要素、ポインターの参照先などが該当する。場所を値として読む際は、要求された型に従って Copy・Move・借用を行う。括弧は場所としての性質を変えない。

Property は読み出し時に getter、書き込み時に setter を使う。代入できる Property でも、その格納領域を直接借用できるとは限らない。Property の型と getter の結果型も区別する。[既存の Property 規則](SPEC.md#6-properties) に従う。

| 文脈 | 例 | 規則 |
| --- | --- | --- |
| 値を使う文脈（Value Context） | 初期化式、引数、演算子のオペランド、条件、`=>` の本体 | 式の結果を使う。期待する型が Unit でも同じ。 |
| 結果を捨てる文脈（Discard Context） | Block に直接書いた式 | 結果だけを捨てる。副作用・型検査・破棄処理は残る。 |
| 代入先を要求する位置 | `target = value` の左辺 | 書き込み権限がある場所、または setter が利用できる Property を要求する。 |

Block の最後の式も自動的には返さない。行末の `;` は文脈や戻り値の有無を変えない。`let` / `var` 宣言は一般の値を返す式ではない。ただし `if` / `while` の `(let name = expression)`、`(var name = expression)` は、初期化した `bool` を条件として使う既存の専用構文である。

### 3.2. 評価順序と寿命（補完仕様）

複数のオペランドは、原則としてソースに書かれた左から右へ、それぞれ一度だけ評価する。優先順位は式のまとまりを決め、評価順序は副作用の順番を決める。

| 構文 | 評価順序 |
| --- | --- |
| `a() + b() * c()` | `a()` → `b()` → `c()` → 乗算 → 加算 |
| `receiver().method(a(), b())` | receiver → 呼び出し対象の確定 → 第1引数 → 第2引数 → 呼び出し |
| `array()[index()]` | 対象 → 添字 → 要素へのアクセス |
| `(a(), b())` / `[a(), b()]` | 各要素を左から右へ |
| `[key(): value(), ...]` | エントリー順に、キー → 重複検査 → 値 → 挿入。重複なら値の評価前に Panic。 |
| `start()..end()` | 開始境界 → 終了境界 |
| `"\(a()) / \(b())"` | 各埋め込み式の評価と文字列化を左から右へ |

`and` / `or` と選択式は必要な部分だけを評価する。代入も左辺の対象を先に確定するが、値の読み出し・書き込みの順序は 7 節で定める。型引数や変換先の型は実行時には評価しない。

評価中に制御移動・発散・Panic Termination が起きたら、残りのオペランドとその演算は実行しない。ただし、実行されない構文も名前・型・制御移動先の検査を受ける。`#if` / `#case` で除外された構文は既存の条件付きコンパイル規則に従う。

一時的な所有値は、原則としてその値を作った最外の式の評価終了まで保持し、生成と逆順に破棄する。呼び出しの引数用一時値は呼び出し終了まで保持する。束縛や結果へ Move した値は移動先の寿命に従う。反復対象や `match` 対象など、構文全体で必要な値はその利用が終わるまで保持する。

借用は元の値の寿命を延長しない。例えば一時的な配列から得た Slice を、その配列が破棄された後も保持することはできない。制御移動時の結果確保・`defer`・破棄の順序は [SPEC.md 9 節](SPEC.md#9-scope-exit-and-cleanup) に従う。

### 3.3. 型の決定（補完仕様）

宣言型、引数の型、戻り値の型などの期待型を式の内部へ伝える。期待型がなければオペランドから推論する。通常の数値演算は同じ数値型どうしで行い、整数の幅・符号や整数と浮動小数点を暗黙に混在させない。

型未確定の整数リテラルは、値が収まる期待整数型に適合する。期待型がなければ `i32` とし、収まらなければ明示的な型指定を要求する。浮動小数点リテラルは期待する `f32` / `f64` に適合し、期待型がなければ `f64` とする。現在の内部保存型 `i128` / `f64` はこの既定型を意味しない。

```kimi
let a: i64 = 10
let b = a + 20          // 20 を i64 として扱う。
let c: i32 = 3
let d = a + c@i64       // 確定した異なる型の値は明示変換する。
let minimum: i8 = -128  // 符号を含む値が期待型の範囲内か検査する。
```

`bool`、`char`、数値の間に暗黙変換はない。条件に整数やポインターを直接渡すこともできない。借用・再借用は所有権規則に基づく別の適合であり、暗黙の数値変換とは区別する。

### 3.4. 区切りと改行

式の区切りには改行や `;`、引数・要素の区切りには `,` を使う。カンマは二項演算子ではない。`()` と `[]` の内部でもインデント規則は残る。複数行の引数・要素は一段深く書き、閉じ括弧は開始行の位置に戻せる。行頭の `.` によるメソッドチェーンの継続も一段深く書く。

一般の二項演算子を任意の行頭に置いて前行を継続する構文は、この仕様では追加しない。`:`、`=>`、`->`、`in` はラベル・本体・型・反復など各構文の区切りであり、一般の二項演算子として扱わない。

### 3.5. Panic Termination（補完仕様）

**Panic Termination** は、正常な実行を継続できない場合にプログラム全体を異常終了する共通の規則である。明示的な要求と暗黙の実行時検査失敗を同じ体系で扱う。

```text
Panic Termination
├─ explicit
│  └─ $panic(...)
└─ implicit runtime check failure
   ├─ integer overflow
   ├─ division by zero
   ├─ out-of-range index
   └─ invalid conversion
```

暗黙的 Panic の分類は主な例を示す。この文書でいう実行時の「検査失敗」はすべてこの規則に従い、不正なシフト数・Range 境界・辞書の重複キー・存在しないキーへのアクセスも含む。どの値を不正とするかは各演算の規則で定める。例えば浮動小数点のゼロ除算には IEEE 754 の規則を適用し、整数のゼロ除算と同じ Panic 条件にはしない。

#### 3.5.1. 明示的 Panic

`$panic(message)` は Composition Root が提供する終了操作で、`message` は `string`。通常の呼び出しと同様に引数を一度評価し、評価が正常に終わったら Panic Termination を発生させる。呼び出し式の型は Never であり、正常復帰しない。

```kimi
func requirePositive(value: i32) -> i32
    if value <= 0
        $panic("value must be positive")
    return value
```

#### 3.5.2. 共通の終了規則

明示的・暗黙的 Panic は、発生理由とソース位置を診断情報として扱う。暗黙的 Panic の位置は失敗した演算、明示的 Panic の位置は `$panic(...)` の呼び出し位置とする。診断の出力先・形式は実行環境に委ね、出力の成功を終了の前提にしない。

Panic は回復不能とし、発生したスレッドだけでなくプログラムを終了する。通常の制御移動先へ結果を届けず、スタック巻き戻し、`defer`、デストラクターの実行を保証しない。cleanup 中の Panic でも、残りの cleanup や確保済みの戻り値の配送は保証しない。この規則は SPEC.md の異常終了時の扱いを補完する。

Debug / Release で Panic の条件と終了規則を変えない。暗黙の検査失敗は通常の関数呼び出しへの単純な書き換えではなく、言語が保証する終了動作とする。`$panic` や診断処理の差し替えによって正常復帰へ変更することはできない。

コンパイル時の必須定数評価で判明した不正は、引き続きコンパイルエラーとする。実行時検査の対象となる演算は、実際に評価されて検査に失敗した時にだけ Panic を発生させる。最適化は短絡評価などで実行されない演算から Panic を発生させてはならない。unsafe の契約違反による未定義動作を、すべて Panic として検出する保証もない。

Panic Termination は言語上の終了規則であり、`trap` 命令などの具体的な実装手段とは区別する。失敗を値として返す操作や、整数の循環・飽和演算は、明示的なライブラリー操作として別途定義する。

## 4. 基本式と後置の構文

### 4.1. 名前・リテラル・括弧

| 形式 | 意味 |
| --- | --- |
| `name` | 可視な束縛・関数などへの参照 |
| `123`、`0xff`、`1.5`、`true`、`'あ'`、`"text"` | 数値・真偽値・文字・文字列リテラル |
| `"value = \(value)"` | 式の結果を埋め込む文字列 |
| `null` | 期待型で定まる `unsafe/T` の null ポインター |
| `()` | Unit 値 |
| `(value)` | 括弧によるグループ化 |
| `(value,)`、`(a, b)` | 1要素または複数要素のタプル |
| `[a, b]`、`[]` | 配列リテラル |
| `[key: value]`、`[:]` | 辞書リテラル |

数値表記・エスケープ・Unicode の詳細は [SPEC.md 3 節](SPEC.md#3-lexical-structure) に従う。`null` は汎用の「値なし」ではない。安全な参照は non-null であり、`let value = null` のように型を決められない使用はエラー。

**補完仕様:** タプルは位置ごとに別の型を持てる。配列の要素型、辞書のキー型と値型は、それぞれ一つに決まらなければならない。空リテラルは期待型を必要とする。具体的なコレクション型の選択・格納方式はライブラリー仕様に委ね、曖昧な場合に暗黙の汎用オブジェクト型へ逃がさない。

辞書リテラルでは同値のキーの重複をエラーとする。定数として重複を判定できる場合はコンパイルエラー、それ以外は生成時に 4.1.1 の手順で検査する。補間には対象型の文字列化能力を要求し、任意の型を自動的に表示可能とはしない。

```kimi
let pair = (10, "ten")
let single = (10,)
let values = [10, 20, 30,]
let names = [1: "one", 2: "two",]
let message = "first = \(values[0])"
```

#### 4.1.1. 辞書リテラルの生成と重複キー（補完仕様）

各エントリーをソース順に、次の手順で処理する。

1. キー式を一度評価し、そのキーを保持する。
2. それまでに挿入したエントリーに、同値のキーがあるか検査する。
3. 重複があれば、暗黙の Panic Termination を発生させる。そのエントリーの値と、後続エントリーは評価しない。
4. 重複がなければ、値式を一度評価する。
5. 保持したキーと得られた値を挿入する。

同値性は辞書のキー等値規則に従い、ハッシュ値が一致しただけでは重複としない。キー型は、辞書に保持されているキーについて、その論理的な等値性およびハッシュ値が格納中に変化しないことを保証しなければならない。後のエントリーで既存の値を上書きしない。

挿入候補のキーについても、値式の評価を含め、重複検査の開始から挿入完了まで論理的な等値性およびハッシュ値が変化してはならない。

```kimi
let x: i32 = getKey()
let map = [x: first(), x: second()]
```

実行時に検査する場合、この例では最初の `x` の評価・重複検査、`first()`、挿入を終えてから、二つ目の `x` を評価して重複を検出する。`second()` は実行しない。

キーの評価・重複検査・値の評価が正常終了しなければ、そのエントリーを挿入せず、後続の処理も行わない。途中まで構築した辞書を結果として返すことはなく、すでに完了した副作用も巻き戻さない。Panic 時の診断位置は重複した後側のキー式とし、終了と cleanup は 3.5 節の共通規則に従う。

### 4.2. メンバーアクセス

`expression.name` でメンバーを選ぶ。`Group.name` のような宣言コンテナーの修飾と、`object.name` のような値へのアクセスを名前解決で区別する。

**補完仕様:** `.` の右辺はメンバー名、またはタプルの要素番号に限る。`pair.0` は先頭要素で、番号は範囲内の10進整数リテラルとする。任意の式を置く動的メンバー検索はない。現在の Parser はより広い Primary Expression を受け入れるため、追加の検査が必要である。

getter の結果型、setter の有無、受け手の借用権限に従ってアクセスを検査する。生ポインターからの自動参照外しは行わず、`(*pointer).name` と書き、Unsafe Block 内で使用する。

```kimi
let count = collection.count
collection.count = 10   // setter と書き込み権限が必要。
let first = pair.0
```

### 4.3. 呼び出しと型引数

`callee(arg1, arg2)` は関数・メソッド・関数値の呼び出し。引数ゼロは `callee()`、末尾のカンマは許可する。`callee<T, U>(args)` は明示的な型引数の適用後に呼び出す。

**補完仕様:** 通常の引数は位置順に対応付ける。`name: expression` は宣言の外部引数名を指定する。位置引数を先に、名前付き引数を後に書く。名前付き引数の順序は自由だが、評価は記述順とする。未知の名前、同じ引数への二重指定、不足・過剰な引数はエラー。外部名と内部名を分ける宣言は `external => internal: T` を使う。

省略可能な引数は `name?: T = defaultExpression` と宣言する。`?` には既定値を必須とし、既定値だけによる省略可能性は導入しない。`?` はここでは引数の省略を示し、nullable 型を意味しない。省略した既定値は、明示引数の評価後、パラメーター宣言順に呼び出しごとに一度評価する。名前は宣言位置で解決し、先行パラメーターを参照できるが、後続パラメーターや呼び出し元のローカルは参照できない。

```kimi
func scale(value: i32, by => factor: i32) -> i32 => value * factor
let result = scale(3, by: 4)

func offset(value: i32, by?: i32 = 1) -> i32 => value + by
let next = offset(3)
let adjusted = offset(3, by: 5)
```

一般の関数型は引数名や既定値を保持しない。関数値として呼ぶ場合は全引数を位置で指定する。オーバーロードの候補は引数・型引数・制約で一意に決まる必要があり、宣言順で選ばない。戻り値型だけで区別するオーバーロードは作らない。

式内の型引数は対象名に `<` を隣接させる。現在の Parser はこの隣接と閉じる `>` の先読みで比較式と区別する。`f<T>(x)` は型引数の適用、`a < b` は比較。入れ子の型引数では `>>` を二つの閉じ括弧として扱う。比較には演算子の前後に空白を入れ、曖昧な表記を避ける。

型引数の推論・特殊化、定数の型引数、ユーザー定義型の構築規約は別途実装する。`T(args)` が自動的に任意の型を構築するという規則は、この文書では追加しない。

### 4.4. 関数式

無名関数は `func (parameters) -> Result => expression` またはインデント Block で記述する。関数を作った時点では本体を実行せず、呼び出した時に実行する。`=>` は結果を返し、Block 形式では非 Unit の結果に `return` が必要。

```kimi
let twice = func (value: i32) -> i32 => value * 2
let result = twice(5)
```

**補完仕様:** まず外側のローカルを捕捉しない関数値を規定する。捕捉が必要な場合は、捕捉リスト・Copy / Move・借用寿命・呼び出し能力をまとめて定義するまで診断対象とする。暗黙に全変数をコピーしたり寿命を延長したりしない。

### 4.5. 添字・Range・Slice

長さのある組み込み列への `value[index]` は要素を選ぶ。要素添字には、非負の `isize` または From-end Index `^n` を指定できる。`n` は非負の `isize` で、`^n` は長さを基準に `length - n` と解決する。`^1` は最後の要素、`^0` は終端境界であり要素には使えない。

| Range | 意味 |
| --- | --- |
| `start..end` | start から end の直前まで |
| `start..=end` | start から end を含む位置まで |
| `start..` | start から終端まで |
| `..end` / `..=end` | 先頭から、指定した終了境界まで |
| `..` | 全体 |

Range の明示境界は非負の `isize` または `^n`。省略した開始は 0、終了は列の長さで、その終了は含まない。`..=` の終了境界は省略できない。Range 自体は境界情報を保持し、列へ適用する時に長さ依存の境界を解決する。

`value[range]` は要素をコピーしない Slice を返し、元の値の Origin に従う。境界解決後、半開区間は `0 <= start <= end <= length`、閉区間は `0 <= start <= end < length` を満たす必要がある。`start == end` の半開区間は空である。

```kimi
let values = [10, 20, 30, 40]
let last = values[^1]       // 40
let middle = values[1..^1]  // 20, 30 を参照する Slice
let all = values[..]
let empty = values[2..2]
```

**補完仕様:** 安全な列の範囲外アクセスは検査失敗となり、Panic Termination を発生させる。検査省略は安全性を証明できる場合に限る。`^n` の負値も不正とする。Range の連鎖 `a..b..c` は構文エラー。括弧を付けても Range 自体を別の Range の数値境界にはできない。

生ポインターの `pointer[index]` は符号付き `isize` の変位で、負値も許す。長さ検査はなく、`^n` と Range は禁止する。詳細は [生ポインター仕様](SPEC.md#46-raw-pointers-and-unsafe-operations) に従う。

辞書の `dictionary[key]` は列の数値添字と別の操作である。**補完仕様:** 読み出しにはキーの存在を要求し、不在なら検査失敗とする。失敗を通常の値として扱う検索、挿入、ユーザー定義 indexer の宣言はライブラリー側で別途定義する。UTF-8 の `string` も、バイト・Unicode scalar・書記素のどれを数えるかを別途定義するまで、整数添字で「文字」を取得できるとはしない。

## 5. 演算子の優先順位

### 5.1. 設計方針と他言語との比較

通常の演算子は Rust の順序を基準にする。算術で値を作り、シフト・ビット演算でビット列を組み立て、比較で真偽値にし、論理演算で条件を組み合わせる。`flags & mask == 0` は `(flags & mask) == 0` と解釈する。

Rust と Swift は、どちらもビット演算を比較より強くし、比較・等値判定を同じ非結合の段に置く。一方、シフト・算術・型変換・Range の相対的な強さは異なるため、次の方針で統一する。[Rust Reference](https://doc.rust-lang.org/reference/expressions.html#expression-precedence)、[Swift 標準の優先順位定義](https://github.com/swiftlang/swift/blob/main/stdlib/public/core/Policy.swift)

| 比較点 | Rust | Swift | Kimigayo の採用規則 |
| --- | --- | --- | --- |
| シフトと算術 | 算術が先 | シフトが先 | 算術が先。`1 << n + 1` は `1 << (n + 1)`。 |
| ビット演算の段 | 算術・シフトの後に `&` → `^` → `\|` | `&` は乗除算と同段、`^` / `\|` は加減算と同段 | Rust と同じく、算術とビット演算を分ける。 |
| 型変換 | 前置演算より弱く、乗除算より強い | 算術・Range より弱く、比較より強い | Rust と同じ位置に `@` を置く。 |
| 比較・等値判定 | 同段・非結合 | 同段・非結合 | 同段・非結合。連鎖には括弧を要求する。 |
| Range | 論理演算より弱く、代入より強い | 算術より弱く、型変換・比較より強い | Rust と同じ。既存の Range 仕様を維持する。 |

`is` は型要件の組み合わせを右辺に取る Kimigayo 固有の構文なので、6.5 節の特則を維持する。Swift の実行時型判定と同じ構文としては扱わない。

### 5.2. 優先順位表

上ほど強く結び付く。左結合は `a op b op c = (a op b) op c`、右結合は `a op (b op c)` を表す。結合規則は型として有効な使用を保証しない。

| 強さ | 演算子・構文 | 結合 |
| --- | --- | --- |
| 1 | `.name`、`(...)`、`<Types>`、`[...]`、後置 `++` `--` | 後置の連鎖を左から適用 |
| 2 | 前置 `+` `-` `not` `*` `^` `++` `--` | 右 |
| 3 | `@Type` | 左 |
| 4 | `*` `/` `%` | 左 |
| 5 | `+` `-` | 左 |
| 6 | `<<` `>>` | 左 |
| 7 | `&` | 左 |
| 8 | `^` | 左 |
| 9 | `\|` | 左 |
| 10 | `<` `<=` `>` `>=` `==` `!=` | 非結合 |
| 11 | `and` | 左 |
| 12 | `or` | 左 |
| 13 | `..` `..=` | 非結合 |
| 14 | `=`、複合代入 | 右 |

`is` の左側は比較と同じ強さで結び付き、右辺は `or` まで含む要件式を取り込む。上表の6種類の値比較とは区別し、他の条件と組み合わせる場合は `(T is A) and enabled` と書く。`as` は予約扱いであり、利用可能な演算子の表には含めない。

比較・等値判定を続けた `a < b < c`、`a == b == c`、`a < b == flag` は括弧なしでは構文エラー。`a < b and b < c`、`(a < b) == flag` と書く。括弧を付けても、各比較の型検査は必要である。

`@` は右辺に型を取る変換構文で、前置演算より弱い。`-x@i64` は `(-x)@i64` となり、変換後に符号を反転する場合は `-(x@i64)` と書く。特に `-128@i8` は負値全体を変換できる。変換先の型は修飾名を含むため、変換結果のメンバーには `(x@T).name` と書く。

変換先の型引数も、型名への `<` の隣接と対応する `>` で比較式と区別する。`value@Box<i32>` は型引数を含む変換、`value@i64 < limit` は変換結果の比較である。比較の `<` の前後には空白を入れる。

`if` / `match` / 反復 / ラベル付き Block は、それぞれ自身の本体構文を持つ。制御移動の `return` / `exit` / `yield` は、後続の式全体を結果オペランドとして読む。`return a + b` は加算の結果を返す。

### 5.3. 解釈例

| 記述 | 解釈 |
| --- | --- |
| `flags & mask == 0` | `(flags & mask) == 0` |
| `a \| b ^ c & d` | `a \| (b ^ (c & d))` |
| `1 << n + 1` | `1 << (n + 1)` |
| `a + b << count` | `(a + b) << count` |
| `a + b@i64 * c` | `a + ((b@i64) * c)` |
| `-value@i64` | `(-value)@i64` |
| `value@i64@f64` | `(value@i64)@f64` |
| `value@i64 < limit` | `(value@i64) < limit` |
| `not ready and flags & mask != 0` | `(not ready) and ((flags & mask) != 0)` |
| `a < b and b <= c or done` | `((a < b) and (b <= c)) or done` |
| `start + 1..end - 1` | `(start + 1)..(end - 1)` |
| `target = flags & mask == 0` | `target = ((flags & mask) == 0)` |

型として有効な例でも、意図を強調する括弧は自由に付けられる。優先順位・結合性は評価順序や型変換の可否を変えない。

## 6. 演算の意味

### 6.1. 単項演算（補完仕様）

| 演算子 | 対象と結果 |
| --- | --- |
| `+value` | 数値を同じ型・同じ値で返す。 |
| `-value` | 符号付き整数または浮動小数点の符号を反転する。 |
| `not value` | `bool` を反転し `bool` を返す。 |
| `*pointer` | 生ポインターが指す場所。形成・アクセスには unsafe の規則を適用する。 |
| `^value` | 非負の `isize` から末尾基準の添字を作る。 |
| `++target` / `--target` | 整数を 1 増減し、更新後の整数値を返す。 |
| `target++` / `target--` | 整数を 1 増減し、更新前の整数値を返す。 |

増減は読み書き可能な整数の代入先に限る。浮動小数点・生ポインター・任意の型には広げない。対象の確定・読み出し・書き込みはそれぞれ一度で、オーバーフロー時には書き込まない。Property の読み戻しを行わず、前置形は計算した更新値を返す。一般の更新には `+=` / `-=` を使える。

```kimi
var count: i32 = 1
let before = count++  // before = 1, count = 2
let after = ++count   // after = 3, count = 3
let disabled = not enabled
```

`not` は比較より強いので、比較結果の否定は `not (a == b)` と書く。生ポインター以外の明示的な参照外しを、この文書から自動的には導入しない。

### 6.2. 算術・ビット演算（補完仕様）

`+ - * /` は同じ数値型を受け取り、同じ型を返す。`%` は整数に限る。整数除算は 0 方向に切り捨て、剰余は `a = (a / b) * b + a % b` を数学的な整数上で満たす。0 でない剰余の符号は左辺と同じ。

```kimi
let quotient = -7 / 3   // -2
let remainder = -7 % 3  // -1
let bits: u32 = 0b1010
let masked = bits & 0b0110  // 0b0010
let shifted = bits << 1     // 0b10100
```

整数の `+ - *`、単項 `-`、増減、複合代入の算術部分は範囲を検査する。整数のゼロ除算は不正。符号付き最小値と `-1` の `/` および `%` も不正とする。コンパイル時の必須定数評価で判明した不正はコンパイルエラー、実行時に判明した不正は暗黙の Panic Termination を発生させる。共通の終了規則は 3.5 節に従い、Debug / Release で値の意味を変えない。

`& | ^` は同じ整数型のビット積・和・排他的論理和で、`bool` には使用しない。`<< >>` の右辺は任意の整数型とし、`0 <= shift < 左辺のビット幅` を要求する。範囲外のシフト数は検査失敗。左シフトは上位ビットを捨てて下位を 0 で埋め、右シフトは符号付きなら符号拡張、符号なしなら 0 拡張する。シフトのビット切り捨ては算術オーバーフローとは区別する。

浮動小数点演算は `f32` / `f64` の IEEE 754 の規則に従い、通常の丸めは最近接・偶数丸めとする。無限大・NaN・符号付きゼロを扱い、整数のゼロ除算とは区別する。通常の演算を、丸めや NaN の結果が変わる再結合・融合に暗黙に置き換えない。

`string + string` は文字列連結として認めるが、暗黙の数値文字列化は行わない。生ポインターには既存仕様の `pointer + isize`、`pointer - isize` だけを認め、別の型との算術やポインターどうしの減算を認めない。

### 6.3. 比較・論理演算（補完仕様）

`== != < <= > >=` は `bool` を返す。数値比較は同じ型どうしで行う。`bool` と Unit は等値比較だけを持つ。`char` は Unicode scalar の値順、`string` は正規化やロケール処理を行わない UTF-8 のバイト列による等値比較・辞書順比較とする。別表現の正規化が必要な場合は明示する。

浮動小数点の `+0.0 == -0.0` は true。NaN を含む `== < <= > >=` は false、`!=` は true。したがって浮動小数点の順序は全順序ではない。

比較はオペランドを読み取るために借用でき、比較だけのために非 Copy の所有値を Move しない。ユーザー定義型の比較能力は明示的な契約で提供する。安全な借用どうしの比較は同じ参照先型の比較能力を必要とし、参照先の値を比較する。タプルは対応する全要素が比較能力を持つ場合に、要素ごとの等値比較・先頭からの辞書順比較を行う。

値の等値とオブジェクトの同一性は別の操作とし、`==` を型によって暗黙にアドレス比較へ切り替えない。生ポインターの `== !=` は既存仕様どおり、同じポインター型のアドレス比較である。

| 演算 | 規則 |
| --- | --- |
| `left and right` | left が false なら false。true の時だけ right を評価する。 |
| `left or right` | left が true なら true。false の時だけ right を評価する。 |
| `not value` | true と false を反転する。 |

論理演算のオペランドと結果は `bool`。`and` / `or` の短絡性はユーザー定義で変更できない。

```kimi
let valid = index >= 0 and index < count
let found = valid and matches(values[index])
let clear = (flags & mask) == 0
```

### 6.4. 型変換 `@`（補完仕様）

`expression@Type` は明示的な型変換。`value@ref` のように Type Semantics だけを指定した場合は元の Core Type を使う。実行式の Origin は推論し、ここに `from origin` は書かない。

| 変換 | 規則 |
| --- | --- |
| 整数 → 整数 | 値が変換先の範囲内なら変換。それ以外は検査失敗。切り捨てない。 |
| 整数 → 浮動小数点、浮動小数点 → 浮動小数点 | 最近接・偶数丸め。有限値が丸めにより無限大になる場合は検査失敗。NaN・無限大は浮動小数点間で保持するが NaN のペイロードは保証しない。 |
| 浮動小数点 → 整数 | 小数部を 0 方向に切り捨て、変換後の数学的な整数が範囲内か検査。NaN・無限大は検査失敗。 |
| 値 → `ref` / `uniq` などの借用 | 有効な場所・適切なアクセス権・Origin が必要。参照寿命を延長しない。 |
| 生ポインター間、生ポインター ↔ `usize` | Unsafe Block 内に限り、SPEC.md の変換・provenance 規則を適用する。 |

数値と `bool` / `char`、文字列解析、任意のビット再解釈は `@` の組み込み数値変換には含めない。安全な借用から所有権の取得、共有借用から排他的アクセスへの昇格、参照カウントの暗黙増加も行わない。

```kimi
let wide = value@i64
let truncated = 3.9@i32  // 3
let negative = (-128)@i8
let shared = data@ref
```

実装には `as` の二項ノードもあるが意味は未定義である。変換表記を増やさないため、**本仕様では `as` を予約扱い**とし、`@` の別名や動的キャストとして使用しない。失敗を返す動的変換は結果型と併せて別途定義する。

### 6.5. 型要件の判定 `is`

`subject is requirement` は型・Type Semantics の要件を表す。宣言の Type Contract と `#if` / `#case` では既存仕様に従う。値の等値比較には `==` を使う。

**構文実装済みの特則:** `is` は左側では比較の強さで結び付き、右辺には `and` / `or` を含む要件式を取り込む。直後の `not` は右辺全体を否定する。

| 記述 | 解釈 |
| --- | --- |
| `T is A and B` | T が A と B の両方を満たす。 |
| `T is A or B` | T が A または B を満たす。 |
| `T is not A or B` | T が `(A or B)` を満たさない。 |
| `T is A and not B` | T が A を満たし、B を満たさない。 |

他の真偽条件と組み合わせる時は `(T is A) and enabled` のように `is` 全体を括弧で囲む。否定を部分的に適用する場合も、`T is (not A) or B` と括弧で明示する。

**補完仕様:** 一般の値に対する実行時の `value is T`、パターン束縛、成功後の型の絞り込みは本仕様では追加しない。型・Semantics に対する判定はコンパイル時に確定する `bool` とし、未解決の要件を実行時まで持ち越さない。

## 7. 代入（補完仕様）

### 7.1. 単純代入

`target = value` は代入先を更新し、Unit `()` を返す。代入した値そのものは返さない。

1. 左辺の受け手・添字を左から右へ評価し、代入先を一度だけ確定する。Property の getter は呼ばない。
2. 右辺を評価し、代入先の型に適合する結果を Copy / Move で確保する。
3. 通常の場所なら、置換される初期化済みの旧値を破棄し、新値を格納する。Property なら setter に新値を渡す。
4. 正常に書き込みが終われば Unit を返す。

右辺が正常終了しなければ書き込まない。それ以前の副作用を巻き戻す意味ではない。左辺の確定から書き込みまで場所の有効性を保ち、右辺からの競合する変更・Move は借用検査で拒否する。未初期化ローカルへの初期化は確実な初期化の規則に従い、生の未初期化メモリーへの書き込みとは区別する。

```kimi
var count: i32 = 0
count = 10
let done: () = (count = 20)
// if count = 30 ... はエラー。条件は bool、代入結果は Unit。
```

右結合なので `a = b = c` の構文上の意味は `a = (b = c)`。内側の結果が Unit のため、通常の数値変数への連鎖代入は型エラーになる。複数の変数を更新する場合は別々に書く。分解代入や Slice 全体への一括代入は別途定義する。

### 7.2. 複合代入

対象は `+= -= *= /= %= &= |= ^= <<= >>=`。`target op= value` は対応する二項演算を行って結果を代入し、Unit を返す。

対象を一度確定し、旧値を一度読み、右辺を評価し、演算結果を一度書き込む。`target = target op value` の文字列置換ではなく、受け手や添字を再評価しない。Property は getter 一回と setter 一回を使う。getter の結果で演算でき、結果を Property の型へ格納できなければエラー。隠れた Move や複製で不足する能力を補わない。

```kimi
values[nextIndex()] += amount() // 添字 → 旧値の読み出し → amount → 加算 → 書き込み
```

右辺・演算が正常終了しなければ書き込みを行わない。getter や右辺による副作用は残る。この操作は不可分なアトミック操作ではなく、並行アクセスの同期は別途必要。

生ポインターの `+=` / `-=` は既存の変位演算と代入の組み合わせに限り、同じ unsafe 条件を要求する。それ以外のポインター複合演算は認めない。

## 8. 制御フローを持つ式（既存仕様）

詳細は [SPEC.md 8 節](SPEC.md#8-control-flow)。ここでは式としての結果を整理する。

| 構文 | 正常終了時の結果 |
| --- | --- |
| `if` / `match` | `=> expression` または自分を対象とする `yield expression` の結果 |
| `for` / `while` | Unit。`exit` に結果オペランドを付けられない。 |
| `loop` | 自分を対象とする `exit expression` の結果。`exit` だけなら Unit。 |
| `Label:` Block | `exit expression from Label` の結果。末尾の式は結果にならない。 |
| `return` / `exit` / `continue` / `yield` | 式自体は Never。結果オペランドは移動先へ渡す。 |

選択式は、値を使う文脈にあるか、`=>` の枝があるか、自分への `yield` があると結果を要求する。その場合、`if` は最後の `else`、`match` は網羅性が必要。正常終了する各経路は結果を供給し、型が適合しなければならない。Block 形式の枝では `yield ()` も省略できない。

結果を要求しない選択式は Block 形式で、到達した末尾や非選択経路から Unit を返す。ラベル付き Block は値を使う文脈、または結果付きの自己対象 `exit` がある場合に結果を要求し、正常終了する全経路で明示的な結果付き `exit` が必要になる。

```kimi
let sign = if value < 0 => -1
else if value > 0 => 1
else => 0

let answer = if ready
    prepare()
    yield calculate()
else
    yield 0

let text = match enabled
    true => "on"
    false => "off"

let found = loop
    let value = next()
    if acceptable(value)
        exit value

let result = resolve:
    if cached()
        exit cachedValue() from resolve
    exit calculate() from resolve
```

`yield` は最も内側の `if` / `match`、ラベルなしの `exit` は最も内側の反復または Deferred Block を対象とする。ラベル付き Block を終えるには `exit from Label` を明示する。`continue Label` は指定した反復の次回へ進む。関数境界・Deferred 境界をまたぐ移動や、反復境界をまたぐ `yield` はできない。

正常終了する経路がない式は Never である。ただし、必要な結果の欠落を Never として受理してはならない。到達不能な結果も、期待型または到達可能な結果から決まる型があれば、その型に対して検査する。

## 9. 拡張方針と実装との差

### 9.1. 表記を増やす前に固定する規則

演算子の記号・優先順位・結合性は言語で固定する。ユーザー定義型の算術や比較は、対応する型契約の実装として拡張する方針とする。宣言構文・契約メンバー・解決手順は別途定義し、それまでは組み込み演算以外を実装済みとは扱わない。

拡張しても、左から右の評価、オペランドの評価回数、比較の `bool`、代入の Unit を変えられない。`and` / `or` / `not`、`=`、`@`、`is`、Range、制御移動はユーザー定義で再解釈しない。新しい記号や独自の優先順位宣言も導入しない。

`!`、`&&`、`||`、`~`、`**`、`??`、`?.`、三項 `?:` はこの仕様の演算子ではない。論理演算は `not` / `and` / `or`、条件による値の選択は `if` を使う。単項 `&` も借用表記として導入せず、既存の `@ref` / `@uniq` を使う。これらの一部が字句として認識されても、式として使用できるという意味ではない。

`#Name` は属性、`#if` / `#case` はコンパイル時指令で、一般の実行時単項演算とは区別する。`$` は **Composition Root** でありマクロではない。`$panic(...)` は 3.5 節で定める終了操作である。現行の内部名 `MacroKoto` は言語上の意味を確定しない。Composition Root のその他の操作、依存関係の解決・寿命・失敗の規則は別文書で定める。

### 9.2. 実装時に埋める差分

| 項目 | 現状と必要な対応 |
| --- | --- |
| 基本式・演算子・後置構文 | 構文木の生成は広く対応。名前解決・一般の型検査・実行コード生成は未完成。 |
| 演算子の優先順位 | 5 節の順序を Parser に反映。`@` は前置演算と乗除算の間、ビット演算は比較より強く、比較・等値判定は同段とする。 |
| 比較の連鎖・メンバー名 | 括弧なしの値比較の連鎖は Parser で診断する。メンバー名の制限は追加検査が必要。 |
| 呼び出し | ラベル・外部名・`?`・既定値を保存するが、対応付け・順序制限・省略可能性・解決は補完が必要。 |
| 単項・算術・代入 | ノードは存在するが、結果型・オーバーフロー・評価順序・Property の一回アクセスは本仕様に沿う実装が必要。 |
| リテラル・コレクション | 字句・構文検査はある。一般の型推論、整数の既定型、辞書の値評価前の重複検査と Panic などは未実装。 |
| Range / 添字 | 境界を含む構文と Range 連鎖の診断は実装済み。一般の境界検査・Slice の寿命は未実装。 |
| `is` | 右辺の特則は回帰テストあり。型契約の意味検証・一般の判定は未完成。 |
| `as` / `$` | ノードはある。`as` は本仕様では予約扱い。`$panic` を除く Composition Root の意味は別途定義。 |
| Panic Termination | `$panic` の名前・型の検証、明示的・暗黙的 Panic の共通処理、診断情報、実行時終了は本仕様に沿う実装が必要。 |
| 無名関数 | 構文と関数境界は扱う。関数値の生成・捕捉・呼び出し能力は未完成。 |
| 制御フロー | 結果経路、移動先、Unsafe 許可などの解析は一部実装済み。所有権・Origin・実行時 cleanup は未実装。 |

### 9.3. 根拠と参考資料

実装の主な確認先:

- [Parser.cs](Kimi/Compiler/Parsing/Parser.cs): 前置・後置・二項式、優先順位、型変換、`is`、引数、リテラル。
- [Expressions](Kimi/Compiler/Parsing/Koto/Expressions): 各構文の Koto。クラスが存在することと意味が実装済みであることは別。
- [ControlFlowAnalysis.cs](Kimi/Compiler/Analysis/ControlFlowAnalysis.cs): 結果・制御移動・短絡経路・生ポインターの部分的検査。
- [ParserRegressionTest.cs](xUnitTest/Tests/ParserRegressionTest.cs)、[SpecConformanceParseTest.cs](xUnitTest/Tests/SpecConformanceParseTest.cs): 優先順位、`is`、引数ラベル、タプル、型引数など。
- [ExpressionPrecedenceTest.cs](xUnitTest/Tests/ExpressionPrecedenceTest.cs): 明示的な括弧との構文木比較、比較の連鎖の診断、変換と型引数の境界、再出力・再解析。
- [CollectionLiteralParseTest.cs](xUnitTest/Tests/CollectionLiteralParseTest.cs)、[RangeIndexParseTest.cs](xUnitTest/Tests/RangeIndexParseTest.cs): コレクション・境界・添字の構文。
- [ControlFlowAnalysisTest.cs](xUnitTest/Tests/ControlFlowAnalysisTest.cs)、[ControlFlowRevisionParseTest.cs](xUnitTest/Tests/ControlFlowRevisionParseTest.cs): 制御構文と結果検査。

補完設計の参考（各言語の規則をそのまま採用する意味ではない）:

- 値と場所の区別、通常のオペランドの左から右の評価、比較に括弧を要求する方針は [Rust Reference: Expressions](https://doc.rust-lang.org/reference/expressions.html) を参考にした。
- 代入の Unit と整数演算の境界条件は [Rust Reference: Operator expressions](https://doc.rust-lang.org/reference/expressions/operator-expr.html) を参照した。ただし Kimigayo は単純代入も左辺の確定から行い、Debug / Release でオーバーフロー規則を変えない。
- 条件内の代入ミスを型で防ぐこと、既定の算術でオーバーフローを検出することは [Swift: Basic Operators](https://docs.swift.org/swift-book/documentation/the-swift-programming-language/basicoperators/) を参考にした。
