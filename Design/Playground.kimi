alias AliasB

#If(os == "windows" and 
    pointerWidth == 64)
alias AliasC

#If(windows and 1 == 0x_001)
alias AliasD

private func find<T>(
    value?: T,
    in => collection: Collection<T>,
    using => comparer: (T, T) -> Bool
) -> i32
    where T is Comparable
    return 0

public func Main() -> ()
    return

public open struct Test: @A, @B,
    public static #Test1 var #Test2 x: i32 = #One 1
    private let pi = 1.2

var x:&unsafe Base.Map<String, Int>
var count: i32 = #TestAtr 0
let name: String = "abc"
var items: Array<Int> // = [1, 2, 3]
var items2 // = [1, 2, 3, ]
var map: Map<String, Int>// =  ["A": 1, "B": 2]
var array: Array<&owner T.U> = new()
var obj = T.New() // T: stack
var obj2 = &owner T.New() // T.New()
var movedObj = &owner obj // obj => movedObj
var movedObj = &unsafe obj
array.Add(movedObj) // movedObj => array
