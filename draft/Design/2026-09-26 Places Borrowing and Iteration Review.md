# Place・借用・列挙：改善案の採否一覧

日付: 2026-09-26

対象: [仕様変更案：Place・取得・借用・列挙の統一](2026-09-25%20Places%20Borrowing%20and%20Iteration.md)

状態: 提示された改善案A1–A16、P1–P4の判定を変更案へ反映済み。正式仕様への取り込み・実装・実行検証は未実施。

本書は採否と理由の記録であり、規則は対象文書を参照する。番号は提示された改善案に対応する。「部分採用」は修正採用・分割採用を含む。「不採用」は提示された解決策を採らないという意味であり、指摘された不便さや改善目的まで否定するものではない。

個別20件の内訳は、**採用9件、部分採用7件、不採用4件**。採用には、既存規則を維持して参照を明確にしたP3を含む。

## 1. 採用

| 番号 | 改善案 | 反映結果 | 理由 | 本文の場所 |
| --- | --- | --- | --- | --- |
| A4 | 期待型が確定した値位置の暗黙適合を統一 | 引数・型注釈付き初期化・代入・return・集約要素・payload・既定値に、共有借用・再借用・Scalar readの共通表を適用。期待型がない位置は通常取得 | 文脈ごとの不必要な差をなくす。型推論の逆算、寿命延長、新しい排他借用の暗黙生成は不要 | §3.2–3.5 |
| A6 | Scalar readで複数の参照層をたどる | 安全な値参照の終端がScalarの場合に読み取る。束縛型・更新先・非ScalarのCopy型は変更しない | 参照を格納した配列やguardでも自然に計算でき、参照の平坦化を一般化せずに済む | §3.3 |
| A7 | 所有objにルートのlet／var制約を適用 | 所有経路はルートの可変性、借用経路は参照の権限に従う。所有ハンドルのLoanはpayloadも保護。letからの明示Moveは可能 | Tからobj/Tへ格納方法を変えても所有者の更新制限を保てる。スロット経由の借用とpayload更新の競合も防ぐ | §2.4 |
| A8 | for varの文法を定義 | ForSlotに`var Name`を追加。裸の名前はlet。varは反復ローカルの再代入だけを許す | 未定義だった例を正規の構文にし、参照先の権限とローカルの可変性を区別する | §8.3 |
| A10 | 配送値のElementを改名 | IndependentIteratorの関連型をStableItemへ変更 | 格納要素のElementと、参照を含めた配送値全体の型を区別する | §8.5–8.6 |
| A11 | 用語を整理 | Provisionを削除。Storage／Place、Take／Move／Consumeの対応を記載。Originを除く比較は「型形状」とする | 重複用語と既存のnormalized Type identityとの衝突を避ける。権限・操作・解析上の効果は区別して残す | §2.1、§6.1 |
| A14 | @derefを後置の優先順位にする | メンバー・添字と同じlevel 1へ配置。前置演算子・他の@操作との結合例を追加 | 場所の選択として自然な結合になり、負号などとの誤読を減らす | §2.3 |
| A16 | 正式仕様との変更境界を補完 | 所有receiver、関連型のCore制限、Tuple分解、for文法、演算子優先順位、付録D／E／Fなどを列挙 | 新規則と旧規則の併存・記載漏れを防ぐ。今回は取り込み先の一覧だけを更新 | §10.1 |
| P3 | 参照の保証を最適化に利用 | 既存§21.5.5を適用することを明記。条件を満たす参照引数の属性を維持し、Place結果への無条件noaliasは禁止 | 既存の共通契約で足りる。ハンドルのinline格納の保証を要素領域へ拡張しない | §9.2 |

## 2. 部分採用

| 番号 | 改善案 | 反映結果・採らなかった部分 | 理由 | 本文の場所 |
| --- | --- | --- | --- | --- |
| A1 | Placeからの移動を@moveだけにする | 明示Moveを採用し、裸のNon-Copy・Copy未証明Placeの通常取得を拒否。必要な@moveを欠く値渡し候補は適用不可とする。取得済みの所有Subject・itemの分解は転送として別に規定 | overload追加で共有借用が暗黙Moveへ変わることを防ぐ。全ての内部転送へ@moveを要求せず、適用可能な候補間の値渡し優先を維持する | §3、§4.1–4.3、§7.2 |
| A2 | Subject・構造Patternでも参照追跡を省略 | forの借用入口、Case・Tuple・Literalの構造選択に適用。明示@ref／@uniqは直前のスロットを借りる。ByValueで参照先の所有権を得る読み替えはしない | 参照コレクションの列挙やTuple分解を簡潔にしつつ、スロット借用と共有権限上限を維持する。単独名の束縛型も変えない | §2.5、§7.1–7.2、§8.3 |
| A5 | IndependentIteratorを型条件だけへ簡略化 | 専用の効果体系・証明書を廃し、型等式と共通の所有権・Loan・効果検査へ統合。領域分割と契約付きUnsafe境界を共通規則として定義。実際の依存や非重複の条件は残す | Originは有効期間の契約であり、型等式だけでは全ての実際の依存を表せない。標準・ユーザー実装に同じ構築責務を適用する | §8.5、§9.1 |
| A9 | Cursorを削除し、fused保証を標準型へ限定 | 一般Iteratorのfused義務は削除し、forは最初のNoneで終了。標準型の保証は維持。Cursorとアダプターは任意のライブラリAPIとして残す | Cursorには現在位置への反復アクセスという別の用途がある。全ユーザーIteratorへ終了状態の維持を要求する必要はない | §8.1、§8.6 |
| A12 | 関連型の修飾構文を一種類にする | 使用側は`T.(Contract).Item{a}`へ統一。実装指定の`associate Contract.Item{a}`は残し、役割と空白表記を整理 | 型の投影とSelfの実装指定は異なる操作。同じ役割の表記を統一し、宣言側の合理的な省略を維持する | §6.2–6.3 |
| P1 | nonnull形式のOptionを一語にする | 指定した七形式に限定してnull=Noneの表現を採用。入れ子のOptionなどへ一般化せず、関数ABIは別に扱う | 格納サイズと判定を簡素化できる。ただし格納が16バイトなら必ずメモリ返却、という前提は成立せず、レジスタ数も一律に保証しない | §9.2.1 |
| P4 | Dictionaryの空きを償却O(1)で回収 | 生存数に対する走査保証と、削除に伴う通常管理の償却O(1)を規定。物理的な詰め直しは要求しない | 空きリストや生存entryの連結管理などの実装自由度を残し、再構築の費用と通常の削除管理を混同しない | §9.2 |

## 3. 不採用

| 番号 | 改善案 | 維持する規則・代替策 | 理由 | 本文の場所 |
| --- | --- | --- | --- | --- |
| A3 | 一時SubjectをByValueにし、Iteratorをforが直接扱う | 裸のSubjectは一時値もShared。forは三つの列挙入口に統一。標準IteratorへIntoIterable適合を付け、ユーザー型の最小実装例とアダプターを用意 | 一時値をローカルへ取り出しただけでモードが変わるのを避ける。Iterator専用分岐やIterable系との同時適合禁止を増やさない | §7.1、§8.2、§8.6 |
| A13 | Slice／ResolvedRangeの排他列挙適合を外す | 適合を維持し、MutableIterableをExclusiveIterableへ改名。入口のreceiver取得とItemの型を分離 | 排他入口は可変要素を返す保証ではない。適合を外すより、契約名を正確にして総称処理の適用範囲を保つ方がよい | §8.2、§8.4 |
| A15 | 型指定付き値借用を廃止 | 同じスロットの型一致を検査する形として維持。object Viewのupcastとは区別 | 式中で型を明示でき、総称的な借用記法とも整合する。対象・操作が異なる二形式の効果を無理に揃える必要はない | §2.4、§3.1 |
| P2 | Scalarのref引数を署名だけで値渡しし、アドレス観察を未規定にする | 原案は採らず、アドレス観察・逸出・依存・評価順序を保てると証明した最適化だけを許す | 結果が借用に依存しないことは十分条件ではない。Unsafeでの観察も勝手に無意味にせず、間接呼び出しのABI整合を維持する | §9.2 |

## 4. 共通規則への整理（C）

少数の共通規則へ整理する方向は採用する。ただし、型等式だけによる独立性と、列挙モードだけによる束縛型の決定は採らない。

| 軸 | 最終的な整理 |
| --- | --- |
| 取得 | PlaceはCopy、移動は明示@move。一時値・取得済み所有値の分配は転送 |
| 選択 | receiver・列挙入口・構造Pattern・Scalar readでは限定した参照経路省略。明示スロット借用と単独名の束縛は読み替えない |
| 適合 | 期待型が確定した値位置では一つの共通表を使う |
| 権限 | 所有経路はルートのlet／var、借用経路は参照の権限。共有経路から排他権限を回復しない |
| 独立性 | 型等式と共通Loan・効果検査。領域分割の構築側が非重複・依存保持を保証する |

列挙モードは入口とreceiverの取得方法を決め、**反復で受け取る値の型は選択したIteratorのItem{step}が決める**。例えばSharedのResolvedRangeでもItemはisize、ByValueのSliceでもItemは共有参照である。Patternの場所からの束縛規則と、Iteratorからの値配送を一つの型対応表へ混同しない。

## 5. 確認範囲

本文の規則・例・節参照・変更境界・必須検証例と本一覧を照合した。20件の網羅性と分類件数、UTF-8、コードフェンス、ファイルリンク、節番号の重複を機械確認した。コンパイル時間や実行性能の改善量は未計測であり、記載した性能要件を実装済みの保証とは扱わない。
